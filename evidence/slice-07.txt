ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[PASS] dialogs_open_in_their_own_window
       detects: a dialog is drawn inside the application's own window: it cannot be moved off the document it is asking about, cannot be put on a second monitor, and does not appear in the taskbar. Reported by the operator about Print; it was true of all fourteen
       · ★ About is a real OS window: [[788.0 71.0] - [1348.0 551.0]]
       · ★ Render diagnostics is a real OS window: [[788.0 71.0] - [1308.0 451.0]]
       · ★ Export to DXF is a real OS window: [[788.0 71.0] - [1208.0 631.0]]
       · ★ Recognise text is a real OS window: [[788.0 71.0] - [1348.0 491.0]]
       · ★ Keyboard shortcuts is a real OS window: [[238.0 261.0] - [658.0 741.0]]
       · ★ Settings is a real OS window: [[788.0 71.0] - [1408.0 727.0]]
       · ★ New document is a real OS window: [[788.0 71.0] - [1248.0 591.0]]
       · ★ Apply redactions is a real OS window: [[788.0 71.0] - [1548.0 631.0]]
       artifact: target/ui-verify\dialog-file.about.trace.txt
       artifact: target/ui-verify\dialog-tools.render_diagnostics.trace.txt
       artifact: target/ui-verify\dialog-file.export_dxf.trace.txt
       artifact: target/ui-verify\dialog-file.ocr.trace.txt
       artifact: target/ui-verify\dialog-file.shortcuts.trace.txt
       artifact: target/ui-verify\dialog-file.settings.trace.txt
       artifact: target/ui-verify\dialog-file.new_from_template.trace.txt
       artifact: target/ui-verify\dialog-edit.redact_apply.trace.txt

[PASS] shift_arrows_select_text
       detects: there is no selection inside a text draft — no Shift+arrow, no Ctrl+A, no drag-select. Replacing a word means pressing Backspace once per character
       · launched target/release/pdfce-gui.exe as pid 7716
       · ★ the caret landed in a run: `pdfce-diag text-edit-caret kind=Edit page=0 run=86 len=2`
       · the caret's run holds 2 character(s), so this run presses Shift+Right 2 times
       · ★★★ 2 presses selected 2 characters: `pdfce-diag text-select from=0 to=2 n=2`
       · ★★ and a plain Right dropped it, so no highlight is left behind
       · ★★★ a pointer sweep across the box selected 2 character(s) — the gesture that needed the galley to be shared rather than laid out twice
       artifact: target/ui-verify\text-selection.trace.txt

[PASS] bookmark_can_be_written
       detects: the Bookmarks panel is read-only — it lists an outline and offers no way to add to one, or offers one that does not reach the document, so a drawing exported without bookmarks can never be given its first
       · launched target/release/pdfce-gui.exe as pid 11744
       · the authoring row drew on a document with no outline
       · the outline holds 122 item(s) to begin with
       · Add is inert while the title is empty, as R9 requires of a greyed control
       · the bookmark was written at epoch 1
       · the panel reads 123 item(s) back
       artifact: target/ui-verify\bookmark_add.trace.txt
       artifact: target/ui-verify\bookmark_add.png

[PASS] adopt_widget_puts_a_form_control_back
       detects: inserting a form's pages leaves boxes that draw exactly like form fields and swallow every keystroke, with no way to register them — or a Register control that is drawn and does not reach the document, or one that never asks what name it would use and so offers the operator a guess to accept
       · launched target/release/pdfce-gui.exe as pid 34208, with PDFCE_DIAG_INSERT_PATH pointing at D:/Dev/pdfce/fixtures/synthetic\forms/demo-form.pdf
       · inserted: Inserted 1 page after page 2. 2 form controls need re-registering before they can be filled — Forms, Tab order lists them.
       · 2 unclaimed widget(s) listed
       · 36 row(s) previewed, 36 of them knowing the name they would register under
       · the row is on screen after 2 wheel turn(s)
       · registered at epoch 2: Registered as “FullName”. This document had no interactive form before; it has one now.
       · 1 unclaimed remain — the registered widget left the list, which is the round trip
       artifact: target/ui-verify\adopt_widget.trace.txt
       artifact: target/ui-verify\adopt_widget.png

[PASS] add_text_takes_real_keystrokes
       detects: the Add text tool arms and places a caret, and characters typed on the real keyboard never reach the draft — so the operator gets a caret that ignores them and nothing is ever written to the page
       · launched target/release/pdfce-gui.exe as pid 38324
       · Edit > Add text armed the caret in Add mode
       · the click on blank paper started an Add draft
       · real keystrokes reached the draft — the OS -> egui -> draft link holds
       · clicking away committed the draft: `add-text` reached the engine
       artifact: target/ui-verify\add_text.trace.txt
       artifact: target/ui-verify\add_text_draft.png

[PASS] every_declared_chord_dispatches
       detects: a keyboard shortcut the manifest declares — and the menus print beside its command — reaches no command when pressed, so the operator's keyboard silently does nothing and the only evidence is that they stop using it
       · launched target/release/pdfce-gui.exe as pid 26740
       · control chord Ctrl+2 arrived, so the input channel works
       · all 7 chords dispatched the command the manifest binds them to
       · NOT covered here: `F11`. It resizes the window mid-run and would invalidate every rect already measured; its dispatch is covered by the unit gate. Nor is this list swept from the manifest — a chord added tomorrow is not pressed here until it is added above, which is what `every_chord_the_manifest_binds_actually_fires` is for
       artifact: target/ui-verify\chords.trace.txt

[PASS] text_annot_places_and_authors
       detects: the text box control does not arm, the drag authors an EMPTY annotation on release instead of asking for words, the dialog never appears, or accepting it writes nothing to the document
       · launched target/release/pdfce-gui.exe as pid 15636
       · Markup > Text box armed the text-annotation tool
       · the page carries 0 annotation(s) before the drag
       · dragging a 220x220 pt box: screen (1173, 557) -> (1238, 492)
       · the release authored nothing — still 0 annotation(s) — and opened the dialog instead, which is the property that separates this family from the seven geometric kinds
       · Accept authored: the page went from 0 to 1 annotation(s)
       artifact: target/ui-verify\text_annot.trace.txt

[PASS] text_annot_takes_the_keyboard_unclicked
       detects: the text-annotation dialog opens and does not hold the keyboard, so an operator who types straight into it — which is what the window is asking them to do — gets nothing, and Accept stays greyed
       · launched target/release/pdfce-gui.exe as pid 36092
       · Markup > Text box armed the text-annotation tool
       · the drag opened the dialog and it declared its text field
       · typed two characters WITHOUT clicking the field, the way an operator does
       · the unclicked dialog took the keystrokes: Accept authored, 0 -> 1 annotation(s)
       · and it is on the page: 324 of 4225 pixels (7.7%) inside the box changed
       artifact: target/ui-verify\text_annot_focus.trace.txt
       artifact: target/ui-verify\text_annot_focus.before.png
       artifact: target/ui-verify\text_annot_focus.after.png

------------------------------------------------------------------------
  8 passed, 0 failed, 0 skipped

RESULT: PASS — every check drove the application and every assertion held.
