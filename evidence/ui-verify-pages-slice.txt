ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[PASS] pages_stay_drawn_when_you_scroll_back
       detects: the page cache holds only what is on screen, so scrolling a sheet away and back re-renders it from the content stream — 691 ms on a dense drawing, on every return, for ever. Invisible to every other oracle: a re-rendered page and a remembered one are the same picture, and the only symptom is a person waiting
       · launched target/release/pdfce-gui.exe as pid 37200
       · scrolling away drew 2 page(s): 3, 4
       · ★★ scrolled 40 notches away and 40 back, and every one of the 2 page(s) drawn was drawn ONCE: 3, 4
       artifact: target/ui-verify\page_cache.trace.txt

[PASS] page_ops_round_trip
       detects: The whole Pages tab does nothing: rotate, move and delete are registered, drawn, offered by a context menu and bound to chords, and every press traces `command-unimplemented` — or reaches the engine and leaves the shell's page vector, its rasters and its selections describing a document that no longer exists
       · launched target/release/pdfce-gui.exe on D:/Dev/pdfce/fixtures/synthetic/pageops/four-pages.pdf as pid 24556
       · fixture D:/Dev/pdfce/fixtures/synthetic/pageops/four-pages.pdf — 4 pages, 2453 bytes, digest 7a0de778c67103df, no `/Rotate` anywhere
       · ★ extract: a ribbon click reached the picker and the picker's answer reached a write — 1205 bytes of freestanding PDF at target/ui-verify\page-ops-round-trip.pdf, `pdfce-diag extract path="target/ui-verify\\page-ops-round-trip.pdf" pages=1 bytes=1205 asked=1`
       · rotate: `pdfce-diag pages-resync was=4 now=4 renumbered=0 page=1 epoch=1`
       · move down: `pdfce-diag pages-resync was=4 now=4 renumbered=1 page=1 epoch=2`
       · delete: `pdfce-diag pages-resync was=4 now=3 renumbered=1 page=1 epoch=3`
       · the copy was written: `pdfce-diag save-copy path="target/ui-verify\\page-ops-round-trip.pdf" bytes=2844 appended=391 objects=2 verbatim=0 reserialized=2 promoted=0 deleted=2 identical=false delinearized=false epoch=3 origin=Opened`
       · ★ the opened document is byte-identical after a run that deleted one of its pages — 2453 bytes, digest 7a0de778c67103df
       · ★ the saved copy's appended revision carries `/Rotate 90` 1 time(s), and the source carries no `/Rotate` at all — the turn an OS-injected click asked for is in the file
       · launched target/release/pdfce-gui.exe on target/ui-verify\page-ops-round-trip.pdf as pid 30544
       · ★★ ROUND TRIP PROVEN: a second process opened the saved copy from disk and its page tree has 3 pages, one fewer than the fixture's 4. The delete, the reorder that decided WHICH sheet was deleted, and the rotation that survived both are in the file the second process read
       · NOT covered here: the four chords (`[`, `]`, `Alt+Up`, `Alt+Down`). Synthetic keystrokes do not reach the target window from this session (see find_bar), so they are covered by the manifest's keymap test and by the single dispatcher every route shares, and the gap is on the record rather than implied by a green result
       · NOT covered here: the page tile's context menu, which offers these same six verbs. An egui popup declares no ui-rect regions, so there is nothing to aim a click at — and both routes reach `PdfceApp::dispatch_command`, which is what one choke point is for
       · NOT covered here: what is INSIDE the extracted file. `pages.extract` and `file.save_copy` share one PDFCE_DIAG_SAVE_PATH seam, so phase E overwrote phase A2's file. The join — click to picker to write, landing a freestanding PDF — is phase A2's; the content is covered by `app::actions::pages`' unit tests, which write an extraction and load it back, including one carrying an unsaved rotation
       artifact: target/ui-verify\page_ops.trace.txt
       artifact: target/ui-verify\page-ops-round-trip.pdf
       artifact: target/ui-verify\page_ops.reopen.trace.txt

[PASS] pages_drag_shows_where_it_lands
       detects: a page thumbnail cannot be dragged to a new position, or it can and nothing shows where it will land — so reordering is the two one-step ribbon arrows, and moving a sheet across a drawing set is dozens of presses with no preview of the result
       · launched target/release/pdfce-gui.exe as pid 45184
       · the Pages panel is on screen
       · dragging tile 0 from (856, 374) to the right half of tile 1 at (1028, 374)
       · gesture began: `pdfce-diag page-drag-start slot=0 origin=0 carrying=1`
       · the insertion caret was drawn inside the grid, 185 pt tall
       · gesture ended: `pdfce-diag pages-drag-release gap=2 moving=1 reordered=1`
       · the reorder reached the document through the action funnel
       artifact: target/ui-verify\pages_drag.trace.txt

[PASS] two_documents_get_two_tabs
       detects: opening a second PDF replaces the first, or opens beside it with no tab to say so — so the operator has no way to see what they have open and no way back to it
       · launched target/release/pdfce-gui.exe as pid 44012
       · one document, one tab
       · two documents, 2 tabs
       · second document arrived: `pdfce-diag open ok pages=36 path="D:/Dev/temp/pdfce/SW41177.pdf"`
       · clicking the first document's tab at (854, 193)
       · the click switched back to the first document
       artifact: target/ui-verify\document_tabs.trace.txt

[PASS] document_tabs_can_be_rearranged
       detects: a document tab cannot be dragged to a new position — the strip's order is whatever order the files were opened in and stays that way — or it can, and doing it switches the operator to a different document
       · launched target/release/pdfce-gui.exe as pid 42896
       · two documents, slot 1 on screen: `pdfce-diag doc-tabs open=2 active=1 drawn=2 hidden=0`
       · dragging tab 0 from (854, 193) past the middle of tab 1 at (995, 193)
       · the strip took the drop: `pdfce-diag document-reorder from=0 gap=2 to=1 active=0`
       · the same document is still on screen, at its new slot
       artifact: target/ui-verify\tab_reorder.trace.txt

[PASS] a_page_dragged_between_documents_is_copied
       detects: a page cannot be dragged from one open document into another — the drag does not survive the document switch, or no tab springs open under it, or the release lands nowhere — so combining two drawings means Insert-from-file and a dialog
       · launched target/release/pdfce-gui.exe as pid 15168
       · 2 documents open
       · dragging tile 0 from (852, 336), resting 1400 ms on the first document's tab at (854, 193), releasing at (884, 336)
       · the tab sprang open: `pdfce-diag doc-tab-spring slot=0 dwell=0.61`
       · the drag survived the switch: `pdfce-diag pages-drag-release from-slot=1 gap=1 moving=1 copied=1 take=0`
       · came out of slot 1
       · no modifier, and the release asked for a copy
       · the target document went from 4 to 5 pages
       · and the source was left alone, as a copy must
       artifact: target/ui-verify\page_drag_between_documents.trace.txt

[PASS] a_shift_drag_between_documents_moves_the_pages
       detects: holding Shift while dragging a page into another document still copies it — so Windows' own move modifier is inert, and an operator who used it is left with the sheets in both documents and no indication of which one they meant
       · launched target/release/pdfce-gui.exe as pid 44392
       · 2 documents open
       · dragging tile 0 from (852, 336), resting 1400 ms on the first document's tab at (854, 193), releasing at (884, 336)
       · the tab sprang open: `pdfce-diag doc-tab-spring slot=0 dwell=0.61`
       · the drag survived the switch: `pdfce-diag pages-drag-release from-slot=1 gap=1 moving=1 copied=1 take=1`
       · came out of slot 1
       · Shift was held, and the release asked for a move
       · the target document went from 4 to 5 pages
       · and the source lost them: `pdfce-diag page-move-took slot=1 asked=1 removed=1`
       artifact: target/ui-verify\page_drag_between_documents.trace.txt

------------------------------------------------------------------------
  7 passed, 0 failed, 0 skipped

RESULT: PASS — every check drove the application and every assertion held.
