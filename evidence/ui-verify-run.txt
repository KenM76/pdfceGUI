ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[PASS] delete_key_after_canvas_click
       detects: D1 — the canvas takes egui focus on click, and the keyboard guard tests 'any widget focused' rather than 'a text field focused', so Delete is permanently suppressed from the first canvas click onward
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 36580 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · window client area 1100x800 px at desktop (788, 71), DPI scale 1.00
       · clicked the Edit mode segment first — the shell's default mode is Read, where a canvas click on content is refused BY DESIGN (DEFECTS.md D6). Without this step the check reports the mode gate as a selection defect, which is what it did before 2026-08-17
       · canvas rect LRect { min: Pt { x: 296.0, y: 285.7 }, max: Pt { x: 764.0, y: 616.3 } } at zoom 0.196
       · document point (page 0, 300.0, 500.0) → window (354.9, 518.1) → screen (1143, 589) at DPI scale 1.00
       · precondition holds: the click hit ? object(s) and the selection is 1 object(s)
       · oracle: the page object count, which is 28 before the key. This measures the property the check is about rather than the verb meant to change it.
       · page object count after Delete: 27
       · the page object count fell from 28 to 27 across the keystroke
       artifact: target/ui-verify\delete_key.trace.txt

[PASS] ribbon_group_captions_legible
       detects: ribbon group captions rendering below a readable contrast, or not rendering at all — the 2026-08-08 audit found two groups with no caption
       · launched target/scratch/drive/pdfce-gui.exe as pid 38184
       · window client area 1100x800 px at desktop (788, 71), DPI scale 1.00
       · the application declared 97 ui-rect region(s): canvas-viewport, central-panel, doc-tab.0, doc-tabs, dock.body.file.properties, dock.body.view.panel_forms, dock.body.view.panel_layers, dock.body.view.panel_objects, dock.body.view.panel_pages, dock.body.view.panel_tool, dock.left, dock.left.0, dock.left.0.0, dock.left.0.0.tabbar, dock.left.0.1, dock.left.0.1.tabbar, dock.left.0.split.row.0, dock.left.collapse, dock.left.split.side, dock.right, dock.right.0, dock.right.0.0, dock.right.0.0.tabbar, dock.right.0.1, dock.right.0.1.tabbar, dock.right.0.2, dock.right.0.2.overflow, dock.right.0.2.tabbar, dock.right.0.split.row.0, dock.right.0.split.row.1, dock.right.collapse, dock.right.split.side, dock.tab.file.fonts, dock.tab.file.properties, dock.tab.markup.comments, dock.tab.view.panel_bookmarks, dock.tab.view.panel_forms, dock.tab.view.panel_layers, dock.tab.view.panel_objects, dock.tab.view.panel_pages, dock.tab.view.panel_signatures, dock.tab.view.panel_tool, forms.tab_order.header, page, panel-pages, panel-pages-current-tile, panel-pages-grid, panel-pages-tile.0, panel:tool, properties.info, properties.info.0, properties.info.1, properties.info.2, properties.info.3, ribbon.group.file.export.collapsed, ribbon.group.file.file, ribbon.group.file.file.caption, ribbon.group.file.print, ribbon.group.file.print.caption, ribbon.group.file.recognise.collapsed, ribbon.group.file.save, ribbon.group.file.save.caption, ribbon.item.file.close, ribbon.item.file.new, ribbon.item.file.new_from_template, ribbon.item.file.open, ribbon.item.file.print, ribbon.item.file.save, ribbon.item.file.save_compacted, ribbon.item.file.save_copy, ribbon.mode.edit, ribbon.mode.read, ribbon.mode.review, ribbon.modes, ribbon.overflow, ribbon.qat.edit.redo, ribbon.qat.edit.undo, ribbon.qat.file.open, ribbon.qat.file.save, ribbon.tab.file, ribbon.tab.view, status-bar, status-group:filter, status-group:find, status-group:fit, status-group:notes, status-group:page, status-group:zoom, status-page-box, tool.pointer, tool.row.markup.rectangle, tool.row.measure.linear, tool.row.view.tool_hand, tool.row.view.tool_node, tool.row.view.tool_select, tool.row.view.tool_text, tool.tools
       · 3 of them follow the ribbon-caption naming convention
       · region set `ribbon_group_captions` — 3 region(s); source: the application's own ui-rect declarations, this run
       · provenance: declared by the application itself as `ui-rect` events on the frames they were laid out for, out of 97 region(s) it declared in total
       · surface 1100x800 px; threshold 3.0:1 (WCAG 2.1 AA)
       ·   ribbon.group.file.file.caption           4.26:1 (fg #727273 6.4% on bg #F1F1F2 53.6%, 140 px sampled)
       ·   ribbon.group.file.print.caption          4.20:1 (fg #737374 2.6% on bg #F1F1F2 60.0%, 190 px sampled)
       ·   ribbon.group.file.save.caption           4.14:1 (fg #747475 2.2% on bg #F1F1F2 55.0%, 180 px sampled)
       artifact: target/ui-verify\ribbon_captions.trace.txt
       artifact: target/ui-verify\ribbon_captions.png

[PASS] qat_controls_are_icon_only
       detects: the quick-access toolbar falls back to text labels because no icon painter was supplied
       · launched target/scratch/drive/pdfce-gui.exe as pid 29768
       · 4 quick-access control(s) declared their rects
       · ribbon.qat.file.open: 24.0 x 24.0 pt, aspect 1.00
       · ribbon.qat.file.save: 24.0 x 24.0 pt, aspect 1.00
       · ribbon.qat.edit.undo: 24.0 x 24.0 pt, aspect 1.00
       · ribbon.qat.edit.redo: 24.0 x 24.0 pt, aspect 1.00
       artifact: target/ui-verify\qat_icons.trace.txt

[SKIP] new_document_makes_a_page
       detects: File ▸ New does not make a document, or makes one whose page will not draw
       · launched target/scratch/drive/pdfce-gui.exe as pid 36192
       → the fixture opened with 1 page(s). This check's whole evidence that New
         REPLACED the document is the page count changing to 1, and it cannot change
         to 1 from 1. Pass a multi-page document.
       artifact: target/ui-verify\new_document.trace.txt

[SKIP] new_document_sizes_the_page
       detects: the size chooser reports the sheet it was asked for and makes an A4 page
       · launched target/scratch/drive/pdfce-gui.exe as pid 12380 with no document
       · the dialog opened at LRect { min: Pt { x: 0.0, y: 0.0 }, max: Pt { x: 460.0, y: 520.0 } }
       → the click on `new-document.size` published no `new-document.size.item.3`.
         The popup did not open, or opened and closed within the settle. Entries
         declared: none. Reported as SKIPPED: this is a harness timing question, not
         an application claim.
       artifact: target/ui-verify\new_document_size.trace.txt

[PASS] dragging_a_markup_moves_it
       detects: a placed markup can be selected, restyled and deleted and cannot be MOVED — the drag is consumed by the annotation branch of the canvas fork and discarded there, so an operator drags a stamp across the sheet, lets go, and it is where it started with no message anywhere
       · launched target/scratch/drive/pdfce-gui.exe as pid 34356 with PDFCE_DIAG_INVOKE=mode.review,markup.rectangle
       · ★ a rectangle was authored: `pdfce-diag markup-commit kind=Rectangle page=0 x0=835.42 y0=587.24 x1=1309.16 y1=841.94`
       · ★ the shape was selected: `pdfce-diag annot-select page=0 id=ObjId { num: 47, generation: 0 } kind=Markup subtype=Square locked=false rect=[[835.4 841.8] - [1309.2 1096.5]]`
       · ★★ the drag decided to move it: `pdfce-diag annot-drag id=47 dx=402.427 dy=331.111`
       · ★★★ the engine moved it: `pdfce-diag move-annotation-applied id=47 dx=402.427 dy=331.111 keys=0 popup=false`
       · ★ moved dx=402.43 pt, dy=331.11 pt — both axes travelled, geometry keys reported as 0
       artifact: target/ui-verify\markup-move.trace.txt

[PASS] markup_rectangle_arms_from_the_ribbon
       detects: clicking Markup ▸ Shapes ▸ Rectangle does not arm the canvas tool, or arms it without the control rendering pressed — a four-link chain in which every link has a passing unit test and no test observes two of them connected
       · launched target/scratch/drive/pdfce-gui.exe as pid 35112 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · window client area 1100x800 px at desktop (788, 71), DPI scale 1.00
       · ribbon.mode.review declared at LRect { min: Pt { x: 972.6, y: 2.0 }, max: Pt { x: 1032.3, y: 26.0 } }; clicking its centre
       · the Review segment reported the click, so pointer input reaches the ribbon
       · ribbon.tab.markup declared at LRect { min: Pt { x: 245.0, y: 2.0 }, max: Pt { x: 295.2, y: 26.0 } }; clicking its centre
       · the Markup tab reported the click
       · 24 command control(s) declared their rects on the Markup tab
       · ribbon.item.markup.rectangle at LRect { min: Pt { x: 14.0, y: 35.0 }, max: Pt { x: 38.0, y: 59.0 } }, ribbon.item.markup.ellipse at LRect { min: Pt { x: 42.0, y: 35.0 }, max: Pt { x: 66.0, y: 59.0 } }, parking on ribbon.group.markup.shapes.caption at LRect { min: Pt { x: 137.3, y: 93.0 }, max: Pt { x: 165.2, y: 103.0 } }
       · before the click: Rectangle fills #E8E8EA, Ellipse fills #E8E8EA (max channel gap 0)
       · the shell traced `ribbon-command-invoked id=markup.rectangle`, so the click reached the control
       · the application traced `markup-tool tool=Markup(Rectangle)`, so the canvas tool is armed
       · after the click: Rectangle fills #C8D5EB, Ellipse fills #E8E8EA
       · P1 Rectangle across the click: 32; P2 Rectangle vs Ellipse in one capture: 32; P3 Ellipse across the click: 0; threshold 12
       · pressed rendering confirmed from the pixels: the clicked control changed, its un-clicked sibling did not, and the two differ in one capture
       artifact: target/ui-verify\markup_rectangle.trace.txt
       artifact: target/ui-verify\markup_rectangle.before.png
       artifact: target/ui-verify\markup_rectangle.after.png

[FAIL] form_field
       detects: the five form-field commands arm a tool and a click on the page places nothing — or a field is placed and clicking it again offers no way to rename or delete it, leaving every form pdfce authors editable only by authoring it again
       · launched target/scratch/drive/pdfce-gui.exe as pid 33820 with PDFCE_DIAG_INVOKE=mode.edit,edit.form_text_field and PDFCE_DIAG_FORM_ACCEPT=1
       · the text-field tool is armed
       · clicking empty page at PDF (300, 500) on page 0
       · the dialog opened for kind=Some("Text") named Some("Text1")
       · a field was authored
       · aiming at the field "Text1" the application placed at canvas (381.1, 1173.4)
       · selected Some("Text1") on page Some("0")
       · the form-field properties section drew
       → ★ REQUIRED WAS TICKED AND NOTHING REACHED THE DOCUMENT: no new
         `edit-field` line. Three candidates. (1) **The click missed** — the region
         was declared, so the screenshot beside this report settles it. (2) **The
         press raised no action**, which on a plain `ui.checkbox` means the section
         re-drew between the press and the read. (3) **`edit_field` refused** — the
         engine checks its gates against the RESULTING field, so a refusal can name a
         property the request never mentioned; the status bar carries the sentence
         and the trace carries the decline. Trace:
         target/ui-verify\form_field.trace.txt.
       artifact: target/ui-verify\form_field.trace.txt

[SKIP] blend_space
       detects: the same page renders different colours at different zooms, with nothing on screen saying so — the operator compares a patch against a reference, sees it disagree, and has no way to learn that zooming out would make it agree
       · page 2384x1684 pt; the CMYK buffer ceiling is crossed at zoom 183%
       · opened at zoom 20%
       · no blend-space disclosure at fit zoom, which is correct
       · the last raster was spawned at scale 2.16; the ceiling is at 1.83
       · climbed to zoom 216% in 3 batch(es); the crossing is at 183%
       → the zoom passed the 183% crossing and the renderer never engaged the CMYK
         compositing buffer at all — no `raster-blend-space` line reports
         `cmyk_buffer=true`. That is a fact about the FIXTURE: a page with no
         transparency composites nothing, so nothing can fall back to sRGB and there
         is correctly nothing to disclose. Line-work drawings are the common case,
         and this shell's usual `--pdf` is one. SKIPPED rather than failed. Point
         --pdf at a document that uses transparency — this check was written
         against the industry print-conformance suite's composite page, which needs
         `--page-size 596x791` since its page box cannot be read from the file.
         Trace: target/ui-verify\blend_space.trace.txt.
       artifact: target/ui-verify\blend_space.trace.txt

[SKIP] progressive
       detects: panning or zooming out at high magnification blanks the page until the new raster arrives — the operator loses sight of the drawing for as long as it takes to render, on exactly the gesture they use to find their way around it
       · aiming the wheel at PDF (300, 500) on page 0
       · climbed to zoom 2385%
       · 48 ink pixel(s) on the canvas before the gesture
       → only 48 ink pixel(s) on the canvas before any gesture — the view is over
         blank paper, so nothing below would mean anything. Aim --doc-point at
         content.
       artifact: target/ui-verify\progressive.trace.txt

[PASS] markup_freehand_and_vertex_kinds
       detects: Markup ▸ Freehand draws nothing or writes the raw pointer trail unsimplified, a click with Polyline or Polygon armed places no vertex, or Finish shape is live when there is nothing to finish — the three kinds `FEATURES.md` carried as engine-ready-but-not-drag- shaped, and the two call sites in their chain that no unit test in the workspace can observe
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 37168 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · the review segment and the Markup tab both reported their clicks
       · with nothing clicked out, a press of `ribbon.item.markup.finish` produced no `ribbon-command-invoked` — the control is greyed, which is the state phases C and E have to make escapable
       · Freehand armed
       · the freehand stroke committed: `pdfce-diag markup-commit kind=Ink page=0 raw=9 kept=5`
       · ★ MEASURED THROUGH THE RUNNING BINARY: the OS-injected drag produced 9 raw trail point(s) and `markup::ink::simplify` kept 5 — 44 % discarded. The drag this harness can deliver is a straight line, so this is the easy case and the figure is reported rather than bounded; the quality measurement is the unit test's, on a curve with a wander and a jitter on it (`canvas::markup::ink` section 3.3)
       · the engine authored the /Ink annotation
       · three canvas clicks placed three vertices: pdfce-diag markup-vertex kind=PolyLine page=0 n=1 x=713.16 y=760.44, pdfce-diag markup-vertex kind=PolyLine page=0 n=2 x=1380.48 y=791.00, pdfce-diag markup-vertex kind=PolyLine page=0 n=3 x=1049.37 y=1111.92
       · pressed `ribbon.item.markup.finish` — the shell reported 1 invocation(s) of `markup.finish` after it, 0 before
       · ★ the SAME control phase A found greyed authored a polyline: `pdfce-diag markup-commit kind=PolyLine page=0 vertices=3 x0=713.16 y0=760.44 xn=1049.37 yn=1111.92`, by the command ending. The double-click ending is not driven here (see the module header) and is covered by the unit test that runs BOTH and compares the actions
       · pressed `ribbon.item.markup.finish` — the shell reported 1 invocation(s) of `markup.finish` after it, 1 before
       · ★ FALSIFIER CLEAR: with a run two corners deep, `ribbon.item.markup.finish` is greyed for a Polygon where the identical run left it live for a Polyline. The condition really asks `markup::action`, rather than being published for any run or for none
       · pressed `ribbon.item.markup.finish` — the shell reported 2 invocation(s) of `markup.finish` after it, 1 before
       · the polygon committed: `pdfce-diag markup-commit kind=Polygon page=0 vertices=3 x0=713.16 y0=760.44 xn=1049.37 yn=1111.92`
       · pressed `ribbon.item.markup.finish` — the shell reported 2 invocation(s) of `markup.finish` after it, 2 before
       · a second press of `ribbon.item.markup.finish` authored nothing: the run was emptied by its own commit
       · pressed `ribbon.item.markup.finish` — the shell reported 3 invocation(s) of `markup.finish` after it, 2 before
       · ★ the revision cloud committed as its OWN kind rather than as a polygon: `pdfce-diag markup-commit kind=Cloud page=0 vertices=3 x0=713.16 y0=760.44 xn=1049.37 yn=1111.92`
       · the window carrying the freehand stroke, the polyline, the polygon and the revision cloud is saved beside the trace. Evidence, not the oracle — and note that the cloud's scallop is the one thing in it a reader COULD check by eye, because it is the only mark on the page whose border is not a straight line
       · not covered here: the double-click ending, and Escape's two rungs (abandon the run, then retire the pen). This check does not drive them; keystrokes DO reach the window and a synthetic double-click is a timing race that would make this check flaky, so both are covered by unit test alone and the gap is on the record rather than implied by a green result
       artifact: target/ui-verify\markup_shapes.trace.txt
       artifact: target/ui-verify\markup_shapes.png

[PASS] markup_style_group_is_drawn
       detects: Markup ▸ Style declares a colour_swatch item that no renderer draws, so the group is a caption over an empty band and the pen is two hard-coded constants
       · launched target/scratch/drive/pdfce-gui.exe as pid 36844
       · all three Style controls are drawn: markup.style.ink, markup.style.highlighter, markup.style.width
       · the width control is live: 6 `markup-pen` line(s), last width_pts=3.5
       artifact: target/ui-verify\markup_style.trace.txt

[PASS] measure_perimeter_traces_and_closes
       detects: the Perimeter tool is on the ribbon and arms nothing, clicks on the page do not become vertices, the running total does not accumulate, or clicking the first vertex fails to close the ring — which leaves an operator who has just traced a footprint unable to finish it, and is invisible to every unit test because the ring test crosses the page-to-canvas bridge and both sides of it are individually correct
       · launched target/scratch/drive/pdfce-gui.exe as pid 22052
       · Measure ▸ Perimeter armed the tool
       · four vertices taken; running total after each: -0.0 → 718.3 → 1222.6 → 1940.8
       · ★ the ring closed and the dimension reached the engine: `pdfce-diag add-dimension page=0 n=1 epoch=1 disclosures=none`
       · ★ the Length tool took all 5 clicks as vertices, so the first-vertex click did not close it
       · the selected perimeter published its vertex handles
       · ★ the corner drag reached the engine: `pdfce-diag move-dimension-vertex page=0 n=1 epoch=2 disclosures=That corner changed the measurement: 2445.13 pt is now 3324.80 pt.`
       · the corner drag asked the snap query and got `snap=none` (a miss is expected here — the destination is deliberately far from the traced square)
       artifact: target/ui-verify\measure_perimeter.trace.txt
       artifact: target/ui-verify\measure_perimeter_preview.png

[PASS] measure_linear_places_a_dimension
       detects: clicking Measure ▸ Dimension ▸ Linear and then three points on the page does not place a dimension — the control does not arm, or arms without rendering pressed, or the picks never reach the tool, or the third pick's action is refused by the engine and the document is left unchanged while the shell reports a commit
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 38496 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · window client area 1100x800 px at desktop (788, 71), DPI scale 1.00
       · the review segment reported the click, so pointer input reaches the ribbon
       · ribbon.tab.measure declared at LRect { min: Pt { x: 299.2, y: 2.0 }, max: Pt { x: 355.1, y: 26.0 } }; clicking its centre
       · the Measure tab reported the click
       · 16 command control(s) declared their rects on the Measure tab
       · ribbon.item.measure.linear at LRect { min: Pt { x: 14.0, y: 35.0 }, max: Pt { x: 77.6, y: 59.0 } }, ribbon.item.measure.two_line at LRect { min: Pt { x: 176.8, y: 63.0 }, max: Pt { x: 253.2, y: 87.0 } }, parking on ribbon.group.measure.dimension.caption at LRect { min: Pt { x: 135.2, y: 93.0 }, max: Pt { x: 177.3, y: 103.0 } }
       · before the click: Linear fills #E8E8EA, Two-line fills #E8E8EA (max channel gap 0)
       · the shell traced `ribbon-command-invoked id=measure.linear`, so the click reached the control
       · the application traced `measure-tool tool=Measure(Linear)`, so the canvas tool is armed
       · after the click: Linear fills #C8D5EB, Two-line fills #E8E8EA
       · P1 Linear across the click: 32; P2 Linear vs Two-line in one capture: 32; P3 Two-line across the click: 0; threshold 12
       · pressed rendering confirmed from the pixels: the clicked control changed, its un-clicked sibling did not, and the two differ in one capture
       · canvas rect LRect { min: Pt { x: 296.0, y: 285.7 }, max: Pt { x: 764.0, y: 616.3 } } at zoom 0.196, showing page 1
       · picks: A (715, 758), B (1550, 758), placement (1311, 1044) in PDF user space
       · pick 1 of 3: click 1 at document (715, 758) → screen (1224, 538)
       · pick 2 of 3: click 1 at document (1550, 758) → screen (1388, 538)
       · pick 3 of 3: click 1 at document (1311, 1044) → screen (1341, 482)
       · the pick sequence is committed=false, false, true — the third pick is the commit, and there is no accept box
       · the engine accepted it: `pdfce-diag add-dimension page=0 n=1 epoch=1 disclosures=none` — the edit epoch is now 1, which is the counter the selection re-resolve and the raster key both read
       artifact: target/ui-verify\measure_linear.trace.txt
       artifact: target/ui-verify\measure_linear.before.png
       artifact: target/ui-verify\measure_linear.after.png

[PASS] measure_calibrates_by_picking_two_points
       detects: the scale can only be set by typing a ratio — the two-point calibration the operator asked for does not arm, does not route its clicks, or re-opens the dialog with no measurement in it
       · launched target/scratch/drive/pdfce-gui.exe as pid 15084
       · the Set-scale dialog opened from Measure > Scale
       · the calibrate button armed the two-point pick and closed the dialog
       · canvas at zoom 0.196; picking two points 400 pt apart
       · pick A: document (300.0, 500.0) -> screen (1143, 589)
       · pick B: document (700.0, 500.0) -> screen (1221, 589)
       · the pick completed and measured 397.33 pt
       · the dialog re-opened on the real-length path, asking what the line represents
       · 397.33 pt is within 150 pt of the 400 pt clicked, so the measurement is of the page and not of something else
       artifact: target/ui-verify\measure_calibrate.trace.txt

[SKIP] measure_hover_shows_what_it_will_take
       detects: a measure tool is armed and the canvas says nothing about what a click would take — so on a drawing made of near-identical strokes the operator picks by guessing, and a two-line angle taken from the wrong line is a confident, plausible, wrong number
       · launched target/scratch/drive/pdfce-gui.exe as pid 16560
       · armed: `pdfce-diag measure-tool tool=Measure(Linear)`
       · found ink after 1 pointer position(s), at ScreenPoint { x: 1318, y: 522 }
       · hovering reported: `pdfce-diag measure-hover-entity object=0 segment=0`
       → the entity under the pointer is not a straight run — `pdfce-diag
         measure-hover-entity object=0 segment=0` — so its outline was drawn
         instead of a segment, and there is no endpoint for a snap marker to sit on.
         The sweep landed on a curve, a text run or an image rather than on a line,
         which is a property of this fixture and not of the application. Drive a
         fixture with straight vector geometry near the middle of the page to
         exercise this. SKIPPED rather than failed: the assertion below can only be
         met by a segment, so applying it here would report correct behaviour as a
         defect.
       artifact: target/ui-verify\measure_hover.trace.txt

[SKIP] dimension_groups_panel_makes_a_group
       detects: Measure > Dimension groups is drawn and does nothing — or it opens a panel that cannot create a group, or creates one the model never gives back, so a second scale on one drawing is unreachable and every dimension lands in the default group for ever
       · launched target/scratch/drive/pdfce-gui.exe as pid 29368
       · the Dimension-groups panel drew after Measure > Dimension groups
       · the six fold headings published their rects; opening and shutting them is NOT driven here — see this function's comment for the three harness fixes that did not make it work, and for why that is a gap in the instrument rather than a verdict on the panel
       · 1 group row(s) listed: dimension-groups.draw_into.0
       · the Add button is published below the bottom of the window — it is inside the panel's `ScrollArea`, so its rect is a position in the scrolled content. Scrolling the panel and re-reading the rect.
       · the group reached the document through the action funnel
       · the new group is listed and carries an authoring radio: dimension-groups.draw_into.0, dimension-groups.draw_into.1
       → the `dimension-groups.heading.identity` heading was declared at least once
         and is not aimable now: twelve reads of its rect never agreed twice running,
         or a `ui-rect-gone` has retired it as the dock re-laid itself out. Either
         way there is no coordinate this check may aim at, and clicking a moving
         target lands somewhere else — a failure indistinguishable from the control
         not working. SKIPPED rather than failed: this says nothing about the panel.
         See `CONTINUE.md` on the dock settling over several frames.
       artifact: target/ui-verify\dimension_groups.trace.txt

[PASS] save_writes_over_the_file_you_opened
       detects: Save is absent, or it opens a file dialog like Save-a-copy, or it writes somewhere other than the file that was opened — or it truncates the original and leaves a partial file when the write fails, which destroys the operator's only copy
       · subject copy at target/ui-verify\save_in_place_subject.pdf — 39509 bytes before
       · launched as pid 37688
       · a markup was authored, so the document is modified
       · ★ Save reported success: `pdfce-diag save-in-place outcome=ok path="target/ui-verify\\save_in_place_subject.pdf"`
       · the file grew 39509 → 40514 bytes, no temporary was left behind, and it still reads as a page tree
       artifact: target/ui-verify\save_in_place.trace.txt

[PASS] save_copy_round_trip
       detects: Save a copy writes nothing, writes over the document that was opened, writes a full rewrite where the command's own tooltip promises an appended update, or writes a file the operator's edit is not in — the five-link chain from a ribbon click to a file on disk, three links of which no test in the workspace can observe
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt, 39509 bytes, digest 355953d66f19b56b
       · launched target/scratch/drive/pdfce-gui.exe on fixtures/a1-titleblock.pdf as pid 29944 with PDFCE_DIAG_SAVE_PATH=target/ui-verify\save-copy-round-trip.pdf
       · the fixture: the Comments panel lists 12 annotation(s) — `pdfce-diag comments-panel pages=1 listed=12 with_note=12 descriptions=0 authors=12 ce_dimensions=0 suppressed=0 unresolved=0 replies=0 group_members=0 excluded_widgets=0 excluded_popups=0 excluded_trapnet=0 excluded_total=0`
       · the rectangle was authored: `pdfce-diag markup-commit kind=Rectangle page=0 x0=570.53 y0=470.08 x1=1380.48 y1=775.72`
       · the Comments panel now lists 13, one more than before the drag — the annotation is on the session in memory
       · the copy was written: `pdfce-diag save-copy path="target/ui-verify\\save-copy-round-trip.pdf" bytes=40520 appended=1011 objects=3 verbatim=0 reserialized=3 promoted=0 deleted=0 identical=false delinearized=false epoch=1 origin=Opened`
       · ★ the opened document is byte-identical after the run — 39509 bytes, digest 355953d66f19b56b
       · ★ the copy's first 39509 bytes are the source's, verbatim, and it is 40520 bytes long — an appended revision, which is what the command's tooltip promises and what preserves any signature the file carries
       · launched target/scratch/drive/pdfce-gui.exe on target/ui-verify\save-copy-round-trip.pdf as pid 27144 with PDFCE_DIAG_SAVE_PATH=target/ui-verify\save-copy-unused.pdf
       · the SAVED COPY: the Comments panel lists 13 annotation(s) — `pdfce-diag comments-panel pages=1 listed=13 with_note=12 descriptions=0 authors=12 ce_dimensions=0 suppressed=0 unresolved=0 replies=0 group_members=0 excluded_widgets=0 excluded_popups=0 excluded_trapnet=0 excluded_total=0`
       · ★★ ROUND TRIP PROVEN: a second process opened the saved copy from disk and its Comments panel lists 13 annotation(s), one more than the fixture's 12. The rectangle an OS-injected drag authored in the first process is in the file the second one read
       · NOT covered here: the save dialog itself. `PDFCE_DIAG_SAVE_PATH` answers it and it is never opened, because it is a modal top-level window owned by the OS shell that no synthetic input can reach (`app::files`' rule 1). Everything else — the ribbon click, the dispatch arm, `Action::SaveCopy`, the apply phase, `to_incremental_bytes` and the write — is the real code path. That the dialog opens pre-filled with the suggested name is covered by `app::save::suggested_path`'s unit tests and by nothing that runs a window
       · NOT covered here: `Ctrl+S`, which is unwritten work — keystrokes DO reach the window, this session (see find_bar), so the chord is covered by the manifest's keymap test and by the single dispatcher every route shares, and the gap is on the record rather than implied by a green result
       artifact: target/ui-verify\save_copy.trace.txt
       artifact: target/ui-verify\save-copy-round-trip.pdf
       artifact: target/ui-verify\save_copy.reopen.trace.txt

[SKIP] pages_stay_drawn_when_you_scroll_back
       detects: the page cache holds only what is on screen, so scrolling a sheet away and back re-renders it from the content stream — 691 ms on a dense drawing, on every return, for ever. Invisible to every other oracle: a re-rendered page and a remembered one are the same picture, and the only symptom is a person waiting
       · launched target/scratch/drive/pdfce-gui.exe as pid 27432
       → no `strip-raster-requested` line after scrolling 40 notches. Either this
         document has too few pages for a strip, or the mode did not switch to a
         continuous display. Reported as SKIPPED rather than PASSED: a check whose
         subject never happened has measured nothing — and a cache that is never
         asked for anything trivially never asks twice.
       artifact: target/ui-verify\page_cache.trace.txt

[SKIP] page_ops_round_trip
       detects: The whole Pages tab does nothing: rotate, move and delete are registered, drawn, offered by a context menu and bound to chords, and every press traces `command-unimplemented` — or reaches the engine and leaves the shell's page vector, its rasters and its selections describing a document that no longer exists
       → fixtures/a1-titleblock.pdf already contains 1 `/Rotate` entr(y/ies). Phase H
         proves the rotation reached the file by finding `/Rotate 90` in the saved
         copy and NOT in the source; on this document that evidence would be
         indistinguishable from the fixture's own furniture. Point --pdf at a
         document with no page rotation —
         `D:\Dev\pdfce\fixtures\synthetic\pageops\four-pages.pdf` is the one this
         project uses.

[SKIP] pages_drag_shows_where_it_lands
       detects: a page thumbnail cannot be dragged to a new position, or it can and nothing shows where it will land — so reordering is the two one-step ribbon arrows, and moving a sheet across a drawing set is dozens of presses with no preview of the result
       · launched target/scratch/drive/pdfce-gui.exe as pid 32116
       · the Pages panel is on screen
       → the fixture shows 1 page tile(s) and this check needs at least 3. On a
         shorter document every landing is the dragged block's own lip, where
         refusing is the CORRECT answer — so a run here could not tell a working
         gesture from a dead one. Pass a longer --pdf.
       artifact: target/ui-verify\pages_drag.trace.txt

[PASS] embedding_fonts_puts_a_program_in_the_document
       detects: Embed fonts is on the Tools tab and embeds nothing — the command was registered, drawn and inert for the whole life of the project, and a document that asks for Helvetica still asks for it after the operator has pressed the button and read a window telling them what would happen
       · launched target/scratch/drive/pdfce-gui.exe as pid 35480 with PDFCE_DIAG_INVOKE=mode.edit,tools.embed_fonts and PDFCE_DIAG_FONT_DIR=C:\Windows\Fonts
       · ★ the window raised the action: `pdfce-diag embed-fonts-requested targets=3 blocked=3 shown=0 supplied=2`
       · ★ the engine embedded: `pdfce-diag embed-fonts-applied embedded=3 bytes=3081220 missing_before=3 missing_after=0 substituted=true`
       · ★ fonts with no program: 3 before, 0 after — 3 embedded, substituted=true
       artifact: target/ui-verify\embed-fonts.trace.txt

[PASS] embedding_works_with_no_font_folder_at_all
       detects: with no font folder configured, Embed fonts refuses everything — pdfce ships the fourteen standard faces and cannot reach them, so an operator who has not set up a folder is told to go and find one for a font pdfce is already carrying
       · launched target/scratch/drive/pdfce-gui.exe as pid 12836 with NO font folder configured — the point of this run
       · ★★ the window found donors with no folder set: `pdfce-diag embed-fonts-opened targets=3 blocked=3 shown=0 supplied=2`
       · ★ the engine embedded: `pdfce-diag embed-fonts-applied embedded=3 bytes=46394 missing_before=3 missing_after=0 substituted=true`
       · ★★★ 3 font(s) embedded from pdfce's own faces, with no folder configured, and every one disclosed as a substitute — still missing afterwards: 0
       artifact: target/ui-verify\embed-bundled.trace.txt

[PASS] a_compacted_copy_is_actually_smaller
       detects: removing pages, images or embedded fonts never makes the file smaller — pdfce appends every save, so the freed bytes stay in the previous revision and the file grows, and there is no command that asks for the full rewrite that would drop them
       · launched target/scratch/drive/pdfce-gui.exe as pid 35332; the --pdf is 39509 bytes on disk
       · ★ the window raised the save: `pdfce-diag compact-requested before=39509 after=39502 signatures=0`
       · ★ the file was written: `pdfce-diag compact-written path="target/ui-verify\\compacted-copy.pdf" before=39509 after=39502 epoch=0`
       · ★★ the copy is a real PDF of 39502 bytes, against the original's 39509
       · ★★★ the compacted copy is 7 bytes smaller than the original — the space the embed appended was reclaimed, which is what O48 asked for
       artifact: target/ui-verify\compact-save.trace.txt

[PASS] font_folders_lands_on_the_fonts_setting
       detects: Tools ▸ Font folders opens the Settings window at the top of ten collapsed headings and leaves the operator to find the one the command is named after — which is how he came to ask for a font-folder setting that had shipped the day before
       · launched target/scratch/drive/pdfce-gui.exe as pid 12108 with PDFCE_DIAG_INVOKE=tools.font_folders and no document open
       · ★★★ Tools ▸ Font folders landed on the Fonts group and the checkbox is on screen at LRect { min: Pt { x: 18.0, y: 326.0 }, max: Pt { x: 267.1, y: 350.0 } } — an operator asking where font folders live is shown them
       · the Add-folder button is on screen with it
       artifact: target/ui-verify\os-fonts-setting.trace.txt

[SKIP] removing_embedded_fonts_reaches_the_document
       detects: Remove embedded fonts is on the Tools tab and removes nothing — the command was registered, drawn and inert for the whole life of the project behind a blocker that was real, and an operator who presses it gets a window listing what would happen and a button that changes nothing
       · launched target/scratch/drive/pdfce-gui.exe as pid 20208 with PDFCE_DIAG_INVOKE=mode.edit,tools.unembed_fonts
       → the window opened with nothing to remove: `pdfce-diag unembed-fonts-opened
         targets=0 blocked=6 shown=3 bytes=0`. SKIPPED rather than failed: every
         embedded font in this --pdf is one pdfce judges unsafe to unembed -
         identity-encoded or Type 3 - so the greyed button is correct. `pdfce-cli
         list-fonts <file>` names the verdict per font; this check needs one reading
         `removable`. Trace: target/ui-verify\unembed-fonts.trace.txt.
       artifact: target/ui-verify\unembed-fonts.trace.txt

[SKIP] exporting_form_data_writes_a_file
       detects: Export form data is on the ribbon and writes nothing — the command was registered, drawn and inert for the whole life of the project behind a blocker that had stopped being true, and an operator who presses it gets a dialog and an empty result or no dialog at all
       · launched target/scratch/drive/pdfce-gui.exe as pid 28836 with PDFCE_DIAG_INVOKE=mode.edit,file.export_form_data and PDFCE_DIAG_SAVE_PATH=target/ui-verify\exported-form-data.fdf
       → the export declined: `pdfce-diag export-form-data-declined
         reason=no-acroform`. SKIPPED rather than failed: this says the --pdf carries
         no form or no fields, which is the harness's aim rather than the program's
         behaviour. Point it at a document with a form. Trace:
         target/ui-verify\export-form-data.trace.txt.
       artifact: target/ui-verify\export-form-data.trace.txt

[PASS] export_dxf_writes_the_pages_geometry
       detects: File > Export to DXF is drawn and does nothing — or it opens a window whose Export button writes no file, or writes one whose contents do not match what the shell reported, so an operator hands a CAD file to somebody with counts that describe a different page
       · launched target/scratch/drive/pdfce-gui.exe as pid 22184
       · scale inference: `pdfce-diag export-dxf-open page=0 groups=0 suggestion=Uncalibrated`
       · wrote: `pdfce-diag export-dxf page=0 bytes=5606 polylines=13 circles=0 arcs=0 splines=0 skipped_text=0 skipped_images=0 unreadable_text=0`
       · 5606 bytes on disk, with a DXF header
       · 13 line entities reported, 13 found in the file
       artifact: target/ui-verify\export_dxf.trace.txt

[PASS] insert_image_places_a_picture
       detects: Edit > Insert image is drawn and does nothing — or it opens a window whose Insert button reaches no document, or promises a resolution the placement does not deliver, which under the default fit is wrong by exactly the letterbox ratio and looks perfectly reasonable in both places
       · wrote a 64×16 fixture PNG, 3156 bytes
       · launched target/scratch/drive/pdfce-gui.exe as pid 20396
       · the fixture imported and the placement window opened
       · placed: `pdfce-diag add-image page=0 n=1 epoch=1 disclosures=At this size the picture is 72 dpi.`
       · the window promised 72 dpi and the document reported 72 — one producer, two readings, same answer
       · the page repainted: 12115 of 154440 pixels in the page area changed, against a floor of 9 for a 64×16 pt picture at scale 0.196
       artifact: target/ui-verify\insert_image.trace.txt
       artifact: target/ui-verify\insert_image_fixture.png
       artifact: target/ui-verify\insert_image_after.png

[SKIP] properties_metadata_round_trips
       detects: the Properties panel shows no document metadata, or shows boxes that do not reach the file — so a title typed into one is written again on every focus change, or survives an undo that removed it from the document, leaving the panel and the file disagreeing about what the operator's document is called
       · launched target/scratch/drive/pdfce-gui.exe as pid 22204
       → the File tab declares no `ribbon.item.file.properties`, on the band or in
         the overflow, so the Properties panel cannot be opened.
       artifact: target/ui-verify\properties_metadata.trace.txt

[PASS] print_dialog_reaches_the_spooler
       detects: File ▸ Print opens a dialog that says this build cannot print, on a machine with printers
       · launched target/scratch/drive/pdfce-gui.exe as pid 35544
       · the spooler answered with 12 printer(s); the adapter is reaching `pdfce-print`
       · ★★ it is a real OS window: `pdfce-diag viewport-inner id="4206" rect=[[836.0 119.0] - [1636.0 739.0]]`
       artifact: target/ui-verify\print_dialog.trace.txt

[PASS] print_paper_changes_the_plan
       detects: choosing a paper size relabels the control and the job is still planned for the device's default sheet
       · launched target/scratch/drive/pdfce-gui.exe as pid 31728
       · the selected device enumerated 20 paper form(s); form_source=NotListed
       · before: paper=DeviceDefault sheet=Some((792.0, 612.0))
       · print.paper.item.1: paper=Form(9) sheet=Some((841.68, 595.2))
       · choosing `Form(9)` re-planned the job from Some((792.0, 612.0)) to Some((841.68, 595.2)) (after 1 click(s) of a 21-entry list)
       artifact: target/ui-verify\print_paper.trace.txt

[PASS] settings_theme_takes_effect
       detects: three themes ship, and nothing an operator can press chooses one (DEFECTS.md D10)
       · launched target/scratch/drive/pdfce-gui.exe as pid 12112 with NO document
       · window body Rgb { r: 241, g: 241, b: 242 } -> Rgb { r: 36, g: 38, b: 42 } after choosing Dark (mean channel drop 202)
       artifact: target/ui-verify\settings_theme.trace.txt
       artifact: target/ui-verify\settings_theme.before.png
       artifact: target/ui-verify\settings_theme.after.png

[PASS] undo_redo_round_trip
       detects: Undo and Redo do nothing, or take the change off the session without invalidating what the operator is looking at — the five-link chain from a quick-access click to a re-rasterized page, three links of which no test in the workspace can observe
       · launched target/scratch/drive/pdfce-gui.exe on fixtures/a1-titleblock.pdf as pid 35024
       · baseline: the Comments panel lists 12 annotation(s) — `pdfce-diag comments-panel pages=1 listed=12 with_note=12 descriptions=0 authors=12 ce_dimensions=0 suppressed=0 unresolved=0 replies=0 group_members=0 excluded_widgets=0 excluded_popups=0 excluded_trapnet=0 excluded_total=0`
       · the rectangle was authored (`pdfce-diag markup-commit kind=Rectangle page=0 x0=570.53 y0=470.08 x1=1380.48 y1=775.72`) and the Comments panel lists 13, one more than the baseline — there is now exactly one change to take back
       · the undo: the arm decided — `pdfce-diag undo kind=AddAnnotation { kind: Square } undo_depth=1`
       · the undo: the engine ran — `pdfce-diag undo-applied page=0 n=1 epoch=2 disclosures=none`
       · the undo: the epoch moved (2 → 3 `objects` lines) and the page re-rasterized (2 → 3 `render-spawn` lines)
       · the undo: the Comments panel now lists 12 annotation(s)
       · the redo: the arm decided — `pdfce-diag redo kind=AddAnnotation { kind: Square } undo_depth=0`
       · the redo: the engine ran — `pdfce-diag redo-applied page=0 n=1 epoch=3 disclosures=none`
       · the redo: the epoch moved (3 → 4 `objects` lines) and the page re-rasterized (3 → 4 `render-spawn` lines)
       · the redo: the Comments panel now lists 13 annotation(s)
       · ★★ ROUND TRIP PROVEN: 12 → 13 authored → 12 undone → 13 redone, with a fresh `objects` line and a fresh `render-spawn` at each step — so the epoch moved and the page was re-rasterized, not merely the session mutated. `ribbon.qat.edit.undo` was greyed before the edit and `ribbon.qat.edit.redo` is greyed after the redo
       · ★ …and phase B's silence is evidence rather than an absence: `ribbon.qat.edit.undo` was clicked at its centre with an empty command log and the shell traced no `ribbon-command-invoked`, while the SAME control clicked in phase D did — so the pointer reaches it, and a greyed `egui` control is what swallowed the first press
       · NOT covered here: `Ctrl+Z`, `Ctrl+Y`, `Ctrl+Shift+Z`. `checks::chords` presses all three and asserts each dispatches. Until 2026-08-18 this note said they COULD NOT be driven and were covered by the manifest keymap test; both halves were false, and all three were among fourteen declared chords the dispatcher ignored.
       · NOT covered here: that the page's PIXELS show the undone state. `render-spawn` proves a raster was asked for, which is the step that was in doubt; it does not prove what came out. A pixel oracle was rejected for `save_copy`'s reason — a 2 pt rectangle over a CAD drawing is a few hundred antialiased pixels among a page full of thin dark lines, and no threshold in this crate separates the two
       artifact: target/ui-verify\undo_redo.trace.txt

[PASS] copy_and_paste_page_content
       detects: Ctrl+C on a shape, a line or a piece of text does nothing, or says pdfce cannot copy page content — the operator's oldest open request. Or it copies and the paste arrives WITHOUT ITS RESOURCES, which puts the right glyphs on the page in the wrong typeface and errors nowhere
       · launched target/scratch/drive/pdfce-gui.exe as pid 33424
       · the click selected page content
       · ★ the copy reached the engine: `pdfce-diag clipboard-copy kind=content page=0 objects=1 bytes=251`
       · ★ the paste was raised: `pdfce-diag clipboard-paste kind=content page=0 from=0 objects=1 offset=10.0`
       · ★★ 1 object(s) reached the document: `pdfce-diag paste-objects page=0 pasted=1 resources_added=0 at=[1.0000 0.0000 0.0000 1.0000 10.00 -10.00]`
       · resources_added=0 — the pasted objects consume no page-local resource names, which a bare stroked path genuinely may not. The resource-rebinding half of the clip is therefore UNVERIFIED by this run; aim --doc-point at text or an image to exercise it
       artifact: target/ui-verify\object-clipboard.trace.txt

[SKIP] ctrl_c_copies_text_to_the_os_clipboard
       detects: sweeping text on the page and pressing Ctrl+C puts "1 object copied from pdfce" on the clipboard instead of the text. Pasting into any other program gives that sentence
       · launched target/scratch/drive/pdfce-gui.exe as pid 25500
       → the drag from (page 1, 300.0, 500.0) rightwards 60 pt selected no text, so
         there is nothing for Ctrl+C to copy. That is a fact about the fixture and
         the point rather than about the build — aim --doc-point at a run of text.
         SKIPPED.
       artifact: target/ui-verify\clipboard-text.trace.txt

[PASS] select_filter_changes_what_a_click_hits
       detects: the Select filter is drawn and does not gate the hit test — switching every class off still lets a click select an object, so the control is decorative
       · launched target/scratch/drive/pdfce-gui.exe as pid 31508
       · with every class on, the click selects
       · with every class off, the same click selects nothing
       · switching them back on restores it
       artifact: target/ui-verify\select-filter.trace.txt

[FAIL] scrolling_far_keeps_the_canvas_its_pointer_input
       detects: after scrolling a long way down a document the canvas stops receiving pointer input altogether — the page is still drawn in the right place and nothing responds to the mouse
       · before scrolling: 1 pointer event(s)
       · scrolled to an offset of 1125 pt
       → ★★★ THE CANVAS STOPPED SEEING THE POINTER AFTER SCROLLING. 1 pointer
         event(s) before the wheel and 1 after it, at a scroll offset of 1125 pt. The
         page is still drawn and its rect is still published — only the input is
         gone. ★ This is the condition `OPERATOR_REQUESTS.md` O23 was blaming its
         pasteboard for: it reproduces with NO pasteboard in the build, from an
         ordinary wheel scroll, which makes it a defect the operator meets whenever
         they scroll a long way down a drawing.
       artifact: target/ui-verify\scroll-input.trace.txt

[PASS] the_zoom_readout_opens_the_maximum_zoom_popup
       detects: the status bar's zoom readout is drawn as a button and clicking it opens nothing — a control that looks available and is inert, which is how the Select popup shipped
       · the popup opened with 6 row(s)
       artifact: target/ui-verify\max-zoom.trace.txt

[PASS] zooming_past_the_pixmap_ceiling_still_renders
       detects: zooming past about 1000% stops the page rendering — "requested raster size … exceeds MAX_PIXMAP_EDGE" — because the whole page is rasterized however deep the zoom goes
       · zoomed to 999999995904%
       · 8 raster(s) completed, none failed
       artifact: target/ui-verify\deep-zoom.trace.txt

[PASS] panning_at_deep_zoom_stays_where_it_was_put
       detects: panning a little at high zoom does nothing, or moves and then jumps back to where it started — so a point just off screen cannot be reached at all
       · zooming at the cells — page (540, 560)
       · zoomed to 1072%
       · at 1072% on tier `scroll`: (5681.0, 11569.625) → (5681.0, 12529.625) → (5681.0, 12529.625)
       · at 999999995904% on tier `deep`: (5405968168553.3, 12028199962923.16) → (5405968168553.3, 12028199963883.148) → (5405968168553.3, 12028199963883.148)
       artifact: target/ui-verify\deep-pan.trace.txt

[PASS] zooming_does_not_throw_away_where_the_operator_panned
       detects: zooming after panning snaps the view back to the centre of the page, or loses it entirely past about two million percent — the zoom discards the position instead of magnifying about it
       · panned off-centre; the page point under the centre is (1192.05, 840.16) at 20%
       · stage 1: 20% to 97% on tier `scroll`, worst per-notch drift so far is 19% of the tolerance
       · stage 2: 97% to 394% on tier `scroll`, worst per-notch drift so far is 19% of the tolerance
       · stage 3: 394% to 2385% on tier `scroll`, worst per-notch drift so far is 21% of the tolerance
       · stage 4: 2385% to 11815% on tier `scroll`, worst per-notch drift so far is 21% of the tolerance
       · stage 5: 11815% to 58519% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 6: 58519% to 289844% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 7: 289844% to 1435608% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 8: 1435608% to 7110605% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 9: 7110605% to 35219025% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 10: 35219025% to 174440950% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 11: 174440950% to 864011300% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 12: 864011300% to 4279475200% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 13: 4279475200% to 21196374400% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 14: 21196374400% to 104986227200% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 15: 104986227200% to 520000000000% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 16: 520000000000% to 1000000000000% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · stage 17: 1000000000000% to 1000000000000% on tier `deep`, worst per-notch drift so far is 21% of the tolerance
       · saturated at 1000000000000% after 17 stage(s)
       · crossed tiers: scroll to deep -- 1 notch(es) spanned the hand-over and were judged
       artifact: target/ui-verify\zoom-keeps-place.trace.txt

[PASS] zooming_back_out_keeps_the_view
       detects: zooming back out from past about two million percent throws the page off screen into a corner — the f64 anchor's position is never handed back to the f32 scroll offset, so the first shallow frame solves its zoom against an offset that was forced to zero while deep
       · panning off the centred position first: the centred position is what a position-discarding zoom lands ON, so a run that started there could not tell holding from losing
       · reached tier `deep` after 39 notches, at 47911%
       · turning round at 159070% on tier `deep`; the page point under the viewport centre is (1189.842225, 1656.848324)
       · descended 14 of 14 notches back to 14087%, crossing deep to scroll — 1 notch(es) spanned the hand-over and were judged; the worst asserted drift was 22% of its tolerance
       artifact: target/ui-verify\zoom-out-keeps-place.trace.txt

[PASS] a_fit_command_puts_the_page_on_screen
       detects: a fit command sets the scale and leaves the view wherever it was, so with O23's pasteboard the page can be correctly sized and entirely off screen — and there is no Fit height at all
       · FIT PAGE: panned 30 notches into the pasteboard first, and the page moved
       · FIT PAGE: page 296.0,285.7..764.0,616.3 in canvas 288.0,143.3..772.0,758.0
       · FIT WIDTH: panned 30 notches into the pasteboard first, and the page moved
       · FIT WIDTH: page 296.0,285.7..764.0,616.3 in canvas 288.0,143.3..772.0,758.0
       · FIT HEIGHT: panned 30 notches into the pasteboard first, and the page moved
       · FIT HEIGHT: page 288.0,151.7..1135.5,750.4 in canvas 288.0,143.3..772.0,758.0
       artifact: target/ui-verify\fit-places-the-view.trace.txt

[FAIL] the_wheel_turns_pages_when_the_operator_asks_it_to
       detects: in single-page view the wheel has nothing to scroll on a page that already fits, so it does nothing at all — and the toggle that should make it turn pages is either absent, or writes a preference that only takes effect after a Settings apply
       · single-page display, on page 1 of the document
       · with the toggle off the wheel turned no page, which is the shipped default
       · pressed the wheel-paging toggle beside the page buttons
       → ★★★ THE WHEEL DID NOT TURN A PAGE. Still on page 1 after the toggle
         was pressed and the wheel rolled forward. The most likely cause is the one
         this check was written for: `OpenDoc::prefs` is a SNAPSHOT adopted when the
         Settings window is applied, so a wheel preference left to it writes the file
         correctly, draws the control correctly, and changes nothing until Settings
         is opened and applied. `app::frame` pushes this one into every open document
         the moment it changes.
       artifact: target/ui-verify\wheel-flips-pages.trace.txt

[PASS] the_page_still_renders_at_every_decade_of_zoom
       detects: the canvas stops actually drawing the page at high zoom — the position arithmetic and the rasterizer both report success while the operator is looking at blank paper
       · aiming at page 0 (300, 500)
       · 0000000000145: 145% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000000000145.png
       · 0000000002385: 2385% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000000002385.png
       · 0000000017626: 17626% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000000017626.png
       · 0000000058519: 58519% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000000058519.png
       · 0000000432398: 432398% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000000432398.png
       · 0000003195012: 3195012% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000003195012.png
       · 0000035219184: 35219184% — drawn=1, canvas shows 6 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000035219184.png
       · stopped short of 350000000%: the application saturated at 52540836%, which is its configured maximum
       · 0000052540836: 52540836% — drawn=1, canvas shows 54 distinct tone(s), capture at target/ui-verify\zoom-gallery-0000052540836.png
       artifact: target/ui-verify\zoom-gallery.trace.txt
       artifact: target/ui-verify\zoom-gallery-0000000000145.png
       artifact: target/ui-verify\zoom-gallery-0000000002385.png
       artifact: target/ui-verify\zoom-gallery-0000000017626.png
       artifact: target/ui-verify\zoom-gallery-0000000058519.png
       artifact: target/ui-verify\zoom-gallery-0000000432398.png
       artifact: target/ui-verify\zoom-gallery-0000003195012.png
       artifact: target/ui-verify\zoom-gallery-0000035219184.png
       artifact: target/ui-verify\zoom-gallery-0000052540836.png

[FAIL] panning_past_the_overscan_renders_the_new_area
       detects: panning far to one side above the raster crossover leaves the newly exposed area blank — the region changed, no re-render was ever requested, and the old picture simply slid off
       · at 1072%, region 1.128933716e3,7.946165466e2,1.219248413e3,9.093757019e2
       · after the pan: region 1.128933716e3,6.511676025e2,1.219248413e3,7.659267578e2, 0 render(s) completed
       → ★★ NO RENDER WAS REQUESTED. The view moved from region
         1.128933716e3,7.946165466e2,1.219248413e3,9.093757019e2 to
         1.128933716e3,6.511676025e2,1.219248413e3,7.659267578e2 at 1072% and not one
         raster completed in the 260 frames after it. This is the operator's "it
         doesn't always render the new exposed area": `render::settle`'s staleness
         test compares the page, the annotations, the layers and the scale — and
         the REGION is in the cache key without being in any of them, so a pan is not
         stale by any measure it applies. See `RenderKey::same_region`.
       artifact: target/ui-verify\pan-refresh.trace.txt

[FAIL] resize_scales_a_shape
       detects: the eight resize grips change the cursor, consume the drag and commit nothing — a control that looks available and is inert, which the operator experiences as a resize that silently does not work
       · launched target/scratch/drive/pdfce-gui.exe as pid 31964
       · the click selected a shape, so the outline and its grips are drawn
       → ★ THE GRIP DRAG COMMITTED NOTHING AND DECLINED NOTHING. That is the state
         this whole feature is a fix for: until 2026-08-19 every resize drag was
         consumed and thrown away, so a build that has reverted to it is silent on
         both channels — which is exactly what an operator reports as 'resize does
         not work'. Look at `canvas::interact`'s `GestureOutcome::Resize` arm. Trace:
         target/ui-verify\resize.trace.txt.
       artifact: target/ui-verify\resize.trace.txt

[FAIL] rotate_handle_turns_a_selection
       detects: there is no way to rotate anything on the canvas — the third word of the operator's "reposition, resize, or rotate", with no affordance at all. Or the handle is drawn and a press on it MOVES or RESIZES the selection instead, which is a working gesture aimed at the wrong verb and looks entirely deliberate
       · launched target/scratch/drive/pdfce-gui.exe as pid 15500
       · the click selected something, so the outline and its handles are drawn
       → ★ THE ROTATE HANDLE COMMITTED NOTHING. The press was made at the point the
         application itself declared, above the selection outline it published, and
         no `rotate-commit` line followed. Three links can produce this and they are
         worth checking in order: `Grip::is_resize` must answer FALSE for `Rotate`
         (if it answers true the press became a RESIZE), `gesture::meaning` must
         match `Grip::Rotate` by name before the `Some(_)` wildcard (if not the press
         became a MOVE), and `needs_targets` must include `GestureOutcome::Rotate`
         (without it the commit has no decomposition and declines). All three produce
         a working gesture aimed at the wrong verb, which looks deliberate. Trace:
         target/ui-verify\rotate.trace.txt.
       artifact: target/ui-verify\rotate.trace.txt

[FAIL] shift_constrains_a_resize
       detects: Shift does not preserve aspect on a resize — *the* resize convention, present in every program in the class, and absent from every drag in this shell until 2026-08-20. An operator holding Shift gets a free-form resize and cannot tell whether the key did anything
       · launched target/scratch/drive/pdfce-gui.exe as pid 35600
       · the click selected a shape, so the outline and its grips are drawn
       → the grip drag committed nothing and declined nothing — the state the whole
         resize feature is a fix for. Before asking about the constraint, look at
         `resize_scales_a_shape`, which measures that alone. Trace:
         target/ui-verify\shift-constrains.trace.txt.
       artifact: target/ui-verify\shift-constrains.trace.txt

[PASS] geometry_fields_resize_a_shape
       detects: the Properties panel's X/Y/W/H fields accept a number and Apply commits nothing — the typed route to a resize looking available and being inert, which is the same defect shape the eight grips had for the whole life of the project
       · launched target/scratch/drive/pdfce-gui.exe as pid 22092
       · the geometry section drew, so a single path object is selected
       · ★ Apply committed a scale: `pdfce-diag resize-scale sx=1.0168 sy=1.0000 ax=0.00 ay=0.00 objects=1`
       · ★★ the typed width reached the engine through `transform_objects`
       artifact: target/ui-verify\geometry_fields.trace.txt

[SKIP] restyling_selected_text_reaches_the_document
       detects: text can be swept on the page and the Properties panel offers nothing to change about it — or offers a Bold button that commits nothing, which is the same defect shape the eight resize grips had for the whole life of the project
       · launched target/scratch/drive/pdfce-gui.exe as pid 11712
       → the drag from (page 0, 300.0, 500.0) rightwards 60 pt selected no text, so
         there was nothing for the panel to describe. SKIPPED rather than failed:
         this says the --doc-point is not on text, which is the harness's aim and not
         the program's behaviour. Trace: target/ui-verify\restyle-text.trace.txt.
       artifact: target/ui-verify\restyle-text.trace.txt

[FAIL] the_format_tab_offers_font_controls_for_swept_text
       detects: an operator who wants to change how text looks has to already know to press T and sweep — the ribbon offers no font controls, or offers them on a tab that never appears for a text selection, so a capability the engine has and the panel exposes is unreachable from the surface an operator looks at first
       · launched target/scratch/drive/pdfce-gui.exe as pid 33816
       · ★ clicking a piece of text raised the contextual Format tab
       → ★ A PIECE OF TEXT IS SELECTED AND THE PROPERTIES PANEL DOES NOT SAY HOW TO
         CHANGE IT: no `properties.text.route` region. This is O37's own complaint.
         `panels::properties::text::route` draws the heading and one sentence naming
         the Text tool and its chord whenever the selected object is text and nothing
         is swept. Three candidates: the guard decided this object is not text
         (`summary::object_kind`), more than one object is selected (the sentence is
         deliberately single-selection only), or the section returned before drawing
         — which is what it did for the whole of the feature's first week. Trace:
         target/ui-verify\font-group.trace.txt.
       artifact: target/ui-verify\font-group.trace.txt
       artifact: target/ui-verify\font_group.no-route-sentence.png

[FAIL] multi_node_move_moves_every_picked_anchor
       detects: Shift-picking several anchors highlights all of them and a drag moves ONE — a selection the program shows and does not honour, which is worse than not offering it, because the operator has no reason to check
       · launched target/scratch/drive/pdfce-gui.exe as pid 38800
       · the Part rung was entered on an object with 4 anchors: `pdfce-diag canvas-anchors total=4 selected=0 unselected_drawn=4`
       · 1 anchor(s) selected after the Shift-click
       → ★ TWO MARKED ANCHORS WERE CLICKED, THE SECOND WITH SHIFT, AND 1 ENDED UP
         SELECTED. The multi-node MOVE is downstream of this: if the selection cannot
         hold two anchors there is nothing for the plural verb to carry.
         `SelectionState::pick_within` is the function — it adds a Shift-picked
         entry and removes one that was already there, so a build that toggles rather
         than extends ends up back at one. Trace:
         target/ui-verify\multi_node.trace.txt.
       artifact: target/ui-verify\multi_node.trace.txt

[SKIP] bezier_handle_drag_changes_a_curve
       detects: a Bézier handle is drawn, takes the cursor and moves the whole object instead of the control point — because a handle sits inside the selection box, where `grip_at` answers `Grip::Move` for every press on one
       · launched target/scratch/drive/pdfce-gui.exe as pid 36388
       → none of the 6 published anchors drew a Bézier handle, so every segment in
         the entered subpath is straight. That is an honest fact about the fixture
         — `move_handle` refuses a straight segment by name and this shell draws no
         handle there — so it is SKIPPED. Use `fixtures/polyline-nodes.pdf`, whose
         tail is two cubics. Trace: target/ui-verify\bezier_handle.trace.txt.
       artifact: target/ui-verify\bezier_handle.trace.txt

[PASS] the_text_tool_types_on_one_click
       detects: pressing T and clicking text gives an I-beam and no caret, because the text tool SWEEPS text and the tool that types is a different one reachable only through Edit ▸ Content — four steps of ritual before a character can be typed, with nothing on screen saying so
       · launched as pid 13476
       · ★ one press of T and one click: `pdfce-diag text-edit-caret kind=Edit page=0 run=0 len=10`
       · the second click did not fall through to a new run — the point named an existing run, which is a fact about this fixture rather than about the feature
       artifact: target/ui-verify\tool_text.trace.txt

[PASS] the_points_tool_shows_points_on_one_click
       detects: the only way to see an object's points is to click it and then double-click twice to descend a rung ladder nothing on screen mentions — so an operator who wants to move an end point has to already know the ladder exists in order to discover it
       · launched as pid 35884
       · ★ one press of A and one click: `pdfce-diag canvas-anchors total=4 selected=0 unselected_drawn=4`
       · ★★ 4 point(s) drawn on the first click
       artifact: target/ui-verify\tool_points.trace.txt

[PASS] show_points_draws_an_objects_points_without_descending
       detects: View ▸ Show points is on the ribbon and changes nothing on screen — the command was drawn and inert for the life of the project, and the first wiring of it was inert too, because the draw falls out on a rung guard two lines below the one the toggle was added to
       · launched as pid 35828
       · ★ a plain click drew no anchors, which is the Object rung behaving as designed
       · ★★ the toggle drew points: `pdfce-diag canvas-anchors total=4 selected=0 unselected_drawn=4`
       · ★★★ 4 of 4 points drawn at the Object rung, with no descent — which is what the toggle is for
       artifact: target/ui-verify\show_points.trace.txt

[PASS] a_dropped_image_reaches_the_placement_window
       detects: a file dragged onto the window is ignored, silently and with no feedback — which teaches the operator that this program does not accept drops, a conclusion they will not revisit about a program that opens documents for a living
       · dropping target/ui-verify\dropped_fixture.png
       · launched as pid 29488
       · ★ the shell saw the drop: `pdfce-diag dropped n=1 first=Some("dropped_fixture.png")`
       · ★★ the dropped image opened the SAME placement window the ribbon command opens
       artifact: target/ui-verify\dropped_file.trace.txt

[PASS] the_first_frame_names_the_tools
       detects: the panel whose job is to teach the tools does not name the current ones — it can be complete about yesterday's tools and silent about today's, because every test of it asks whether a NAMED command is listed and none asks what an operator sees
       · launched as pid 34336
       · the tool list drew at LRect { min: Pt { x: 786.0, y: 159.0 }, max: Pt { x: 1086.0, y: 231.0 } }
       · ★★ all four pointer tools are named, on screen, in palette order, with no click
       artifact: target/ui-verify\first_frame.trace.txt

[SKIP] redaction_removes_and_proves_it
       detects: Applying a redaction reports success and leaves the marked text in the saved file, writes over the document it came from, or offers its irreversible confirm control before the operator has acknowledged anything — none of which a passing test suite can see, because the only honest evidence is the bytes on disk read by a second process
       · fixture target/ui-verify\redaction-fixture.pdf — 878 bytes, digest d56823ffb39e92ee; both `CONFIDENTIALWITNESSALPHA` (page 1) and `UNTOUCHEDWITNESSBETA` (page 2) are present in its bytes
       · launched target/scratch/drive/pdfce-gui.exe on target/ui-verify\redaction-fixture.pdf as pid 29940 with PDFCE_DIAG_SAVE_PATH=target/ui-verify\redaction-applied.pdf
       · phase A: pdfce's own extraction reads 24 character(s) from page 1 of the fixture
       → the click on `redact-whole-page` produced no mark (`redact-panel marks=0`).
         There is no input-channel confirmation for a panel control — it is not a
         command — so this is reported as a SKIP rather than as a failure of
         marking: the click may never have been delivered.
       artifact: target/ui-verify\redaction.trace.txt

[SKIP] two_documents_get_two_tabs
       detects: opening a second PDF replaces the first, or opens beside it with no tab to say so — so the operator has no way to see what they have open and no way back to it
       → no second document. Pass --second-pdf <path> — a file DIFFERENT from
         --pdf. The same path cannot be used twice: opening a path that is already
         open activates its tab by design, so this check would be asserting the
         opposite of what it is for.

[SKIP] document_tabs_can_be_rearranged
       detects: a document tab cannot be dragged to a new position — the strip's order is whatever order the files were opened in and stays that way — or it can, and doing it switches the operator to a different document
       → no second document. Pass --second-pdf <path> — a file DIFFERENT from
         --pdf. There is nothing to rearrange with one tab.

[SKIP] a_page_dragged_between_documents_is_copied
       detects: a page cannot be dragged from one open document into another — the drag does not survive the document switch, or no tab springs open under it, or the release lands nowhere — so combining two drawings means Insert-from-file and a dialog
       → no second document. Pass --second-pdf <path> — a file DIFFERENT from
         --pdf. This check drags a page from one document into another and there is
         no second document to drag between.

[SKIP] a_shift_drag_between_documents_moves_the_pages
       detects: holding Shift while dragging a page into another document still copies it — so Windows' own move modifier is inert, and an operator who used it is left with the sheets in both documents and no indication of which one they meant
       → no second document. Pass --second-pdf <path> — a file DIFFERENT from
         --pdf. This check drags a page from one document into another and there is
         no second document to drag between.

[SKIP] about_reports_the_build
       detects: About does not open, or opens without saying when this program was built and which engine revision is inside it — so an operator looking at two builds that behave differently has no way to tell them apart
       · launched target/scratch/drive/pdfce-gui.exe as pid 33652 with NO document, which is a state About must work in
       → no `ribbon.item.file.about` region on the File tab or in its overflow. Items
         declared: ribbon.item.file.new, ribbon.item.file.new_from_template,
         ribbon.item.file.open, ribbon.item.file.close, ribbon.item.file.save,
         ribbon.item.file.save_copy, ribbon.item.file.save_compacted,
         ribbon.item.file.print, ribbon.item.file.ocr, ribbon.item.file.export_dxf,
         ribbon.item.file.export_form_data, ribbon.item.file.import_form_data,
         ribbon.item.file.copy_page_text, ribbon.item.file.copy_document_text.
       artifact: target/ui-verify\about.trace.txt

[SKIP] shortcuts_reference_is_live
       detects: the keyboard reference does not open, opens empty, or lists fewer commands than the keymap binds — so a chord that is declared and dispatches nothing is invisible to the one surface whose job is to say which keys work (DEFECTS.md D5)
       · launched target/scratch/drive/pdfce-gui.exe as pid 19060 with NO document — the state this window is drawn above the guard for
       → no `ribbon.item.file.shortcuts` region on the File tab or in its overflow.
         Items declared: ribbon.item.file.new, ribbon.item.file.new_from_template,
         ribbon.item.file.open, ribbon.item.file.close, ribbon.item.file.save,
         ribbon.item.file.save_copy, ribbon.item.file.save_compacted,
         ribbon.item.file.print, ribbon.item.file.ocr, ribbon.item.file.export_dxf,
         ribbon.item.file.export_form_data, ribbon.item.file.import_form_data,
         ribbon.item.file.copy_page_text, ribbon.item.file.copy_document_text.
       artifact: target/ui-verify\shortcuts.trace.txt

[PASS] arrow_keys_walk_between_blocks
       detects: the arrow keys do nothing once a caret is in a piece of text — there is no way to move to the line above or below without clicking, and no way at all to step from one block of text to the next. The shell this project replaces did both
       · launched target/scratch/drive/pdfce-gui.exe as pid 17084
       · ★ the caret landed in a run: `pdfce-diag text-edit-caret kind=Edit page=0 run=0 len=10`
       · ★★★ the caret crossed into another block of text: `pdfce-diag text-caret-step dir=Down from_run=0 to_run=4 to_caret=0`
       · 2 navigation step(s) in total across Down and Up
       · End raised no `text-caret-line`: this line is one piece of text, so its end is inside the run the caret was already in. Not evidence either way.
       artifact: target/ui-verify\block-nav.trace.txt

[PASS] dialogs_open_in_their_own_window
       detects: a dialog is drawn inside the application's own window: it cannot be moved off the document it is asking about, cannot be put on a second monitor, and does not appear in the taskbar. Reported by the operator about Print; it was true of all fourteen
       · ★ About is a real OS window: [[788.0 71.0] - [1348.0 551.0]]
       · ★ Render diagnostics is a real OS window: [[788.0 71.0] - [1308.0 451.0]]
       · ★ Export to DXF is a real OS window: [[788.0 71.0] - [1208.0 631.0]]
       · ★ Recognise text is a real OS window: [[788.0 71.0] - [1348.0 491.0]]
       · ★ Keyboard shortcuts is a real OS window: [[238.0 261.0] - [658.0 741.0]]
       · ★ Settings is a real OS window: [[788.0 71.0] - [1408.0 727.0]]
       · ★ New document is a real OS window: [[788.0 71.0] - [1248.0 591.0]]
       · ★ Apply redactions is a real OS window: [[788.0 71.0] - [1548.0 631.0]]
       artifact: target/ui-verify\dialog-file.about.trace.txt
       artifact: target/ui-verify\dialog-tools.render_diagnostics.trace.txt
       artifact: target/ui-verify\dialog-file.export_dxf.trace.txt
       artifact: target/ui-verify\dialog-file.ocr.trace.txt
       artifact: target/ui-verify\dialog-file.shortcuts.trace.txt
       artifact: target/ui-verify\dialog-file.settings.trace.txt
       artifact: target/ui-verify\dialog-file.new_from_template.trace.txt
       artifact: target/ui-verify\dialog-edit.redact_apply.trace.txt

[PASS] shift_arrows_select_text
       detects: there is no selection inside a text draft — no Shift+arrow, no Ctrl+A, no drag-select. Replacing a word means pressing Backspace once per character
       · launched target/scratch/drive/pdfce-gui.exe as pid 34680
       · ★ the caret landed in a run: `pdfce-diag text-edit-caret kind=Edit page=0 run=0 len=10`
       · the caret's run holds 10 character(s), so this run presses Shift+Right 3 times
       · ★★★ 3 presses selected 3 characters: `pdfce-diag text-select from=0 to=3 n=3`
       · ★★ and a plain Right dropped it, so no highlight is left behind
       · ★★★ a pointer sweep across the box selected 8 character(s) — the gesture that needed the galley to be shared rather than laid out twice
       artifact: target/ui-verify\text-selection.trace.txt

[PASS] bookmark_can_be_written
       detects: the Bookmarks panel is read-only — it lists an outline and offers no way to add to one, or offers one that does not reach the document, so a drawing exported without bookmarks can never be given its first
       · launched target/scratch/drive/pdfce-gui.exe as pid 11188
       · the authoring row drew on a document with no outline
       · the outline holds 0 item(s) to begin with
       · Add is inert while the title is empty, as R9 requires of a greyed control
       · the bookmark was written at epoch 1
       · the panel reads 1 item(s) back
       artifact: target/ui-verify\bookmark_add.trace.txt
       artifact: target/ui-verify\bookmark_add.png

[PASS] adopt_widget_puts_a_form_control_back
       detects: inserting a form's pages leaves boxes that draw exactly like form fields and swallow every keystroke, with no way to register them — or a Register control that is drawn and does not reach the document, or one that never asks what name it would use and so offers the operator a guess to accept
       · launched target/scratch/drive/pdfce-gui.exe as pid 27480, with PDFCE_DIAG_INSERT_PATH pointing at D:/Dev/pdfce/fixtures/synthetic\forms/demo-form.pdf
       · inserted: Inserted 1 page after page 2. 2 form controls need re-registering before they can be filled — Forms, Tab order lists them.
       · 2 unclaimed widget(s) listed
       · 32 row(s) previewed, 32 of them knowing the name they would register under
       · the row is on screen after 2 wheel turn(s)
       · registered at epoch 2: Registered as “FullName”. This document had no interactive form before; it has one now.
       · 1 unclaimed remain — the registered widget left the list, which is the round trip
       artifact: target/ui-verify\adopt_widget.trace.txt
       artifact: target/ui-verify\adopt_widget.png

[PASS] add_text_takes_real_keystrokes
       detects: the Add text tool arms and places a caret, and characters typed on the real keyboard never reach the draft — so the operator gets a caret that ignores them and nothing is ever written to the page
       · launched target/scratch/drive/pdfce-gui.exe as pid 31120
       · Edit > Add text armed the caret in Add mode
       · the click on blank paper started an Add draft
       · real keystrokes reached the draft — the OS -> egui -> draft link holds
       · clicking away committed the draft: `add-text` reached the engine
       artifact: target/ui-verify\add_text.trace.txt
       artifact: target/ui-verify\add_text_draft.png

[PASS] every_declared_chord_dispatches
       detects: a keyboard shortcut the manifest declares — and the menus print beside its command — reaches no command when pressed, so the operator's keyboard silently does nothing and the only evidence is that they stop using it
       · launched target/scratch/drive/pdfce-gui.exe as pid 12616
       · control chord Ctrl+2 arrived, so the input channel works
       · all 7 chords dispatched the command the manifest binds them to
       · NOT covered here: `F11`. It resizes the window mid-run and would invalidate every rect already measured; its dispatch is covered by the unit gate. Nor is this list swept from the manifest — a chord added tomorrow is not pressed here until it is added above, which is what `every_chord_the_manifest_binds_actually_fires` is for
       artifact: target/ui-verify\chords.trace.txt

[PASS] text_annot_places_and_authors
       detects: the text box control does not arm, the drag authors an EMPTY annotation on release instead of asking for words, the dialog never appears, or accepting it writes nothing to the document
       · launched target/scratch/drive/pdfce-gui.exe as pid 37488
       · Markup > Text box armed the text-annotation tool
       · the page carries 0 annotation(s) before the drag
       · dragging a 220x220 pt box: screen (1143, 589) -> (1186, 546)
       · the release authored nothing — still 0 annotation(s) — and opened the dialog instead, which is the property that separates this family from the seven geometric kinds
       · Accept authored: the page went from 0 to 1 annotation(s)
       artifact: target/ui-verify\text_annot.trace.txt

[PASS] text_annot_takes_the_keyboard_unclicked
       detects: the text-annotation dialog opens and does not hold the keyboard, so an operator who types straight into it — which is what the window is asking them to do — gets nothing, and Accept stays greyed
       · launched target/scratch/drive/pdfce-gui.exe as pid 36432
       · Markup > Text box armed the text-annotation tool
       · the drag opened the dialog and it declared its text field
       · typed two characters WITHOUT clicking the field, the way an operator does
       · the unclicked dialog took the keystrokes: Accept authored, 0 -> 1 annotation(s)
       · and it is on the page: 211 of 1849 pixels (11.4%) inside the box changed
       artifact: target/ui-verify\text_annot_focus.trace.txt
       artifact: target/ui-verify\text_annot_focus.before.png
       artifact: target/ui-verify\text_annot_focus.after.png

[PASS] text_box_takes_a_paragraph
       detects: text on the canvas is one line and cannot be made into more — either the text tool has no drag, so there is no rectangle to wrap against, or plain Enter still COMMITS inside one, which ends the draft at the first line break and silently discards everything typed after it
       · launched target/scratch/drive/pdfce-gui.exe as pid 20340
       · the Add-text tool is armed
       · ★ the drag opened a box: `pdfce-diag text-box-open page=0 box=300.5,439.5,499.2,500.6 w=198.7 h=61.1`
       · ★ the commit reached the engine: `pdfce-diag add-text page=0 n=2 epoch=1 disclosures=new run uses a bundled Standard-14 face 'Helvetica' by name+code — no glyph embedding (R79 / ISO 32000-1 §9.6.2.2); provenance is disclosed, not the document's own | boxed add: wrapped to 2 line(s) at 198.7pt box width, left alignment, top-anchored from the box top at 14.40pt leading (derived default 1.2 x size) — DERIVED layout (greedy first-fit, ISO 32000-1 §9.4.4 advances via the shipped 15.x breaker; §14.8 S1-S9), a reviewable add`
       · ★★★ the paragraph survived as 2 lines — a plain Enter inside the box broke the line instead of ending the draft
       artifact: target/ui-verify\text-box.trace.txt

[PASS] text_edit_pins_an_aligned_tail
       detects: Text editing does not reach the page at all, or it reaches it and drags the text the operator did not touch with it — DEFECTS.md D4b's right-aligned and rotated cases, which the old shell got wrong because its only call site passed EditOptions::default() and never asked the page anything
       · fixture D:\Dev\pdfceGUI\tools\ui-verify\../../fixtures/tail-alignment.pdf — 1017 bytes, page 1 is 612 x 792 pt
       · launched on D:\Dev\pdfceGUI\tools\ui-verify\../../fixtures/tail-alignment.pdf as pid 14096
       · the ribbon control armed the caret tool
       · the click placed a caret: `pdfce-diag text-edit-caret kind=Edit page=0 run=0 len=10`
       · the commit planned: `pdfce-diag text-edit-plan page=0 run=0 disposition=Pin reason=Flush(Right) pinned=true`
       · the copy is 1643 bytes, of which 626 were appended
       · ★ the untouched line's operator `412.64 668.00 Tm` is in the appended revision verbatim
       · launched on target/ui-verify\text-edit-round-trip.pdf as pid 32756
       · a second process (pid 32756) opened the copy and drew it
       artifact: target/ui-verify\text_edit.trace.txt
       artifact: target/ui-verify\text_edit.reopen.trace.txt

[FAIL] text_edit_on_a_real_drawing
       detects: Edit text arms on a real CAD sheet and a click on text the engine reports as present resolves no run — or it declines and the operator is never told why, which is indistinguishable from a feature that does nothing and is the operator's own report
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2383.937 x 1683.78 pt, aiming at (300.0, 500.0) on page 1
       · launched target/scratch/drive/pdfce-gui.exe as pid 26420
       · Edit ▸ Edit text armed the caret on the real drawing
       · ★ the click on real drawing text placed a caret: `pdfce-diag text-edit-caret kind=Edit page=0 run=0 len=10`
       · this point is on a single-run line, so no multi-piece disclosure was owed. The refusal this check exists for is not exercised at this coordinate — aim at a table cell to reach it
       → the caret took keystrokes, the commit REACHED the engine, and the engine
         refused it: `pdfce-diag edit-text-refused page=0 n=1 detail=R-INV-2: font
         'AAAAAA+JetBrainsMono-Regular' is symbolic with a built-in/custom cmap and
         no usable /Encoding (§9.6.6.4 Branch B ignores /Encoding); its code↔glyph
         relation lives inside the embedded program, which pdfce-core does not parse
         (R21). Editing is refused.`. The shell half of this works — a caret was
         placed on run 0, characters were typed and the plan was built. This is a
         `pdfce-core` verdict and belongs in a request, not in the shell. Trace:
         target/ui-verify\text_edit_real.trace.txt.
       artifact: target/ui-verify\text_edit_real.trace.txt

[PASS] read_mode_refuses_canvas_edits
       detects: a click on page content in Read mode still selects it, or a selection made in Edit survives into Read — the mode gate's capabilities not reaching the canvas, which is the one link in that chain no unit test observes
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 35336 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · window client area 1100x800 px at desktop (788, 71), DPI scale 1.00
       · --doc-point (page 1, 300, 500) will be tried first
       · candidate 1: the click at (300, 500) traced no `canvas-selection via=click` line in read
       · candidate 1: the SAME click in edit traced `pdfce-diag canvas-selection via=click mod=false sel=1 level=Object first=object:0` — so the point is on content, the pointer reached the canvas, and read's silence a moment ago was a refusal rather than a miss
       · entering read traced `pdfce-diag mode-capabilities content=false markup=false measure=false retired_tool=false cleared_selection=true abandoned_drag=false cleared_text=false cleared_annot=false` — the selection made in edit was dropped on the way in
       · verdict established at document point (300, 500) on page 1: read refused the click that edit honoured, and read dropped the selection edit had made
       artifact: target/ui-verify\read_mode.trace.txt

[PASS] text_selection_sweeps_and_copies
       detects: a drag across text on the page selects nothing, selects text but highlights nothing, or sweeps text in a mode whose primary button is the content marquee — the canvas wiring that assembles a page's extraction per frame, which is the one link in that chain no unit test observes
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 28108 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · band 1: the sweep traced `pdfce-diag canvas-text-selection via=drag page=0 chars=4 quads=1` — so the drag reached the canvas, the press meant text, and the range covers glyphs
       · the window with the selection on it is saved beside the trace. It is evidence, not the oracle: at the wash's alpha over a drawing sheet, selected and unselected text are the same picture to any threshold, which is why this check asserts on `chars` and `quads` instead
       · 1 `page-text` line(s) so far — one per (page, edit epoch) actually extracted. A sweep is many frames; anything near the frame count means the cache in `app::cache::PageTextCache` is not being hit
       · most recent extraction: `pdfce-diag page-text page=0 runs=27 chars=123 ms=0 status=ok`
       · the same band in edit traced no new `canvas-text-selection` — the primary button is the content marquee there, as it was before this feature existed
       · verdict established on band 1: read swept text the same drag did not sweep in edit, and the selection it made both copied characters and drew boxes
       · not covered here: Escape clears the selection, Ctrl+A selects the page and Ctrl+C copies it. This check does not drive them; keystrokes DO reach the window (see find_bar), so all three are covered by unit test alone and the gap is on the record rather than implied by a green result
       artifact: target/ui-verify\text_selection.trace.txt
       artifact: target/ui-verify\text_selection.png

[PASS] rotated_text_selects_and_copies_as_one_line
       detects: text placed at 90 degrees on the page selects one letter per line, shades each letter as a separate box in the wrong place, pastes with a newline between every character, and leaves the I-beam upright over it
       · launched target/scratch/drive/pdfce-gui.exe as pid 24684 on D:\Dev\pdfceGUI\tools\ui-verify\..\..\fixtures/rotated-text.pdf (612x792 pt)
       · sweeping the 90° string from (97, 303) to (97, 350) in PDF user space — up the page, three points to the LEFT of the baseline, which is where the ink of anticlockwise-turned text sits
       · the sweep traced `pdfce-diag canvas-text-selection via=drag page=0 chars=6 quads=1`
       · the I-beam turned: angles traced during the drag were [90]
       · the OS clipboard holds "UPWARD" — read from outside the process, which is the only place the operator's complaint can be answered
       artifact: target/ui-verify\rotated-text.trace.txt

[PASS] text_markup_marks_a_selection
       detects: Markup ▸ Text markup ▸ Underline is live with nothing selected, does nothing when text IS selected, authors an annotation over different boxes from the ones highlighted, or marks a selection that a later edit has invalidated — the join between the text-selection gesture and the markup authoring path, which neither feature's tests can observe
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 11456 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · the review segment reported the click — the one mode that both selects text and authors markup (see this check's MODE constant)
       · the Markup tab reported the click
       · ribbon.item.markup.underline at LRect { min: Pt { x: 342.5, y: 35.0 }, max: Pt { x: 366.5, y: 59.0 } }, with ribbon.item.markup.strikeout and ribbon.item.markup.squiggly beside it
       · with nothing selected, a click on ribbon.item.markup.underline produced no `ribbon-command-invoked` — the control is greyed, which is what P3 requires of a command that can only act on a selection
       · band 1: the sweep traced `pdfce-diag canvas-text-selection via=drag page=0 chars=4 quads=1` — there is now a selection to mark
       · with text selected, the same control reported `ribbon-command-invoked id=markup.underline` — so the selection is what enabled it
       · the application traced `pdfce-diag text-markup-commit kind=Underline page=0 quads=1`
       · the wash and the mark agree on 1 box(es) — the one-derivation promise, asserted across two modules and two trace lines
       · the engine authored it: `pdfce-diag add-text-markup page=0 n=1 epoch=1 disclosures=none` — one annotation, one undo entry, the revision moved
       · a second press on the same control authored nothing: marking is an edit, the edit retires the selection that authored it, and the control greys itself. That is a deliberate difference from Acrobat (which keeps the selection) and is recorded at `canvas::textsel` section 7
       · the window with the authored markup on it is saved beside the trace. It is evidence rather than the oracle: what a reader needs from it is whether the line landed under the words that were swept, and the harness has no independent account of where those glyphs are
       · verdict established on band 1: greyed with nothing selected, live with a selection, authored one annotation over the boxes that were highlighted, and greyed again afterwards
       · not covered here: Strikeout and Squiggly are not driven — they are the same dispatch arm with one `match` arm between them, and `shell::commands::mapping` walks all three. Both controls' presence IS asserted above, so a half-done registration still fails. Nor is `review` compared against Edit: in Edit these controls are correctly dead for want of a text selection, and a phase asserting that would be pinning the known gap rather than the feature (see this check's MODE constant)
       artifact: target/ui-verify\text_markup.trace.txt
       artifact: target/ui-verify\text_markup.png

[PASS] a_click_inside_a_form_selects_what_is_drawn_there
       detects: a click inside a form XObject selects the form — a page-sized /BBox wins every click at every point — so on a wrapped drawing nothing the operator can see is selectable, and the outline hugging the page edge reads as 'the page is selected'
       · fixture D:/Dev/pdfce/fixtures/synthetic\forms-xobject/page-sized-form.pdf — one page object (a page-sized form) and three 40x40 squares painted from inside it
       · launched target/scratch/drive/pdfce-gui.exe as pid 36480
       · clicked the Edit mode segment first — the shell's default mode is Read, where a canvas click on content is refused by design (DEFECTS.md D6). A check that skipped this step once reported the mode gate as a selection defect
       · canvas rect LRect { min: Pt { x: 296.0, y: 217.0 }, max: Pt { x: 764.0, y: 685.0 } } at zoom 2.340
       · the middle square's centre (page 0, 100.0, 100.0) -> screen (1318, 522)
       · after the click on the square: first=leaf:1
       · the gap between the first and second squares (page 0, 65.0, 65.0) -> screen (1236, 604) — inside the form's page-sized /BBox and on no square
       · after the click in the gap: first=none
       artifact: target/ui-verify\form_selection.trace.txt

[SKIP] ocr_recognises_a_page_and_the_document_keeps_it
       detects: Recognise text is unreachable in the mode the operator asked for it in, produces no layer, writes nothing, or — the one that cannot be caught anywhere else — writes the recognised document back over the file that was opened
       · fixture D:\Dev\pdfceGUI\tools\ui-verify\..\..\fixtures/synthetic-image-only.pdf — 37214 bytes, digest c7b3b6776b3eb37d
       · launched target/scratch/drive/pdfce-gui.exe as pid 33052
       · the `file` tab is present and took a click while the mode selector is on `read` — so the command is reachable in the mode the operator asked for it in, which the specification's Tools placement would not have been
       → the application declared no `ribbon.item.file.ocr` region, so `file.ocr` has
         no control to click. Band controls it did declare: ribbon.item.file.new,
         ribbon.item.file.new_from_template, ribbon.item.file.open,
         ribbon.item.file.close, ribbon.item.file.save, ribbon.item.file.save_copy,
         ribbon.item.file.save_compacted, ribbon.item.file.print.
       artifact: target/ui-verify\ocr.trace.txt

[PASS] text_tool_selects_and_marks_in_edit
       detects: View ▸ Navigate ▸ Text does not arm the canvas tool, a sweep in Edit with it armed selects nothing, or Markup ▸ Underline stays greyed in Edit once text IS selected — the P3 tension of three controls drawn on a tab where they could never enable, and the join between a ribbon toggle, a canvas gesture and a condition that no unit test observes in one window
       · fixture fixtures/a1-titleblock.pdf — page 1 is 2384x1684 pt
       · launched target/scratch/drive/pdfce-gui.exe as pid 38432 with PDFCE_DIAG=1 and EGUI_SHELL_DIAG=1
       · the edit segment reported the click — the mode whose primary button is the content marquee, and therefore the only one where the text tool is not redundant
       · ribbon.item.markup.underline at LRect { min: Pt { x: 342.5, y: 35.0 }, max: Pt { x: 366.5, y: 59.0 } } on the Markup tab in edit
       · with nothing selected, a click on ribbon.item.markup.underline produced no `ribbon-command-invoked` — the control is greyed, which is the state this feature had to make ESCAPABLE rather than remove
       · ribbon.item.view.tool_text at LRect { min: Pt { x: 204.0, y: 35.0 }, max: Pt { x: 228.0, y: 59.0 } }, with ribbon.item.view.tool_hand beside it in View ▸ Navigate
       · the control armed the canvas: `pdfce-diag text-tool tool=Text`. This line is the ONLY evidence available — an armed text tool changes the cursor and nothing else, and a captured window does not carry the cursor
       · band 1: the sweep traced `pdfce-diag canvas-text-selection via=drag page=0 chars=4 quads=1` — a drag in edit selected TEXT, which is the inversion this tool closes
       · the window with the Edit-mode text selection on it is saved beside the trace. Evidence, not the oracle: the wash is deliberately low-alpha and the cursor is not captured at all
       · ★ the SAME control that phase A found greyed reported `ribbon-command-invoked id=markup.underline` — in edit, with a text selection made by the text tool. That is the P3 tension closed, observed from outside the process
       · the engine authored it: `pdfce-diag add-text-markup page=0 n=1 epoch=1 disclosures=none` — and the wash and the mark agree on 1 box(es), which is the one-derivation promise asserted across two modules and two trace lines
       · with the tool retired the SAME band traced no new `canvas-text-selection` — so the sweep in phase C was caused by the tool, not by a missing mode gate. Without this phase every assertion above would hold against a build that had deleted the gate outright
       · verdict established on band 1: Underline greyed in edit, the ribbon control armed the canvas tool, the same drag then selected text, Underline came alive and authored one annotation over the boxes that were highlighted, and retiring the tool put the content marquee back
       · not covered here: Escape with the tool armed (rung 5 clears the text selection, and the tool is deliberately NOT an Escape claimant — see `canvas::keys`' header), and Ctrl+A / Ctrl+C in Edit. This check does not drive them; keystrokes DO reach the window (see find_bar), so all three are covered by unit test alone and the gap is on the record rather than implied by a green result
       artifact: target/ui-verify\text_tool.trace.txt
       artifact: target/ui-verify\text_tool.png

[PASS] render_diagnostics_opens_its_report
       detects: Tools ▸ Render diagnostics is drawn, enabled, and opens nothing
       · launched target/scratch/drive/pdfce-gui.exe as pid 20372
       · the shell traced `ribbon-command-invoked id=tools.render_diagnostics`, so the click reached the control
       · the report was laid out at 520 x 380 pt
       artifact: target/ui-verify\render_diagnostics.trace.txt
       artifact: target/ui-verify\render_diagnostics.png

[PASS] find_opens_and_finds
       detects: Ctrl+F does not reach Find, or Find opens without finding anything
       · launched target/scratch/drive/pdfce-gui.exe as pid 14752
       · control chord Ctrl+2 arrived, so the input channel works
       · Ctrl+F dispatched `edit.find`
       · the find bar reported open
       · find-bar occupies 474.0 x 38.0 pt
       · searched e and reported 9 hit(s)
       artifact: target/ui-verify\find_bar.trace.txt

[SKIP] read_mode_hides_the_chrome
       detects: View ▸ Read mode is drawn, bound to Ctrl+H, and does not hide the ribbon or the panels
       · launched target/scratch/drive/pdfce-gui.exe as pid 31012
       → the click on `ribbon.item.view.fullscreen` produced no new
         `ribbon-command-invoked id=view.fullscreen` line, so no click reached the
         ribbon. Nothing has been done to the display and nothing was learned; see
         `egui-shell-diag` in target/ui-verify\read_mode_chrome.trace.txt.
       artifact: target/ui-verify\read_mode_chrome.trace.txt

[SKIP] settings_headings_legible
       detects: D2 — the theme assigns widgets.active.fg_stroke a near-white but never assigns widgets.active.bg_fill, so CollapsingHeader headings and dock tab labels render near-white on light grey
       · launched target/scratch/drive/pdfce-gui.exe as pid 37024
       → `ribbon.item.file.settings` was not on the band, in any collapsed group, or
         in the overflow, so there is no route to the Settings dialog on this window.
         Ribbon regions declared: ribbon.qat.file.open, ribbon.qat.file.save,
         ribbon.qat.edit.undo, ribbon.qat.edit.redo, ribbon.tab.file,
         ribbon.tab.view, ribbon.modes, ribbon.mode.read, ribbon.mode.review,
         ribbon.mode.edit, ribbon.item.file.new, ribbon.item.file.new_from_template,
         ribbon.item.file.open, ribbon.item.file.close,
         ribbon.group.file.file.caption, ribbon.group.file.file,
         ribbon.group.file.recognise.collapsed, ribbon.item.file.save,
         ribbon.item.file.save_copy, ribbon.item.file.save_compacted,
         ribbon.group.file.save.caption, ribbon.group.file.save,
         ribbon.group.file.export.collapsed, ribbon.overflow, ribbon.item.file.print,
         ribbon.group.file.print.caption, ribbon.group.file.print,
         ribbon.item.file.ocr, ribbon.group.file.recognise.caption,
         ribbon.group.file.recognise, ribbon.item.file.export_dxf,
         ribbon.item.file.export_form_data, ribbon.item.file.import_form_data,
         ribbon.item.file.copy_page_text, ribbon.item.file.copy_document_text,
         ribbon.group.file.export.caption, ribbon.group.file.export,
         ribbon.group.file.document.collapsed, ribbon.group.file.pdfce.collapsed,
         ribbon.scroll.left.
       artifact: target/ui-verify\settings_headings.trace.txt

[PASS] ui_scale_resizes_the_chrome
       detects: the UI-scale preference is parsed, edited and never handed to egui, so every control is drawn at one size on every machine — or it IS applied and something lands outside the window at a large scale, which no unit test can see
       · launched at ui_scale = 1.00 as pid 29812
       · launched at ui_scale = 1.80 as pid 23256
       · client area 1100x800 pt → 611x444 pt (the OS window is 1100x800 px at both scales, so a smaller area in POINTS is `pixels_per_point` having moved) — x1.80
       · both launches traced `ui-scale-initial`, so the preference was applied before the first frame and no launch flashed at the wrong size
       ·   ribbon.tab.file              28.3x24.0 pt (both) · 2.57%x3.00% → 4.63%x5.40% of the window (x1.80, y1.80)
       ·   ribbon.modes                 179.1x24.0 pt (both) · 16.28%x3.00% → 29.29%x5.40% of the window (x1.80, y1.80)
       ·   ribbon.qat.file.open         24.0x24.0 pt (both) · 2.18%x3.00% → 3.93%x5.40% of the window (x1.80, y1.80)
       ·   status-page-box              44.0x19.0 pt (both) · 4.00%x2.38% → 7.20%x4.27% of the window (x1.80, y1.80)
       · 4 of 4 chrome regions scaled by ~1.80
       · all 81 declared region(s) fit inside the 611x444 pt client area at 1.80x
       ·   page                         468.0 pt wide → 238.4 pt wide   (x0.51)
       artifact: target/ui-verify\ui_scale.base.trace.txt
       artifact: target/ui-verify\ui_scale.base.png
       artifact: target/ui-verify\ui_scale.large.trace.txt
       artifact: target/ui-verify\ui_scale.large.png

------------------------------------------------------------------------
  57 passed, 10 failed, 25 skipped

RESULT: FAIL — a check drove the application and the assertion did not hold.
