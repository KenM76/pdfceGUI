ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[FAIL] the_line_weight_switch_reaches_the_resize
       detects: the operator's Scale line weight switch cannot reach the engine — `annots::resize` DERIVES `scale_stroke_width` from whether the drag was proportional, so the switch is drawn, stores its value, and is overridden on exactly the resizes where somebody was most likely to have an opinion
       · launched target\sweep\pdfce-gui.exe as pid 21960 with the Tool panel open
       · ★ a rectangle was authored
       → ★★★ THE SWITCH IS NOT DRAWN after 5 Escape(s): no LIVE
         `tool.scale.stroke` region. **The state the feature shipped in for one
         afternoon** was that the switches were written into
         `panels::tool::armed::options`, and `super::body` calls the armed block only
         in its `else` arm — Select is this panel's IDLE state, so the branch was
         dead code that compiled and drew nothing. ★ If the region APPEARS in the
         list below but is not live, that is the other failure: the panel drew it and
         retired it, meaning a tool re-armed. Regions beginning `tool.`:
         tool.pointer, tool.tools, tool.row.view.tool_select,
         tool.row.view.tool_node, tool.row.view.tool_text, tool.row.view.tool_hand,
         tool.row.markup.rectangle, tool.row.measure.linear, tool.scale_switches,
         tool.scale.stroke, tool.scale.insets, tool.scale.distort, tool.armed. Trace:
         D:\Dev\pdfceGUI\evidence\sweep-20260828\switch6\scale-switch.trace.txt.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\switch6\scale-switch.trace.txt

------------------------------------------------------------------------
  0 passed, 1 failed, 0 skipped

RESULT: FAIL — a check drove the application and the assertion did not hold.
