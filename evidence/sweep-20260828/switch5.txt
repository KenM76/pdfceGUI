ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[SKIP] the_line_weight_switch_reaches_the_resize
       detects: the operator's Scale line weight switch cannot reach the engine — `annots::resize` DERIVES `scale_stroke_width` from whether the drag was proportional, so the switch is drawn, stores its value, and is overridden on exactly the resizes where somebody was most likely to have an opinion
       · launched target\sweep\pdfce-gui.exe as pid 41624 with the Tool panel open
       · ★ a rectangle was authored
       · ★ the pen went down and the Select options drew (attempt 1)
       · ★★ the switch is on: `pdfce-diag resize-modifiers stroke=true keep_rd=false distort=false`
       · ★ the shape was selected, so its grips are drawn
       · ★★★ the engine resized it: `pdfce-diag resize-annotation-applied id=390 sx=1.9722 sy=2.6923 uniform=false keys=0 appearance=Rebuilt stroke=true`
       → the drag came out NON-uniform (`pdfce-diag resize-annotation-applied id=390
         sx=1.9722 sy=2.6923 uniform=false keys=0 appearance=Rebuilt stroke=true`),
         and this check drags equal amounts in both axes on purpose so that it is
         about the switch rather than about the distortion refusal. Reported as
         SKIPPED rather than failed: the assertion above passed, and a non-uniform
         drag means the harness's two axes disagree — probably a page whose aspect
         ratio makes equal page-fraction travel unequal in points.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\switch5\scale-switch.trace.txt

------------------------------------------------------------------------
  0 passed, 0 failed, 1 skipped

RESULT: NOTHING VERIFIED — no check reached a verdict.

  This is NOT a pass. Every check above was unable to begin, and the
  reasons are printed with each one. A suite that cannot run has told
  you nothing about the application, and 'told you nothing' rendered
  as green is the exact failure this harness exists to remove.
