ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[FAIL] a_pan_leaves_the_fit
       detects: a fit stays active after the operator pans away from it, so the next canvas resize re-places the page and throws away the position they chose — the half of O55 that only bites once a resize re-places at all
       · ★ fitted: page 296.0,269.7..764.0,631.3
       · ★★ panned: page 417.0,423.7..885.0,785.3
       · ★★★ after the resize: margins l=8.0 r=8.0 t=108.4 b=108.3
       → ★★★ THE RESIZE RE-CENTRED A PAGE THE OPERATOR HAD PANNED AWAY FROM:
         margins l=8.0 r=8.0 t=108.4 b=108.3 are equal on both axes.
         `OPERATOR_REQUESTS.md` O55: *"unless the person has changed the zoom or
         panned around"*. The fit is still active after the pan, so
         `canvas::fit::placement` re-placed on the viewport change and discarded the
         position they chose. `canvas::offset`'s pan arm must call
         `doc.view.set_fit(FitMode::None)`. Note that the WHEEL deliberately does not
         — `a_fit_command_puts_the_page_on_screen` asserts the opposite for it, and
         a build that treats all view movement alike fails one of this pair whichever
         way it goes. Trace:
         D:\Dev\pdfceGUI\evidence\sweep-20260828\falsify\fit-pan.trace.txt.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\falsify\fit-pan.trace.txt

------------------------------------------------------------------------
  0 passed, 1 failed, 0 skipped

RESULT: FAIL — a check drove the application and the assertion did not hold.
