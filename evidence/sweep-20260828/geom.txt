ui-verify — profile `pdfce-gui` (the application this project is building (crates/pdfce-gui))
  NOTE: this harness drives the REAL cursor and keyboard. It raises the target window, moves the pointer, and types into it. The pointer is put back where it was when the run ends. Pass --no-input to disable (checks that need input then report SKIPPED, never PASS).

[SKIP] dragging_a_form_field_moves_it
       detects: a form field's box can be placed, selected, renamed and deleted and cannot be DRAGGED — the press is consumed by the canvas's content branch, which finds no content under it and drops the gesture, so the operator drags a field across the sheet and it stays where it was with no message anywhere
       · launched target\sweep\pdfce-gui.exe as pid 28008 with PDFCE_DIAG_INVOKE=mode.edit,edit.form_text_field
       → the point (1225, 492) is owned by "Inbox - ken@toprops.com - Outlook"
         (window class `rctrl_renwnd32`, pid 26700), not by the application under
         test, so a click there would go to that window instead. Something is drawn
         OVER the target. The recorded case on this machine is `osk.exe`, the
         on-screen keyboard, which synthetic keystrokes summon and which cannot be
         closed from a process of ordinary integrity — but read the name above
         before assuming it: a stray dialog left on the desktop behaves identically.
         Reported rather than clicked: sending the click anyway would make this check
         report a working feature as broken, which it did repeatedly before this
         guard existed.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\widget-move.trace.txt

[FAIL] right_clicking_a_form_field_opens_its_menu
       detects: a form field can be clicked, dragged, resized and deleted and has no CONTEXT MENU — a right-click on a text box resolves the view menu and offers four zoom levels, because a /Widget is in neither the object selection nor the caret and the canvas had only those two questions to ask
       · launched target\sweep\pdfce-gui.exe as pid 38336 with PDFCE_DIAG_INVOKE=mode.edit,edit.form_text_field
       · ★ placed the field "Text1" at canvas (263.0, 346.3)
       → THE FIELD COULD NOT BE SELECTED: a click at its centre produced no
         `form-field-selected` line naming "Text1". Selecting has worked since the
         form surface landed, so this says the click missed or the tool was still
         armed. Trace:
         D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\field-menu.trace.txt.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\field-menu.trace.txt

[FAIL] form_field
       detects: the five form-field commands arm a tool and a click on the page places nothing — or a field is placed and clicking it again offers no way to rename or delete it, leaving every form pdfce authors editable only by authoring it again
       · launched target\sweep\pdfce-gui.exe as pid 34812 with PDFCE_DIAG_INVOKE=mode.edit,edit.form_text_field and PDFCE_DIAG_FORM_ACCEPT=1
       · the text-field tool is armed
       · clicking empty page at PDF (150, 260) on page 0
       · the dialog opened for kind=Some("Text") named Some("Text1")
       · a field was authored
       · aiming at the field "Text1" the application placed at canvas (229.8, 522.2)
       → clicking an existing form field selected nothing: no `form-field-selected`
         line with a field name. In Edit mode a click on a widget must select it for
         its properties; what an operator meets if this regresses is a field they can
         see, can place, and cannot rename or delete. Trace:
         D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\form_field.trace.txt.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\form_field.trace.txt

[PASS] resize_scales_a_shape
       detects: the eight resize grips change the cursor, consume the drag and commit nothing — a control that looks available and is inert, which the operator experiences as a resize that silently does not work
       · launched target\sweep\pdfce-gui.exe as pid 17044
       · the click selected a shape, so the outline and its grips are drawn
       · ★ the resize committed: `pdfce-diag resize-commit grip=SouthEast sx=1.2188 sy=1.5833 ax=100.00 ay=380.00`
       · ★★ the scale reached the engine through `transform_objects`
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\resize.trace.txt

[FAIL] the_line_weight_switch_reaches_the_resize
       detects: the operator's Scale line weight switch cannot reach the engine — `annots::resize` DERIVES `scale_stroke_width` from whether the drag was proportional, so the switch is drawn, stores its value, and is overridden on exactly the resizes where somebody was most likely to have an opinion
       · launched target\sweep\pdfce-gui.exe as pid 25296 with the Tool panel open
       · ★ a rectangle was authored
       · ★ the shape was selected, so its grips are drawn
       → ★★★ THE SWITCH IS NOT DRAWN: the application declared no
         `tool.scale.stroke` region with the Select tool armed and the Tool panel
         open. `panels::tool::armed::options` returns early for every tool but
         Select, and this check disarms the markup pen at step B — so if the pen
         did not go down, the panel is showing the markup options instead. Regions
         beginning `tool.`: tool.pointer, tool.tools, tool.row.view.tool_select,
         tool.row.view.tool_node, tool.row.view.tool_text, tool.row.view.tool_hand,
         tool.row.markup.rectangle, tool.row.measure.linear, tool.armed. Trace:
         D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\scale-switch.trace.txt.
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\scale-switch.trace.txt

[PASS] rotate_handle_turns_a_selection
       detects: there is no way to rotate anything on the canvas — the third word of the operator's "reposition, resize, or rotate", with no affordance at all. Or the handle is drawn and a press on it MOVES or RESIZES the selection instead, which is a working gesture aimed at the wrong verb and looks entirely deliberate
       · launched target\sweep\pdfce-gui.exe as pid 37472
       · the click selected something, so the outline and its handles are drawn
       · ★ the rotation committed: `pdfce-diag rotate-commit deg=-89.70 px=340.00 py=290.00 objects=1 constrained=0`
       · ★★ and it turned the right way: -89.70° in page space for a clockwise drag on screen
       · ★★ the rotation reached the engine through `transform_objects`
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\rotate.trace.txt

[PASS] shift_constrains_a_resize
       detects: Shift does not preserve aspect on a resize — *the* resize convention, present in every program in the class, and absent from every drag in this shell until 2026-08-20. An operator holding Shift gets a free-form resize and cannot tell whether the key did anything
       · launched target\sweep\pdfce-gui.exe as pid 11496
       · the click selected a shape, so the outline and its grips are drawn
       · unmodified: sx=1.3281 sy=1.1167 — the control
       · ★ with Shift: sx=1.3281 sy=1.3281
       · ★★ the locked resize kept the dominant factor: 1.3281, the same the free drag produced on x
       · ★ and it was announced — `constrain lock=Aspect` reached the status row
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\shift-constrains.trace.txt

[PASS] multi_node_move_moves_every_picked_anchor
       detects: Shift-picking several anchors highlights all of them and a drag moves ONE — a selection the program shows and does not honour, which is worse than not offering it, because the operator has no reason to check
       · launched target\sweep\pdfce-gui.exe as pid 32408
       · the Part rung was entered on an object with 6 anchors: `pdfce-diag canvas-anchors total=6 selected=0 unselected_drawn=6`
       · 2 anchor(s) selected after the Shift-click
       · ★ the drag committed: `pdfce-diag canvas-move page=0 level=Node dx=33.0723 dy=-33.0723 action=Vector(MoveNodes { page: 0, object: 0, moves: [(0, Point { x: 133.0723419189453, y: 166.92767333984375 }), (1, Point { x: 233.0723419189453, y: 286.92767333984375 })] })`
       · ★★ every picked anchor reached the engine as ONE `move_nodes` command
       artifact: D:\Dev\pdfceGUI\evidence\sweep-20260828\geom\multi_node.trace.txt

------------------------------------------------------------------------
  4 passed, 3 failed, 1 skipped

RESULT: FAIL — a check drove the application and the assertion did not hold.
