# pdfceGUI — what is built, and what is next

**Updated:** 2026-08-19 (seventh revision). **Scope:** the new shell only. `pdfce-core` and
`pdfce-cli` capabilities live in `D:\Dev\pdfce\docs\FEATURES.md`, whose
**`gui` column is this project's acceptance criteria** — nothing there may
regress at fold-in.

Two lists. The first is what works **today, in the running binary**. The
second is what does not, **in the order it is planned**.

**A row is ticked only when an operator can reach it in a real build.**
Not when the code exists, not when a test passes. Three panels in the old
shell shipped with a body, a rail entry and a diagnostic step and **no
control anyone could click**, and every verification passed for the whole
of their shipped life. That is what this bar exists to prevent.

**Legend:** ✅ reachable · 🔨 in progress · ⬜ planned · ⛔ blocked, with
the blocker named.

---

## Where it stands

| | |
|---|---|
| **Stages complete** | S0 skeleton · S1 `ui-verify` · S2 ribbon · S3 panels + dock · S4 selection · **S5 salvage** (print, forms, icons, settings) · **Phase 3** (navigation, Find, thumbnails, rulers, grid, guides) · **Phase 4** (page display modes) · **Phase 5** (text editing incl. lines shared with other runs; aligned and rotated tails; new text with a font, size and colour; live re-layout blocked on the engine) · **Phase 1 complete but for the clipboard** (selection, move, resize by grip and by typed number, multi-node move, Bézier handles) · **Phase 6** (markup substrate, **eight kinds** including the revision cloud, text markup, Comments panel) · **Phase 7** (measure: Linear, Two-line, Radius/diameter, snapping, dimension groups) · **the mode gate** (Read is genuinely read-only, canvas *and* keyboard) |
| **Tests** | 1,525 (GUI) + 384 (egui-shell) + 144 (ui-verify) passing, 0 failing · **56 driven checks**, of which **50 verified** on the last full run and 6 skipped for a stated reason, **0 failed** |
| **Gates** | 14 of 14 checks, 0 skipped — eight gate scripts, five of them carrying a self-test that catches its own planted violations, plus `fmt` and `clippy` |
| **Source** | ~215,000 lines across three crates and the harness |
| **Commands** | 106 registered · 74 declared-and-deferred (`PLANNED`) · **14 registered-but-inert** (`SCAFFOLDED`), five of which breach P3 by being drawn at all |
| **Panels** | **12** — Pages · Bookmarks · Layers · Signatures · Fonts · Objects · Properties · Forms · Comments · Redact · Dimension groups · **Tool** |
| **Ribbon surface built** | ~55 % of `RIBBON_IA.md` §5 · 31 groups |

---

## ★★ What landed on 2026-08-20 — **several documents at once**, and pages
## dragged between them

The operator's request, verbatim:

> *"make it so we can open multiple PDFs at once and drag and drop pages from
> one thumbnail image sidebar to another or onto the canvas to add pages and
> insert them in between the pages we've dragged to on the canvas or the
> thumbnail preview area."*

| | |
|---|---|
| **Several documents open** | ✅ a **document tab strip** under the ribbon — labels, an unsaved `*` marker, a ✕, middle-click to close, `Ctrl+Tab` / `Ctrl+Shift+Tab` to cycle, `Ctrl+W` to close, an overflow menu past the width. Opening a file that is already open activates its tab instead of opening it twice |
| **Drag a page between documents** | ✅ press a thumbnail, rest on the other document's **tab** until it springs open, drop at a caret between two sheets. The drag survives the document switch because it lives in `egui::Memory` rather than on the panel that started it |
| **Drop onto the page view** | ✅ the same gesture, released on the canvas, with a horizontal caret in the gap between two sheets — every display mode, including single page |
| **It is a COPY, and it says so** | ✅ *"Copy 2 sheets from SW41177.pdf to before page 3."* on the status row before the button is released. A cross-document move would be two commands on two undo stacks with no single `Ctrl+Z` able to reverse it |
| **★ Open and New stopped destroying work** | ✅ and by **removing** a guard rather than adding one. They no longer replace the open document, so the unsaved-edits prompt in front of them would have been stating something false — and a confirmation that says something untrue is how an operator learns to dismiss confirmations unread |
| **Live window title** | ✅ `SW41177.pdf — 3 documents open — pdfce`, so Alt-Tab and the taskbar say what you left open |

### And what the operator asked for on the second pass, same day

| | |
|---|---|
| **Shift makes it a move** | ✅ Windows' own drag modifier — Ctrl copies, Shift moves — sampled at the **release** as Explorer does, so the caption follows the key under your hand. The source's pages go only if the target's insert actually happened, and if the removal is the half that fails it says so, with the remedy |
| **Tabs rearrange by dragging** | ✅ a caret marks the boundary, resolved by neighbours' centres. **The document on screen does not change** — the active document follows its own tab through the permutation, swept across all 80 combinations |
| **Right-click on a tab** | ✅ Close, and Close others. Close is `file.close` itself with the tab as its operand, not a second command — two gates refused the second command and were right |
| **Drag a tab off into a new window** | ⛔ **not built** — it is a scope question with two different conventional answers. See below |

### ✅ Driven — 50 verified, 0 failed, 6 skipped

All four multi-document checks pass against the real binary:

```text
two_documents_get_two_tabs                       PASS
document_tabs_can_be_rearranged                  PASS   document-reorder from=0 gap=2 to=1 active=0
a_page_dragged_between_documents_is_copied       PASS   copied=1 take=0 · 36 → 37 · source untouched
a_shift_drag_between_documents_moves_the_pages   PASS   copied=1 take=1 · 36 → 37 · page-move-took removed=1
```

The run also found two things a green `cargo test` could not, both recorded in
`D:/dev/rag/egui/`:

- **the drag caption moved the grid under the pointer** — a wrapping label above
  a drop target, whose height depends on its own wording, moved the aimed-at row
  by 49 pt and then 34 pt mid-gesture. Moved to the status bar, which has a fixed
  height by construction;
- **`Response::hovered()` is false on every widget but the dragged one**, so the
  spring-loaded tab could never fire. Resolved geometrically now.

### ⛔ Dragging a tab off the strip into a new window — the one open question

Not built, and not because it is hard to *start*. The two applications an
operator would compare it to **disagree about what it means**:

| | what dragging a tab off does |
|---|---|
| **Chrome, Firefox, VS Code** | the document **moves** to a new window, fully functional |
| **Acrobat `Window ▸ New Window`**, Bluebeam's split | a **second view** opens; the document stays where it was |

`CONTINUE.md` §1b makes the convergence of the product class the specification.
Here there is no convergence, so it is a scope decision — and the two cost very
different things:

- **a second view** (canvas + page list + status bar, document stays in the
  strip) is roughly a day, and it delivers the *original* request from another
  direction: two page lists visible at once to drag between;
- **a full move** needs every window to own its ribbon state, dock arrangement,
  mode and panel state — a `Workspace` extraction — because a document that has
  left the strip must still be editable, and today all of that is
  application-scoped.

Building the wrong one is worse than building neither: a window the document
cannot be edited in is a trap, and a second view that pretends to be a move is
a lie about where the operator's work is.

---

## ★ What landed on 2026-08-19, and why this revision exists

Four of the operator's six standing complaints closed in one day, plus two
defects nobody had reported because neither had a symptom anybody could see.

| | |
|---|---|
| **The I-beam** | ✅ two-tone, legible on white — his #1 |
| **Measure hover** | ✅ entity **and** node, before the first click — his #2 |
| **Dimension groups** | ✅ a **dock panel** with six foldable sections, replacing a window whose content could push its own title bar off the desktop — his #3 |
| **Tool panel** | ✅ what the pointer does, what is armed, where every tool lives — his #4, **and his #5** |
| **Revision cloud** | ✅ `markup.cloud`, its own glyph, `/BE /I 1.0` — his #6, blocked on a blocker that had already been removed |
| **⚠ Unsaved edits** | ✅ **Close, Open and New destroyed every edit since the file was opened, silently, while `file.close`'s tooltip promised otherwise.** Found by audit, not by use |
| **⚠ Settings coverage** | ✅ a gate asserting every setting the engine carries has a control — it found `quad_point_order`, which had none |

★ His #5 was *"no text editing or adding text on the canvas"*, and it was
**false in the literal sense**: `edit.text` and `edit.add_text` were registered,
drawn, chord-bound and driven-verified. He could not find them. That is a
discoverability defect, and it was marked green for three weeks because *"the
tests pass"* is not a report of working software. The Tool panel is the fix.

★★ **Two recorded blockers turned out to be false on the same day**, both
naming `pdfce-core`, both disproved by one `grep`. The revision cloud's cost the
operator three weeks of asking for a tool whose only blocker had already
shipped. A blocker that names a repository this project does not build **cannot
fail a test** — nothing compiles differently, nothing lints, and CI stays green
*precisely because the feature is still absent*. Every external blocker here is
now written as a dated citation rather than as a verdict. See `NO_SURFACE.md`
§1c and `D:\dev\rag\rust\`.

---

## Complete

### Shell and chrome

- ✅ **★★ A dialog is a real OS window — Print, 2026-08-20.** The operator's report: *"Print dialogue box doesn't pop up in its own movable window. It is locked within the boundaries of the program's window. Like, I just assume you've been trained on a million lines of code and software that pops it up in its own window."* The last sentence is the diagnosis, and the convention corpus says why immediate-mode toolkits get it wrong: the in-viewport window widget is one function call and the real thing needs a newer, more awkward viewport API — the default is wrong and nothing pushes back. Every retained toolkit makes the OS window the **default** and the inline panel the special case, and `dialogs::host` restores that ordering here, so **the other thirteen dialogs are one line each** rather than thirteen implementations. Title bar, taskbar entry, draggable outside the application and onto a second monitor; it degrades to the old in-viewport window on a backend with no multi-viewport support, which is the web target, and egui owns that fallback. **Enter presses Print** and Print is drawn in the theme's selection fill so you can see what Enter will do; Escape is Cancel and is the X. It comes back where you left it for as long as it is open — not across a restart, because a remembered position must be validated against the *current* monitors and a dialog that opens on an unplugged screen is worse than one that opens centred. ⛔ **It is not OWNED by the main window and cannot be**: `eframe 0.35`'s window builder has thirty options and none of them is an owner, so the dialog can fall behind the parent. Always-on-top was considered and refused — it breaks the driven harness in a way that produces confident wrong bug reports. ★ **Verified without taking the operator's cursor**, through a new headless seam that presses one ribbon command in an invisible window: an offscreen window is genuinely laid out and OS input cannot reach it, so before this a headless run could read the trace and press nothing
- ✅ **Ribbon, seven tabs** — File · View · Pages · Edit · Markup · Measure · Tools, plus the contextual **Format** tab that appears on selection
- ✅ **The ribbon is data, not code** — a serializable manifest, which is what makes it customizable *and* reusable by another application
- ✅ **Mode selector** — Read / Review / Edit, right-aligned on the tab row, driving both the tab set and the panel layout
- ✅ **Quick access toolbar** — Open, Save a copy, Undo, Redo, drawn **icon-only** now that a painter exists
- ✅ **Open, Recent, Close** — a second document can be opened. `Ctrl+O` was in the keymap and printed in the tooltip since the ribbon landed and **did nothing**: `DERIVED` held only digits, so the chord could not be spelled
- ✅ **Keyboard chords derive from the manifest keymap** — `keyboard.rs` no longer knows what `Ctrl+0` *means*; it spells the key, looks it up, and returns a command id through the same dispatcher a ribbon click reaches. Rebind it in a customization layer and the keyboard follows
- ✅ **`Ctrl+1/2/3` switch mode**, `Ctrl+0` is actual size — one owner per chord, with a test that fails naming any chord claimed twice
- ✅ **Every ribbon control that can be ON renders pressed** — the last two were the hand tool and marquee zoom, whose armed flag lives in `egui::Memory` rather than on the document, so `conditions()` now takes the `Context`. The obvious alternative, a shadow copy on `PdfceApp`, was refused: it fails as a ribbon claiming Hand while the canvas selects, and each half would be self-consistent so no test would catch it. Published outside the `Status::Open` arm on purpose — with no document the control is greyed **and** pressed, which is exactly "this is the tool you are in, and there is nothing to use it on"
- ✅ **Group captions, enforced by construction** — one closure draws every group, so a caption cannot be omitted
- ✅ **Band overflow** — reserved-space "⏷ N more", proven hit-testable at a width narrow enough to hide groups
- ✅ **Tab-strip overflow** — active tab pinned; collapses to the affordance below ~47 pt rather than hiding the tab you are looking at
- ✅ **Status bar** — render-notes disclosure (closed by default), Actual size / Fit width / Fit page, zoom −/%/+, page ⏴ n/N ⏵
- ✅ **Find** — `Ctrl+F` and a status-bar toggle open a floating box at the page's top right: search, `3 of 47`, Enter / Shift+Enter to step, hits highlighted with the current one distinguished, and Match case · Whole word · Wildcards behind an Options menu. Literal by default — the old shell's bar ran through `find_text`, which patterns, so typing `?` matched every character on the page. An edit clears the highlights and says so rather than drawing a mark that may no longer be over the text it names. Measured at **350 ms** on the 5.6 MB benchmark drawing, which is why nothing searches on a keystroke
- ✅ **Editable page box** — type `37`, press Enter. Commits on Enter or focus loss, clamps out-of-range and says so, rejects non-numeric without discarding what you typed
- ✅ **Three themes** with a *rendered-pair* contrast gate over all five widget states — **and, as of 2026-08-14, actually installed.** This row was ticked from the day the theme module landed and was false for all of it: `Theme::apply` was never called, so every colour an operator ever saw was `egui`'s stock style. Found by sampling pixels, not by a test — see `DEFECTS.md` D10. Enabling it immediately exposed a second defect the stock style had been hiding: the active ribbon tab borrowed `egui`'s *selection* visuals, which this theme points at the translucent **canvas** object-selection tint, so the tab drew pale text on a 27 % wash. It now paints `accent` + `on_accent`, as the mode selector beside it always did. ~~**The preset is not yet choosable** — that is the settings dialog, still unsalvaged~~ **★ Corrected 2026-08-19: it became choosable on 2026-08-17 when the Settings dialog was salvaged, and this sentence was never updated.** `Settings ▸ Appearance`, three radios, live-previewed from the draft — you cannot judge a theme from its name. `dialogs/settings/appearance.rs`. The stale line was found by the `pdfce` session reading this file for the `gui`-column re-base and noticing it contradicted a later entry in the same file; the lesson is the same one `NO_SURFACE.md` §1c draws — **a claim with no falsifier decays**, and a 113 KB document nobody re-reads end to end is exactly such a place

- ✅ **⚠ Closing a document asks about unsaved edits — 2026-08-19, and until that day it did not.** `file.close`'s shipped tooltip has read *"You are asked what to do about unsaved edits first"* since the day it shipped, and nothing asked: `Action::Close` consulted `save_pending`, which is permanently `false` **by design** because the one save this build has is synchronous, and then dropped the `EditSession`. Every edit since the file was opened went with it, with no prompt and no undo. **Open and New did the same, and Open is the worse one** — an operator who marks up a drawing and then opens the next has destroyed exactly as much work, and is far more likely to do it. Why it survived is the useful half: **the guard that should have caught it existed, was well argued, was correct, and was answering a different question.** ★ The first button says **"Save a copy…"**, not *Save*, and it cannot say Save — `file.save` is `PLANNED`, blocked on autosave and crash recovery, so the only writer produces a new file and leaves the open one untouched. A *Save* label would be the same lie as the tooltip: the operator would name a file, press Close, and believe their work was in a document that still holds its original contents. The count is real — *"48 edits have been made since it was opened"* is a decision somebody can make where *"you have unsaved changes"* is not — and cancelling the file picker cancels the whole transaction, because pressing Cancel in a file dialog must never be a route to destroying a document
- ✅ **★ Every setting the engine carries has a control, and a gate says so.** `pdfce-core` gains settings on its own schedule and this project finds out by reading a note, so the old shell carried a test asserting the coverage — and it was about to be deleted with that crate. It could not be copied: it read the store's *source* through a relative path that only exists when both crates share a repository. The replacement enumerates from **`Settings::write_to_string`, at runtime, against the compiled dependency** — which is strictly better: it describes the engine actually linked, cannot be fooled by a `pub` field that is not persisted or by one persisted under a different key, and needs no path into another repository, so it survives the fold-in in either direction. It found `quad_point_order` immediately. ★ That setting's silence line is **the only one in the window that does not describe a silence**: §12.5.6.10 states a `/QuadPoints` corner order and essentially no producer follows it, so the sentence says pdfce is choosing between the wording and the tools rather than filling a hole — *"the standard is silent"* would have been the comfortable sentence and a false one

### Panels and dock

- ✅ **Dock** — multiple columns per side, vertical stacks, tabbed groups, draggable splitters
- ✅ **Tab overflow** — reserved-space menu; the old two-panes-per-group cap is retired (nine panels in one stack, tested)
- ✅ **Layout persistence** — `<settings dir>/layout.ron`, debounced 750 ms with a 5 s ceiling, per-item fail-soft loading
- ✅ **Named workspaces**, and **a mode is a workspace** — leaving Edit and returning restores your arrangement, not a default
- ✅ **Scoped reset** — right dock alone, left alone, or all
- ✅ **Bookmarks** — navigation, raising `GoToPage`
- ✅ **★ And a bookmark can be WRITTEN — 2026-08-19, the day `EditSession::add_outline_item` shipped.** pdfce had read bookmarks since the reader passes and carried **zero authoring verbs opposite them**; this was the first ask of the `insert_pages` request. Type a title, and it points at the page on screen, filed under whichever bookmark you last clicked — the click navigates *and* records the parent, because both are true of the row you pointed at. ★ **Nothing counts anything.** `/Count` is two different quantities and its **sign carries the open/closed flag** (§12.3.3 defines no `/Open` key), so a bookmark added under a **collapsed** ancestor leaves the document's visible total unchanged — a surface reporting *"added N"* by diffing it reports **zero for a correct save**. One call adds one bookmark and that is what is said. ★ And the collapsed case is **disclosed before the press**: the operator's real problem is not the count, it is adding a bookmark, looking at the panel and not seeing it — while the panel is correct. The parent is an `ObjId`, never a position, because a position is invalidated by the very edit this performs — which is the hazard the engine hit **in its own CLI**
- ✅ **★ Tool — 2026-08-19, and it closes two of the operator's complaints at once.** *"No side bar area showing what tool is active and its options"*, and *"no text editing or adding text on the canvas"* — the second of which was **false in the literal sense**, because `edit.text` and `edit.add_text` were registered, drawn on Edit ▸ Content, bound to `Ctrl+E`/`Ctrl+Shift+E` and driven-verified. He could not find them. ★ **Captioned Tool, not Tool options**, and the two text tools are the proof: `CanvasTool::TextEdit`'s whole state is a draft whose anchor comes from the click, with no font, size or alignment — §5.8 gives all three to the Format tab — so **the two tools whose absence started this have exactly zero options**, and a panel shaped around options would render nothing for them. Three blocks, every line live: **what a press means in this mode** (the identical drag marquees objects in Edit and sweeps text in Read, and no surface had ever said so); **one row per tool family**, capped at seven, each naming its ribbon tab and its chord so the panel teaches the ribbon rather than replacing it; and **what pdfce last worked out**, which is where the text tools' refusal sentences finally fit — they are 47 words aimed at a status row R128 forbids growing, and *"that click was declined and you could not read why"* is very likely the actual defect behind the complaint. Its own dock stack, at the top, in Review and Edit: it is the only panel in any arrangement that gets one, because a tab invisible until clicked cannot fix a discoverability defect — it has the same shape as the defect. Every row is `Action::Command(id)`, a route rather than a second implementation; the identity row reads the **registry's** label and the operator's own keymap, because two copies of one label drift invisibly
- ✅ **★ Dimension groups — a panel as of 2026-08-19, a window before that, and the move is the operator's.** *"The groups editor popup is too long for some screens so can't close it … should come up in the side bar and be scrollable and each section should be able to fold up like the settings one."* A window taller than the screen can push its own title bar — and its only ✕ — off the desktop: not a layout complaint but a **trap**. It also removes the growth loop rather than tuning it (the body was a `ScrollArea`, the window sized itself to its content, and the two chased each other until the Add button was laid out 300 pt below the bottom of its own window — R128 in a second place). Six foldable sections of which **one** starts open, and the rule behind that is *what does an operator need to READ without asking*, not *what do they most often change* — the second question is the trap that made Appearance, the longest section, open by default. Adding a group moved to directly under the list: the window had hoisted it into a reserved footer to make it reachable, which fixed the reach and left the claim wrong, because **adding is an action on the list, not on the selected group**. One command, `measure.manage_groups`, with no second `view.panel_*` id — so the mode taxonomy does the gating for free
- ✅ **Layers** — full `/RBGroups` radio semantics, locked-member handling, Reset to the document's own default
- ✅ **Signatures** — what each signature *covers*, opening with the sentence that pdfce performs no cryptographic verification
- ✅ **Fonts** — inventory, embed status, byte cost, font-folder resolution
- ✅ **Objects** — every object on the page, front-most first; 129,758 on the benchmark drawing
- ✅ **Properties** — read-only facts for a selection
- ✅ **Pages** — thumbnail grid, click to navigate, multi-select (click / Ctrl+click / Shift+extend), context menu of the six page verbs. Selection is marked by a *shape* change plus a written count, never colour alone
- ✅ **★ Drag a page to where it goes, and see where that is before you let go** — 2026-08-18. The tile sensed `Sense::click()` and nothing else, so reordering was two ribbon arrows that move one place at a time and putting page 40 in front of page 3 was thirty-seven presses. An **insertion caret** follows the nearest tile boundary during the drag, **dimmed** over a boundary that would change nothing — hiding it there cannot be told apart from the panel having stopped tracking the pointer, and that boundary is where *every* drag begins, because a block starts out hovering over itself. The header says the same thing in a page number, in the Insert-from-file dialog's own vocabulary. The arithmetic is **gap-indexed**, not page-indexed: there are `n + 1` landings in `n` pages and numbering them by the page they precede leaves *after the last sheet* unnameable, which is the commonest drag on a drawing set. A scattered selection dropped at its own first page **is** a real edit — gathering sheets together is the most useful thing a drag does — so the no-op predicate tests contiguity rather than the block's endpoints. A drop that lands nowhere raises **no action**: `reorder_pages` would accept an identity permutation and record an undo entry for it
- ✅ **Comments** — every annotation in the document, page order then `/Annots` order, reusing `pdfce-cli list-annotations`' ordering by name. `/Widget` excluded because Forms owns it, `/Popup` excluded as an implementation detail of another annotation, and **ce dimensions deliberately *not* excluded** — they are `/Line` annotations, and filtering that subtype would also hide a real `/Line` markup. Every exclusion is disclosed **in numbers**, so nothing is silently omitted; the old shell stated the rule only on the empty case. Reads the session, so unsaved edits show. A markup with no note says so as a fact about the document, not as an error — pdfce cannot write `/Contents` on geometric markup yet, which is filed and accepted
- ✅ **Forms** — fill, **in all three modes including Read**. Reading the session, so unsaved edits show. Read was the operator's call on 2026-08-14 — *Acrobat Reader fills forms in its default view, and replacing it is the stated goal* — and it cost the taxonomy amendment plus a tab move: a command lives on exactly one tab and Read is shown File and View alone, so `edit.form_fill` became `view.panel_forms` in View ▸ Panels. Edit ▸ Forms keeps create, manage and flatten, which is the line the move draws: **filling is not authoring**

### Canvas

- ✅ **Render** — off-thread, generation-counted, cancellable between content-stream operators
- ✅ **Zoom ladder** with a per-page raster ceiling that accounts for `pixels_per_point`
- ✅ **Cursor-anchored Ctrl+wheel zoom** — under 0.01 px drift
- ✅ **Middle-drag pan**, wheel scroll, **hand tool and space-to-pan**
- ✅ **Four page-display modes** — Single · Continuous · Facing · Facing-continuous, as a radio whose active position renders pressed. **Single page is provably unchanged**: its strip is one row with no gap, so the scroll range, centring margin, pan clamp and zoom anchor are the same arithmetic, asserted as an equality against the pre-Phase-4 expression
- ✅ **An undrawn page says so** — its real boundary, a fill that is visibly not paper, and a sentence centred in the part of the page **on screen**
- ✅ **Selection as identity, not position** — page + object + subpath + node, four integers and no coordinate
- ✅ **Selection survives navigation** — zoom, pan, fit, view rotation, page-display mode and tab change, all asserted byte-identical
- ✅ **Multi-select**, marquee, level ladder with Escape ascending one rung
- ✅ **Delete** — click then Delete, verified end to end by `ui-verify` against the real binary
- ✅ **Context menus** — canvas object, canvas empty, Objects row, Pages row, dock tab; a menu with nothing to offer never opens
- ✅ **Read mode does not edit the document** — 2026-08-14, on the operator's ask. Capability is derived from the **mode's tab list in the manifest**, not from the string `"read"`, so the canvas and the ribbon read one sentence: a mode showing the Edit tab can always edit and one hiding it never can, which makes *"a visible control, silently inert"* unrepresentable rather than merely avoided. Read grants pan, zoom, hand, marquee-**zoom**, Find, a right-click offering the three zoom levels, and **form filling** (unchanged — `canvas::forms` still reads no mode). It refuses object selection, move, resize grips, marquee-select, Delete and markup placement. Review refuses content and keeps markup and dimensions, which is the row that proves the gate is per-capability rather than one on/off. Three things the gesture gate could not fix on its own, each closed separately: a **click is not a drag**, so gating presses alone would have left the commonest gesture on the canvas ungated; an **armed tool outlives a mode switch** (it lives in `egui::Memory`, app-scoped) and left a crosshair promising a gesture already refused; and a **selection outlives it too**, leaving eight resize handles on a page in Read. Entering a mode now retires a tool it forbids, clears a content selection, and cancels a gesture in flight — a selection is not *work* under rule 1, and the undo stack is untouched
- ✅ **Find** — `Ctrl+F` or the status-bar toggle. Case, whole-word (with a configurable word rule, because ISO 32000-1 declines to define "word"), and **wildcards off by default**: `find_text` enables them, which is why typing `?` into the old shell's Find bar matched every character on the page. Exactly one `TextSearchOptions` is constructed in the whole crate
- ✅ **Find never searches on a keystroke** — measured at 331–449 ms per search on the benchmark sheet, because the engine re-extracts the document's text every call. Enter, the step buttons, or an option change after a search has run
- ✅ **Stale hits are cleared and said** — an edit clears the highlights, keeps the query, and the readout reads *Document changed*. A quad recorded before a delete can cover different glyphs afterwards
- ✅ **Move** — drag a selection; live ghost preview with no re-raster; multi-select moves as **one** command
- ✅ **Subpath and node moves** — Part rung → `move_subpath`, Node rung → `move_node`; a text run declines rather than borrowing the Object rung's verb
- ✅ **★★ Several anchors move together — 2026-08-19.** `pdfce`'s `gui` column ticked this `[x]` and their own sweep corrected it: *"objects move together; nodes one at a time"*. It was false in an unusually quiet way — `pick_within` has added a Shift-clicked anchor as its own entry since the Node rung landed, so a multi-node set was **representable from day one**, and `moving::subject` read `entered_object()`, which is the first entry. Four picked, one moved. Nothing failed; both halves' unit tests passed. A capability the data model supports and no consumer reads is the hardest kind of gap to see, so the accessor now has a name and a doc comment. `MoveSubject::Nodes` sits beside `Node` rather than growing a `Vec`, because `EditSession` has both verbs; one stale anchor refuses the **whole** drag rather than moving the three that still resolve
- ✅ **★★ The anchors are VISIBLE — and until 2026-08-19 they were not, at any rung.** `FEATURES.md` recorded it against `view.show_points` and the consequence was not drawn out: an operator could descend two rungs, Shift-click four anchors, and have no surface anywhere saying which four. Squares for on-curve points, hollow circles for control points, tethers between — the idiom Illustrator, Inkscape, Figma and the old shell share. ★ **Scoped to the entered SUBPATH**, and the first version was not: it drew the object's, capped at 400, and driving `SW41177.pdf` printed `canvas-anchors total=4972` — the cap fired, nothing drew, and the feature was blank on precisely the documents it exists for. A cap that suppresses the answer where it is needed is not a performance guard
- ✅ **★★ Bézier handles drag — Phase 1's last row, 2026-08-19, and the engine had it all along.** `EditSession::move_handle` has existed since Pass 30.1 with a `Handle` enum, a planner, a `v`/`y` re-spelling path and a disclosure contract; what was missing was a way to see a handle and a way to grab one. ★ **The priority rule, and it is the same rule twice in one day**: a handle sits *inside* the selection's box, so `grip_at` answers `Grip::Move` for every press on one — and this morning the corner **anchors** were undraggable because the corner resize grips covered them. One rule covers both: *the most specific thing under the pointer wins, and specificity is depth down the selection ladder.* The eight scale grips are now the Object rung's alone, painted and hit-tested by one predicate. ★ **The disclosure is invisible by construction and is forwarded anyway**: `move_handle` answers with sentences that are empty unless a `v`/`y` had to be re-spelled as `c` — Table 59 gives a cubic three spellings and two omit a control point — and **the curve draws identically**. What changes is that the original bytes are gone and dragging back does not restore them, which is rule 4's surviving half exactly. Deliberately absent: turning a line into a curve (the engine refuses it by name, so no handle is drawn there and the refusal is unreachable from the pointer), and smooth/symmetric constraints (a modelling convention PDF has no notion of, which would make one gesture two undo entries)
- ✅ **Escape cancels a drag in flight** without committing, and never both cancels and ascends a rung
- ✅ **Eight handles — and they commit** as of 2026-08-19, through `move_nodes` with the grip's opposite corner as the pivot. Cursors correct, drag consumed so it cannot fall through to a marquee
- ✅ **Dock-tab context menu** — `egui-shell` hands the tab's `Response` to the application; the built-in Close survives for consumers that do not opt in

### Verification

- ✅ **`ui-verify`** — drives the real binary through the OS, asserts on the diagnostic trace **and** the pixels
- ✅ **42 checks**, and the founding three still **fail against the old binary** and pass against this one — which is the property that makes the harness a measurement rather than a ritual. ★ Three were rewritten on 2026-08-19 and **none of the three has been run**: the Dimension-groups check now drives a *panel* and presses fold headings, and `markup_shapes` gained a phase asserting one field — `kind=Cloud` on `markup-commit` — because a build whose revision-cloud control armed `Polygon` would pass every other assertion in it, author a legal annotation, render it, and give the operator a cloud with no scallop
- ✅ **Eight CI gates, five of them self-testing** — ui-strings, theme-colours, strong-text, shipped-assets and string-gaps each carry a self-test that plants its own violations and proves the gate catches them; file size and shell purity do not need one. With `fmt` and `clippy` the runner reports **14 checks, 0 skipped**. The theme gate was **missing entirely** until 2026-08-13 and found five unmarked colours on its first run; `check-string-gaps` was added after a line continuation lost its backslash and shipped a ten-space hole into an operator-visible sentence, and it fired twice more the day it landed
- ✅ **`PDFCE_DIAG`** — canvas layout, pointer in three coordinate spaces, object counts, named UI rects

---

## Planned, in order

### Next — small, unblocked

| | |
|---|---|
| ✅ | **Chords are gated on the active mode** — 2026-08-14, operator decision. `app::keyboard` dispatched by id and consulted neither the ribbon nor `Capabilities`, so Read hid the Edit tab and `Ctrl+E` still reached `edit.text`. The rule is *"a chord may reach a command the active mode **shows**, or a command that lives on **no ordinary tab at all**"*, and the second clause is what makes an exception list unnecessary: **undo and redo were already on no tab** — they sit on the QAT, which every mode draws — so they keep working in Read for free. `edit.find`, `view.read_mode`, `view.fullscreen` and the three `mode.*` commands are tab-less for the same reason. A **contextual** tab counts as no tab: Format is governed by `visible_when: selection.any`, and in a mode that cannot select it never appears, so gating it again would be two rules for one thing. `read_mode_refuses_exactly_these_bound_chords` pins the whole consequence as an exact set, so moving a command between tabs fails loudly |
| ✅ | **The text-copy commands moved to File ▸ Export** — 2026-08-14, operator decision, closing the taxonomy question the chord gate surfaced. `Ctrl+Shift+C` was bound to `edit.copy_page_text`, which sat on the Edit tab, so Read refused it — and Acrobat Reader copies text, which is the standard Read is measured against. **Copying is not authoring**: it reads the page and writes to the clipboard, changing nothing. So by the same argument that moved `edit.form_fill` to `view.panel_forms`, both verbs left the authoring tab: `edit.copy_page_text` → **`file.copy_page_text`** (token 420 → 122) and `edit.copy_document_text` → **`file.copy_document_text`** (421 → 123), with the chord following the command in the manifest keymap, which is the only place a chord is bound to a meaning. They are in **Export** rather than a Clipboard band of their own because an export is *content written out to somewhere that is not this document* — DXF to a file, form data to a file, text to the clipboard — and a lone Clipboard band would have implied the object clipboard, which is still **N**. **Edit ▸ Clipboard was deleted, not emptied**: those two were its only members and a captioned band with nothing under it is the placeholder P3 forbids, so the group count went 32 → 31 across six documented sites. The old tokens stay gaps, as `edit.form_fill`'s 430 already does. `both_text_copy_commands_are_offered_by_every_mode` asserts the restored property directly — the document-text command has no chord, so nothing else stands under it |
| ✅ | **Read selects text, and copies it** — 2026-08-14, closing the last gap in the operator's original ask. Drag sweeps a range, double-click a word, triple-click a line, Shift+click extends from the anchor, Escape clears, Ctrl+A takes the page, Ctrl+C copies. **It needed no new capability**, and that is the design: selecting text authors nothing, so it belongs beside Find in `capability`'s not-gated list — a `select_text` flag would restate the operator's own *copying is not authoring* ruling somewhere free to disagree with it. What it needed was the **primary button**, which in a content mode is already the marquee, so the rule is one predicate: *a press means text when the select tool is active and the mode cannot select content.* Read ✓, Review ✓, Edit ✗ — and the two meanings are mutually exclusive **by construction**, both branches reading the same flag, asserted as an exclusive-or over every capability combination |
| ✅ | **Panel toggles** — 2026-08-14, operator decision: pressing an open panel's control **closes** it. `view.panel_*` and `file.fonts` had no dispatch arm at all and now have one, through `Panel::from_command_id` — the single id↔panel binding, so there is no second table to drift. Three states, and the **middle one is the trap**: a panel behind a sibling tab is *not* on screen, so its control **raises** it rather than closing the thing the operator just asked to see; `DockState::is_on_screen` is the dock's own predicate for exactly that distinction. Closing goes through `DockLayout::close`, the same path the dock's own tab Close takes. ★ **`file.properties` and `markup.comments` are deliberately NOT toggles** — Properties is offered by the Objects row context menu to describe the row just clicked, and a second invocation that closed the description would be hostile. The first draft toggled everything and an existing test caught it, having argued the point in advance. The rule is about the **control**, not the panel: *is this panel open?* toggles, *tell me about this thing* shows |
| ✅ | **Edit-disclosure surface** — the move and delete verbs return operator-facing disclosures when the surgery changed an operator's *form* (an `re` rectangle expanded into explicit segments, an implicit `m` materialised). They were traced and nothing more, which `vector_edit`'s own header named as the outstanding half: *"tracing is not surfacing"*. Now one line in the status bar's left half, in every mode, keyed on the **edit epoch** — so an undo retires it with nothing having to remember to clear it. Core's sentences pass through **verbatim**; the catalog contributes only the mark, the lead-in and the separator. Shares one body with the form-fill disclosure, which is the R128 defence: bounded width, fixed row, elide-not-wrap, hover for the rest — four rules that only work together, now written once. The height test asserts a measurement *happened* (`Some(true)`, not `None`) **and** counts painted shapes, because a height comparison alone is satisfied by a function that drew nothing |
| ✅ | **★ Insert an image — 2026-08-19, and its scaffold entry was one of only three with NO recorded reason at all.** `edit.insert_image` carried a `★ P3` mark and the note *"No recorded reason for the missing arm"*, while `EditSession::add_image` had shipped the whole time. Pick a picture, and a window states what it is — format, pixel size **as displayed** (an EXIF-rotated photograph is transposed by the importer, and the stored shape is not on screen anywhere), and its natural size on paper together with whether the resolution behind that number is one the **file declared** or one **pdfce assumed**. Place it by a rectangle in millimetres, with the landing previewed from `NewImage::placed_rect()` — the engine's own function, public for exactly this. ★ **Placement is numeric rather than a drag, and that is three reasons rather than a shortcut**: this operator's pictures go into title blocks at positions somebody decided; a placed image is page **content**, carries no `/Rect`, and cannot be moved afterwards, so a one-shot freehand placement whose only correction is undo is the worse offer; and a drag needs a new `CanvasTool`, a `DragKind` and a `press_kind` arm — the machinery this project's own RAG names as the least safe thing to change without driving, which R1 forbids shipping. **A drag is a second ROUTE to the same action**, and `Action::InsertImage` already carries everything it would produce. Refusals are the engine's own words, because an `ImageImportError` names the operator's file — *"pdfce does not place GIF images"* — and a catalog sentence would discard the half that is the answer. A box off the sheet is **refused, not clamped**; an overhang is not refused, because bleeding past the crop box is deliberate |
| ✅ | **★ And the resolution IS previewed — filed 2026-08-19 morning, shipped the same day.** This row read *"not previewed … computing it here would be a second derivation"*, which was true for about four hours. `NewImage::effective_dpi()` and `below_screen_resolution()` are pure now, and the half that mattered is that **`add_image` calls them rather than repeating the formula** — the engine deleted its own copy and pinned the equality with a test it sabotaged to prove. ★ **The four-liner this shell nearly wrote would have been wrong**: under `Contain` the placed rectangle is the *letterboxed* sub-rectangle, not the box typed into the spinner, so measuring `rect` reports a figure low by exactly the letterbox ratio. In the engine's words, *"the pure method is not saving you four lines, it is saving you the letterbox"* — which is the strongest argument for asking rather than working around that this project has on record |
| ✅ | **★ The keyboard reference, derived from the keymap that dispatches — 2026-08-19.** `file.shortcuts` was registered, drawn on File ▸ pdfce and inert, and its scaffold entry did not merely say *blocked*: it carried the design, from `SALVAGE.md` — *"Fix `shortcuts_reference()` — it **omits six live bindings** (DEFECTS.md D5) — and derive it from the keyboard map so it cannot drift again. Salvaging it unfixed would import D5."* So nothing was salvaged and **the window holds no list**. Every row is a fold over the same `Keymap` `app::keyboard` resolves a keystroke against, so a binding that exists is listed by construction and a wrong listing is not a thing this window can produce. **D5 is not fixed; it is made unrepresentable** — the same move `canvas::snap`'s tolerance and `dialogs::scale`'s unit list each made after their own copies drifted. A chord naming an **unregistered** command is dropped (R8: an absent capability is expressed by an absent registration, and listing its key would promise a keystroke that does nothing) and the count of drops is **disclosed**, because a stripped build genuinely has fewer shortcuts and an operator comparing two installations needs to know which. Two chords on one command are one row naming both — `edit.redo` really is bound twice, and showing one would be incomplete in exactly D5's way. `text::shortcuts` has a rule of its own, asserted by a test: **a string there may describe the reference; it may not be part of it** |
| ✅ | **★ The measure tools say what they are about to pick, before the first click — 2026-08-19.** His #2: *"the measuring tools don't give me any indication of what is being selected … hover over a line or node."* Both, confirmed with him: the **entity** under the pointer and the **node** it will snap to. The interesting half is that the snap indicator had never worked *before the first click* — the pick state machine had nothing to snap relative to until a point existed, so the one moment an operator most needs to know what they are aiming at was the one moment nothing was drawn |
| ✅ | **★ The I-beam is legible on white — 2026-08-19.** His #1, and the same fix the crosshair had already been given: two-tone, so it survives both a white sheet and a dark object under it. A cursor drawn in one colour is a cursor that disappears against half the documents this operator opens |
| ⬜ | **`pages.merge_into`** — unblocked 2026-08-19 by `EditSession::merge_document`: one undo entry, session intact, fields arriving fillable, collisions **renamed** rather than refused. Two things to surface when it is wired — `fields_renamed > 0` breaks any script, FDF or calculation keyed on the old name and the operator has no other way to learn it happened; and outlines, named destinations, page labels and `/OCProperties` are **not** carried yet |
| ⬜ | **Scoped reset chooser** — reset currently applies `All`; the scoped variants need three commands and a split-button item kind |

### ★★★ The selector was invented, and that was the real defect — 2026-08-19

The operator, on week two:

> *"How do I select and edit end points on the canvas? How do I edit text when
> on the canvas? I get a box and the I cursor, but I can't type anything. How do
> I make new text when I click on the canvas and expect to edit there? Same
> problem. … Even though the old GUI was wonky and buggy it at least had these
> features somewhat operational within the first week. … **The selector should
> be predictable like other programs. It seems a lot of ideas are getting
> invented instead of just using the LLM weighting that would have produced the
> most common method expected.**"*

He is right on every count, and the last sentence is the finding of the project
so far. **Both features already worked. Reaching them was invented.**

| to do this | the ritual, before 2026-08-19 |
|---|---|
| type one character | Edit mode → the Edit tab → *Edit text* → click the run. **Four steps.** |
| move an end point | click the shape → double-click to descend to a subpath → double-click again to descend to a node — with **nothing drawn at any stage** saying a deeper rung existed |

The I-beam he describes was the text tool **sweeping** text for copying; the
tool that types was a different tool on a different tab. Neither ritual is
discoverable and neither resembles any program he has used.

**The fix is the one every editor settled on decades ago: the tool is the rung.**

| | | |
|---|---|---|
| ✅ | **V — Select** | click a shape, drag to move, drag paper to marquee |
| ✅ | **A — Points** | click a shape and **every point appears at once**; click one, Shift-click for more, drag to move; a point on a curve shows its Bézier handles |
| ✅ | **T — Text** | click text to edit it, click empty space to start new text; drag still sweeps for copying |
| ✅ | **H — Hand** | pan |

Bare letters, because that is the layout Illustrator, Photoshop, InDesign,
Figma, Affinity and Inkscape converged on, and this shell binds no bare letter
to anything else. Safe because `canvas::keys` gates every keystroke on
`text_edit_focused()` — `DEFECTS.md` D1's guard, the one place this project has
already been burned and fixed.

★ `SelectionState::click_direct` is the new entry point and is deliberately
**not** a flag inside `click`: the two branch differently at every decision.
The single line doing most of the work sets the **Part** rung on a click that
names a subpath, because `draw_anchors` draws from that rung up — so entering it
*is* showing them. ★ `textedit::click` was `resolve_run(click)?`, a bare `?`, so
a click on blank paper refused and did nothing; `NoRun` now becomes an origin
and one tool does both. Only `NoRun` falls through — an encrypted document is
still reported, because that says *this cannot be done here* rather than *there
is nothing here*.

★★ **Two driven checks assert the COUNT**, which is the whole feature: one key,
one click. A check that armed the tool from the ribbon and then clicked would
pass on the old build too — the old build could do all of that, it just took
four steps to get there.

★ **Drag-and-drop**, reported in the same breath, did nothing at all — nothing
read `dropped_files`. A PDF now opens, an image inserts through the same
placement window the ribbon opens, and every refusal says what to do instead.
**It made a working Insert-image button look broken**, because both were tried
in the same minute and only one of them said anything: a silent failure does not
just hide its own feature, it takes the credibility of everything tried beside
it.

### Phase 1 remainder — making selection mean everything it should

| | |
|---|---|
| ✅ | **★★ Resize — 2026-08-19, and the blocker was a category error.** This row read *"`EditSession` has the whole `move_*` family and no scale verb at all"*, which is true and was never the question. **`move_nodes` takes a list of per-node deltas, and a scale IS a list of per-node deltas** — `d = (p − pivot) × (s − 1)`, computed once per anchor from the grip's opposite corner. The engine has had the whole capability since the day it shipped the verb; what was missing was arithmetic in this shell. ★ **Three defects, all found by DRIVING it and none reachable by a test.** (1) The drag became a **marquee**: `hovered_grip` read the *current* pointer, and by the frame `egui` calls a press a drag the pointer is ~20 pt from an 8 pt grip — it now reads `press_origin`, which is what the operator aimed at. (2) It scaled about the **dragged** corner, because `Grip::anchor` is where the handle is drawn and a pivot is the opposite one; `Grip::pivot` is now its own method with a test relating the two. (3) Commit returned `NoObjectModel` — `Resize` was missing from `needs_targets`, so the gesture ran on a canvas that had never been asked for a target provider. ★ **Six refusals, each a sentence rather than a silence** — nothing selected, more than one object, not a path, no object model, a degenerate box, no nodes — on the same status row the text tools use, because *"that grip was declined and you could not read why"* is the defect shape this project keeps finding. **Text runs and annotations decline**: scaling a run by moving its nodes would move the glyph origins and leave the type size untouched, which is a shear, not a resize. **Line weights do not scale and it says so**, once, off-canvas — `/LW` is not a node |
| 🔨 | **Object clipboard — cut, copy and paste land 2026-08-19**, scoped to **markup and comments**, on the operator's report *"also the standard copy/paste and I didn't try cut so possibly that one too aren't implemented"*. ★ The scope is what the engine can express, measured rather than assumed: `annot_author::spec_from_dict` reads a markup out and `add_markup` writes one back, so the round trip is complete for the things this operator actually copies between sheets — clouds, notes, stamps, callouts. `Ctrl+X`/`Ctrl+C`/`Ctrl+V`, and `Ctrl+C` over swept **text** still copies text because `textsel` reads the key before the dispatcher sees it. A same-page paste offsets 10 pt so the copy is visible; a **cross-page** paste lands in place, because a cloud copied to sheet 12 belongs where it was on sheet 1. The spec translation is an **exhaustive match**, so a tenth `MarkupSpec` in the engine fails to *compile* here — a paste that silently stopped offsetting for one annotation kind would be a defect nobody reports. Edit ▸ Clipboard is back (32 groups); its 2026-08-14 deletion note ended *"the next author of an object clipboard needs the word — which is right here"*, and that is literally what happened. **`paste_in_place` stays absent and is now argued rather than pending**: it would differ from `Ctrl+V` only on the one page an operator would never want it on. ⛔ **Page content still cannot be pasted**, and it was re-measured on 2026-08-19 rather than inherited: `grep "pub fn"` over `edit.rs` returns 157 verbs and **none of them inserts content** — no `paste`, no `duplicate`, no `insert_object`, no `add_path`. `insert_pages` inserts whole pages, which is a different subject. Copy is expressible (the decomposition can be read), so what is missing is exactly the half that writes. Filed as a dated citation rather than a verdict, per `NO_SURFACE.md` §1c |
| ✅ | **Restyling a placed markup — 2026-08-19.** Colour, line width and opacity, through `EditSession::set_markup_style`, on a **selected annotation**. ★ It landed in the **Properties panel** rather than the Format tab, and that is a considered placement rather than a shortcut: Properties is already the surface that appears when something is selected and already reads the annotation, so a second surface with the same trigger and the same subject would be two controls for one job. `MarkupStyle`'s fields are all `Option`, so **only what the operator touched is sent** and everything else is left alone — which matters because `StyleEdit::Clear` and "unchanged" are different edits and a struct of plain values cannot tell them apart. ★ **The reader's view of an annotation is not the author's** and this is where that bites: `annot::Annotation` carries `constant_alpha` but neither `/C` nor `/BS /W`, so the current colour and width are read back through `annot_author::spec_from_dict` — the author's view — and the opacity off the dict. CMYK stroke colours read back as **no swatch** rather than as a converted approximation, because showing a converted colour in a picker invites the operator to press Set and silently rewrite an ink they never touched |
| ✅ | **★ Editable geometry — 2026-08-19**, and it landed the day after the grips out of the same machinery: a position change is `Action::MoveSelection` and a size change is `resizing::action`, so this surface computes two factors and a delta and contributes **no geometry of its own**. A properties panel that reimplemented *"make it 40 points wide"* would be a second scale with a second pivot convention and a second answer about line weights, and the two would drift silently. ★ **Why it is needed even though the grips work**: a grip cannot express *exactly 40.0*, and a drawing is full of *"the same as the one above it"*. It is also the only route for a **small** object — at fit-page zoom on an A1 sheet a 6 mm symbol is four pixels across and its eight grips overlap each other — and the only route that does not require a sub-pixel drag, which `MODES_AND_PANELS.md` §7 asks for by name. ★★ **An Apply button, not live commits**: a scrubbed spinner going 40 → 120 would raise eighty undo entries, which is `MoveNodes`' own argument about looping the singular verbs wearing a nicer face. The draft is stamped `(page, object, **edit epoch**)` and the epoch is the member that is easy to leave out — without it, *type W=40, Ctrl+Z something unrelated, press Apply* divides 40 by a width that no longer exists and the object ends a third size the operator never typed. Bounds come from the object's **anchors**, the same set `move_nodes` moves, so the number shown and the number acted on cannot disagree; a flat object scales only the axis it has, because `h_new / 0` is how a NaN reaches a content stream |
| ⬜ | **Undo/redo** — the action funnel exists precisely so this is possible; the command log is not yet surfaced |

### Phase 3 — viewer conventions

| | |
|---|---|
| ✅ | **Hand tool**, space-to-pan — Space is read by the canvas itself, so it needs no keymap entry and cannot be unbound by accident |
| ✅ | **Cursor-anchored discrete zoom** — the rule was decided once, in `canvas::zoom::anchor_point`, and **all five** paths route through it: wheel, the three commands, and the framing verbs. Armed from one statement in the frame, not from the six sites that raise the actions |
| ✅ | **Zoom to selection** — gated on `selection.bounds`, which is *not* `selection.any`: an identity can outlive the box it described, and framing nothing is a jump to the origin that looks like a bug |
| ✅ | **Marquee zoom to region** — one rubber band shared with marquee-select, branched only at release; a zoom marquee never touches the selection and decomposes nothing |
| ✅ | **Recent files** — `recent.txt` beside `layout.ron`, capped at 10, move-to-front; missing entries dropped at display time only, throttled so a dead network path cannot block the UI thread |
| ✅ | **Rulers** — in **points**, or in the document's own unit at its own scale when its dimension sidecar carries one, through the *same* `pdfce-core` `format_measurement` a dimension label uses, so a ruler and a dimension across one span agree to the digit. The gutters take a **constant** bite out of the viewport (R128): switching them on costs exactly one re-fit, and the trace shows it. Zero is the current page's own top-left, which is the frame the pointer readout already reports |
| ✅ | **Grid** — **per page, in page space**, clipped to the sheet, so it scrolls with the drawing and the row gaps between sheets carry none. Every numbered ruler tick has a grid line under it, because both come from one 1-2-5 ladder. Its pitch is bounded on the **drawn** step, which a measurement of the running binary corrected: bounding the labelled step put a line every 1.4 screen pixels on the benchmark A3 sheet, and neither a screenshot nor the suite saw it |
| ✅ | **Guides** — belong to a **page**, dragged out of a ruler, moved on the canvas, deleted by dragging off it or by a double-click. A guide's catch band is registered after every page widget, so grabbing one cannot also rubber-band a selection. Persisted per document in `guides.txt` — a fourth store beside `layout.ron`, `recent.txt` and `page-display.txt` — and a document that has remembered guides opens showing them, because the presence of the work is the preference. **Escape abandons a guide drag and abandons exactly that**: it is the fourth claimant on Escape and it sits above an armed region zoom, because a drag following the pointer this frame is the more transient of the two. Nothing is rolled back — a drag holds a *proposed* position and only release raises `SetGuides` |
| ✅ | **A new panel reaches an operator who upgrades** — a layout records which panels *existed* when it was written, so a genuinely new one appears while one closed on purpose stays closed. Verified against a real layout file written before the Pages panel existed |
| ✅ | **Thumbnail grid** — one tile per frame, on-screen only, current page first, 64-texture cap, and a **hard stop past 400 ms** naming the page and its cost. Measured: 918 ms for the benchmark CAD sheet, 238 ms for one sheet of a 36-sheet set against 58–72 ms for its siblings |
| ✅ | **Worded decline** — 2026-08-14. `ZoomOutcome::NoBounds`/`NoCanvas` reach the operator instead of the floor: `app/dispatch.rs` carries the outcome into a new `app/status/decline.rs` and the status bar's left half says so. **Same surface, different store** — the bar, `disclosure_line` and the R128 fixed row are reused; the edit-epoch key is not. A decline changes no document, so the epoch never moves and an epoch-keyed sentence would still read *Nothing to zoom to* forty gestures later; and a decline must be **repeatable**, which an epoch key cannot express because nothing changed between the two presses. The precedent is `page_box`'s clamp note: retired by **the operator's next act** (`decline::retire`, at the dispatcher's one choke point) and additionally filtered while its reason still holds — asked through the *same* predicates that produced it, because `doc.page_texture.is_some()` is a different question by one frame and would have hidden a genuine `NoCanvas`. The wording diverges too — `⊗ Nothing to zoom to`, never `⚑ About your last edit` — because one slot for both speech acts would make a completed gesture and a refused one indistinguishable in the same place, which is worse than the trace-only state it replaced. **The ceiling-clamped region zoom is deliberately NOT worded**: it is a partial grant, the region is still framed centred, and the clamped scale is already stated by the zoom readout on the same frame — so a sentence would word a non-event. `Declined` is a narrower type than `ZoomOutcome` and *cannot represent a grant*, so no later edit can word it by accident. `app/status.rs` split a third time under R2, taking out the **narrator** (`app/status/notes.rs`) — the seam its own header already draws between narration and a fact about the operator's document |
| ✅ | **…and how it is reached was decided rather than left open** — 2026-08-14, under the operator's *match Inkscape/Acrobat/SolidWorks* instruction. **SolidWorks and Acrobat both reach zoom-to-selection by right-click**; only Inkscape binds a key (bare `3`, in its `1`–`6` zoom family). Two of three said menu, so `view.zoom_selection` joined the `canvas.object` context menu — least-destructive first, above Delete. **No chord was invented**: Inkscape's family is *unmodified digits*, this shell's manifest chords are `Ctrl`-modified by construction, and `Ctrl+1/2/3` are the mode selector `MODES_AND_PANELS.md` specifies, so transposing `3` onto `Ctrl+4` would match neither convention nor anyone's muscle memory. **Consequence, accepted deliberately:** a menu on an object implies a selection, so the decline sentence stays reachable only by the race in which bounds evaporate between the frame that drew the enabled control and the frame that applied it. That is the case an operator finds most confusing, so a race-only safety net is the right shape — the sentence is correct to be rare |

### Phase 4 — page display modes

| | |
|---|---|
| ✅ | **Continuous scroll**, and **Read defaults to it**. Single page stays the default everywhere else and is unchanged — the strip lays out one row for it, so its size, scroll range and centring margin are the same arithmetic they were, asserted as an equality rather than as an intention |
| ✅ | **Facing** and **facing-continuous**, cover page alone. Fit and the raster ceiling became per-*row*: a spread fits as a spread, and the ceiling is a minimum over the row's pages because a spread is two pixmaps rather than one |
| ✅ | **A continuous fit does not depend on where you scrolled to** — under a continuous mode the fit is taken over the document's tightest row, per axis; under Single and Facing it is the current row, which the operator chose. `page_index` is *derived* from the scroll in a continuous mode, so fitting the current row made the zoom depend on the scroll and the scroll depend on the zoom: a mixed-size document oscillated between `zoom=1.4773` and `zoom=0.9559` for as long as the wheel moved. `PROJECT_PLAN.md` R128 in a new place |
| ✅ | Per-document persistence of the choice, so a sheet set does not inherit a report's setting. A third store, `page-display.txt`, beside `layout.ron` and `recent.txt` — `recent.rs`'s own header had already refused to become one |
| ✅ | **Only visible pages rasterize**, one at a time, nearest the viewport centre first, bounded by a texel budget. Measured: a scroll across four pages of a 20-page document whose pages cost ~460 ms each spent **four** renders, none cancelled and none evicted |
| ✅ | **An undrawn page says so** — its real boundary, a fill that is visibly not paper, and a sentence naming the page and its state, centred in the part of the page on screen. Both the fill and the placement were corrected from a screenshot of a driven scroll after every test was green |

### Phase 5 — text editing

| | |
|---|---|
| ✅ | **The tool itself** — `CanvasTool::TextEdit(TextEditKind)`, click to place a caret, type, Enter or a click elsewhere commits, Escape abandons, a mode change disarms *and* abandons. Gated on `edit_content`, so Read cannot reach it. It takes a click and no drag, the shape `press_kind` already uses for measure and vertex markup; `textsel::takes_the_press` is untouched because it asks `is_text()`, false for this variant **by construction** rather than by an added exclusion |
| ✅ | **Alignment and rotation fixes** — the two cases D4b called genuinely wrong. One pure `choose(text_matrix, ctm, alignment)` at the single commit site: rotation is rung 1 and outranks alignment, non-left alignment is rung 2, both selecting `FollowerDisposition::Pin` — which the engine has always had *"for a justified / right-aligned tail that must not move"* and which the old shell never once passed, sending `EditOptions::default()` from its only call site. `Pin` is **correct** under rotation rather than merely less wrong: it writes no follower `Tm` at all and its compensating `TJ` acts along the rotated baseline, so this refuses rotation less harshly than `reflow_apply` does, deliberately. **Proved against the bytes**: a new right-aligned/rotated fixture — which existed in neither repository — edited in the real binary, saved as a copy, re-opened **in a second process**, and the untouched line's `412.64 668.00 Tm` found verbatim *in the appended revision only*, since §7.5.6 leaves the original in the base revision and a whole-file scan would pass for every build. Planting `EditOptions::default()` made the check fail; neutralising that assertion made it fail again at the byte scan |
| ✅ | **★★★ TEXT INSIDE A DRAWING BLOCK IS EDITABLE — 2026-08-20, and on a CAD sheet it is 99 % of the text there is.** Click a label, a title-block field or a *pdf dimension* callout, get a caret, retype it. That text lives in a **form XObject** — a block the drawing program placed — and until this evening pdfce could read it and not write it, which is why every one of the operator's *"text editing doesn't work"* reports was correct while every driven check was green: **the checks drove fixtures this repository authored, and this repository authors page-stream text.** Measured on his benchmark drawing: **1,696 show operators of real drawing text inside the block, against 3,007 metadata glyphs in the page's own stream.** His own estimate — *"I need that editing capability as it is 99% of the text I will want to edit"* — is what made the engine escalate `Pass 119.0` ahead of the transform verbs. ★★ **The shell's cost was ONE DELETED LINE, and that is the whole return on a decision made two days earlier.** When the guard was written, the request filed with it said: *"my shell encodes a fact about your surgery's internals. The day form editing lands, my guard silently keeps refusing until I notice and delete it — a workaround that outlives its bug."* So the engine published `TextRun::editability()` and this shell asked pdfce instead of modelling the answer; when the capability landed the predicate started answering `Editable`, a `#[deprecated]` attribute pointed at the single arm to remove, and that was the job. A hand-rolled guard would have gone on refusing 99 % of his text with every internal check green. ★ **One thing added beyond deleting it: the commit now NAMES the buffer it measured.** `EditTarget::Auto` is the engine's default and searches the page stream first — and that stream holds 3,007 single-character show operators, so offering a form-relative byte pin to it is a dense field of near-misses whose worst outcome is an edit that succeeds on the wrong glyph with no error anywhere. `plan` takes the target from the same provenance record it takes the pin from, because the two fields are one fact and reading half of it is the defect this shell shipped in the first place |
| ✅ | **★ Shared content is disclosed, in the engine's own words.** A drawing program may place **one** copy of a title block and paint it on six sheets — ISO 32000-1 §8.10.1 names a CAD system's standard component as the illustration — and **no clause in either edition binds a form to a page**. So editing text inside a shared one changes every sheet it appears on, and pdfce cannot make that untrue: there is exactly one stream holding those glyphs. The status row therefore carries the engine's `"SHARED CONTENT: this text is drawn from shared content that appears in N place(s) on M page(s)"`, verbatim — re-wording it here would be a second account of one fact, free to drift — and it is **absent on the ordinary single-paint case**, because a warning that fires every time is one nobody reads. **Nothing is drawn into the page view**: R8b rule 4 as narrowed by the engine's decision 059, and the operator's own finding that *"the nagging and red flagging in the original GUI made for a lot of extra bugs in the visibility when editing."* Known gap, named rather than implied: the warning arrives **after** the edit, not at the caret. Telling him at the caret means asking the document how many places paint that block — one walk of the whole file, cheap once and not per click on text — so it needs a document-scoped cache that does not exist yet. Undo is one press |
| ✅ | **★★ One four-character edit used to move 1,676 labels, and the fix arrived the same evening.** Not a shell defect and worth the row anyway, because the shell is what would have shipped it. Reflow shifts *the rest of the line* by the advance delta, and the engine's walk ran forward through every absolute `Tm` until a `Td`/`TD`/`T*` boundary — **a CAD stream positions everything with `Tm` and never emits `Td`**, so there was no boundary and one edit slid the rest of the drawing sideways: `followers_repositioned=1676`, **34,059 changed pixels across the whole sheet**, against 42 pixels inside one label after the fix. A build published at 20:09 linked the revision without it and was **retracted within the hour**. Two things came back with it: `followers_repositioned` is now on this shell's diagnostic channel at the engine's request — *"if you show one number from an edit report beyond the disclosures, make it that one"* — and a driven check fails above 64, which is a fact about the build rather than about any document. ★ And the **fixture had to grow a fourth block**: the fix left the other three unable to exhibit the hazard at all, so both falsifying assertions in `proof.rs` went quiet — and **a quiet falsifier is a test that has stopped measuring**, which would have let the two `Pin` assertions beside them pass against a build that pinned nothing |
| ⛔ | **Live re-layout while typing** — blocked on the engine. Measured per keystroke: **0.49 ms** on a 3-line fixture, **102.77 ms** on a SolidWorks sheet, **356 ms** on an A3 benchmark. The arithmetic is not the cost: `plan_edit` already computes `advance_delta` before any write, but it is `pub(crate)`, so every public route performs a full incremental save. **Request: `measure_edit(&Document, &EditRequest) -> Result<f64, _>`, or make `plan_edit` public.** Debouncing was rejected rather than overlooked — a re-layout arriving 150 ms after you stop typing is a second surprise. The ghost text is gone regardless: what is drawn now is a caret and an extent bracket, which promise nothing about widths |
| ⬜ | **Reflow reachability** — three gates, one of which demands save-and-reopen *after* showing a correct-looking preview. Untouched |
| ✅ | **★★ Text on a line shared with other runs — 2026-08-19, and this row WAS the operator's bug.** *"Text editing on canvas still doesn't work"*, reported for the third time. It worked: two driven checks pressed a caret into a run, typed, and asserted `edit-text` — **against fixtures this repository generates**, a 924-byte one-run page and a blank sheet. On his own drawings almost every click was refused, because a CAD title block is a row of short `Tj` runs on one baseline and the guard declined *any* line whose extracted range spanned more than one run. ★ **The refusal was guarding the wrong thing.** Editing one run on a shared line is not multi-run editing — it is one edit whose **neighbours must not reflow**, and `FollowerDisposition::Pin` is exactly that and had been available the whole time. The guard now feeds a new `Reason::SharesTheLine` into the disposition ladder at rung 2, so the neighbours are pinned instead of the edit being declined. Genuine multi-run editing — one gesture spanning four runs — is still one edit per run and still needs an engine request; **that is a much smaller claim than the one this row made for weeks.** ★ **The lesson is about the fixtures, not the guard**: a check that drives a document this project authored tests the shape this project imagined, and the operator's documents are the only ones with the shape that broke. `--doc-point` now aims a driven check at a real drawing |
| ✅ | **★ New text gets a font, a size and a colour — 2026-08-19.** Nothing was blocking it: `AddTextRequest` has carried `face`, `size` and `color` since it shipped and the apply arm overrode none of them. ★ **A tool option, not a Format property**, and the reasoning is the split `canvas::markup::pen` already makes — Format describes *the placed thing*, this describes *what the next thing will look like* — so it lives in the **Tool panel**, which is the panel whose stated subject is the next gesture. All fourteen bundled faces, grouped by family the way Acrobat lists them rather than alphabetically (which interleaves four Helveticas with four Times), 4–144 pt, and a colour picker. **The default is the engine's default** — Helvetica, 12 pt, black, asserted by a test — so the release that added the controls changed nothing for anybody who does not touch them. Black resolves to `NewTextColor::Black` rather than `Rgb(0,0,0)`: one operator and one byte instead of four, for the same ink. Parked in `egui::Memory` beside the armed tool, so a panel reaches it through `ui.ctx()` with no plumbing, and **application-scoped** — 8 pt Courier chosen for a note is still 8 pt Courier in the next document. Deliberately absent: a **donor font** (that is a file picker, a subset and a licensing disclosure — two features in one combo) and **alignment/wrap** (both describe a paragraph, and this gesture places a caret) |

### Phase 1½ — the conventions sweep

**★ Where this came from.** The operator asked *"how can you learn from these
other programs so that you can build the missing parts more effectively?"*, and
the honest diagnosis was that **this was never missing knowledge** — asked
directly, *"what happens when you click inside an unfilled rectangle in a drawing
program?"* comes back right every time. Nothing PROMPTED the question while the
code was being written. `D:/dev/rag/ui-conventions/` is five gesture classes,
each a numbered list carrying where the rule comes from and the failure mode when
it is absent; `tools/gates/check-conventions.sh` makes every registered surface
answer every row **in its own source**. It cannot check behaviour and does not
pretend to — it checks the question was asked, which is the whole of the problem.
Fourteen gaps on its first run.

| | |
|---|---|
| ✅ | **★★ Shift constrains every drag on the canvas, and says so — 2026-08-20.** *The* resize convention, present in every program in the class for thirty years, and absent from all five drags here. A **resize** keeps the shape's proportions, applying the factor the pointer travelled furthest to produce — which for a mid-edge grip falls out as proportional-resize-from-an-edge with **no special case**, because the idle axis sits at exactly 1.0 and can never win. A **move**, a **ce-dimension label**, a **perimeter corner** and a **Bézier handle** lock to an axis. The lock is re-decided every frame rather than sampled at the press, so an operator who starts crooked and commits to vertical mid-drag is followed, and letting go returns to the free path. ★ A handle locks to its **anchor's** axis rather than the press's — a control point's meaning is the tangent it defines, so the line that matters runs through the on-curve point; Illustrator and Inkscape both do this, and it is the one place the four differ. ★★ **Apply and announce are ONE CALL.** The first cut had each site check the modifier, do the arithmetic and announce — and a fifth drag could have constrained silently, which is not a smaller version of the feature but the convention's stated failure mode exactly: *"the operator holds Shift, gets a result they did not expect, and cannot tell whether the modifier did anything."* Driven by a **pair** of drags in one process, because a single locked drag reporting equal factors proves nothing — a build ignoring the key could satisfy it by luck on a diagonal. Not built, and named as decisions: Alt-about-centre, Alt-to-break-symmetry, a 45° lock, and a label held to its standoff rather than a page axis |
| ✅ | **★ A perimeter corner snaps back onto the geometry it came off — 2026-08-20.** The sharpest shape a missing convention can take: the tool that **placed** the vertex snaps, and the drag that moves it did not — so a corner could be put onto a line and never put back. It now uses the same `snap_candidates` query, the same tolerance and the same *"Snap to content"* switch a measure pick uses, through one function, because a drag that snapped by its own rules would honour a different switch from the tool beside it. Alt suspends it. ★ **The snap overrides the grab point**, and the two conventions genuinely conflict here: preserving a three-pixel grab offset would land the corner three pixels off the thing it snapped to — a corner that looks snapped and is not, which is worse than either alternative. The marker is the same glyph at the same size, so an operator who learned that a square means *endpoint* while placing reads the identical square while correcting. ★★ `DimensionVertex` had to join the decomposition list, and **that list has now been the defect three times** — a drag works perfectly without the model, it just never snaps, and a snap that never fires is indistinguishable from one that found nothing |
| ⬜ | Still open from the sweep: no rotate handle (blocked on the engine verb), no right-click to add or remove a perimeter point (both engine verbs exist), a zero-travel release still raises an action in three of four drag paths, an unfilled `/Square` still claims its interior, caret indices are characters rather than grapheme clusters, and there is no selection inside a text draft |

### Phase 6 — markup completeness

| | |
|---|---|
~~**★ The substrate comes first, and it does not exist.** This shell can place no markup at all today: the eight `markup.*` commands are registered and drawn, and every one falls through to `command-unimplemented`.~~ **Written 2026-08-13 and true then. The substrate landed 2026-08-14 and Phase 6 is now materially complete**: eight `MarkupKind`s place — Rectangle, Ellipse, Arrow, Polyline, Polygon, **Revision cloud**, Freehand, Highlight — plus the three text-markup verbs on a selection, plus the Comments panel. What is left is **Style** (note text, opacity, fill, line width), the three text-bearing kinds (text box, sticky, stamp), and restyling a **placed** markup, which is the Format tab.

The paragraph is struck rather than deleted because of what it got right: it named the substrate as the blocker where the rows below each named `pdfce-core`, and it was correct. *The six "engine-ready" rows were blocked on our own substrate, not on pdfce* — which is the same distinction §1c of `NO_SURFACE.md` now makes for every external blocker in this project.

| | |
|---|---|
| ✅ | **Markup tool substrate, and four kinds placing** — Rectangle, Ellipse, Arrow, Highlight. One `CanvasTool::Markup(MarkupKind)` carrying a kind rather than one variant per shape, so a tool that is two kinds at once is unrepresentable and the four ribbon controls behave as a radio with nothing enforcing it. Rubber-band preview, `Action::CommitMarkup`, an apply arm calling `EditSession::add_markup`. **A click with no drag places nothing** — the old shell dropped a 120×60 default box justified as "a placeholder the operator will resize", and there is no resize verb here, so that box could only be corrected by undo. Arrow keeps **raw** endpoints rather than a normalised rect, verified by driving a reversed drag and getting the same two page points in the opposite order — which a normalising implementation cannot produce |
| ✅ | **Drag origins are the button-down point** — `Response::drag_started()` fires only once egui has *decided* an interaction is a drag, by which time `interact_pointer_pos()` reports where the pointer has travelled to. Measured at **94 PDF points** of error on A1 at 0.21× zoom. The fix went into `PointerFrame::press_origin`, not into markup, because **the marquee and the move drag had it too**: a marquee that starts late encloses less than the operator drew round, and a move under-shoots by the same distance. Neither had ever been reported, because both fail by a plausible amount rather than a visible one |
| ✅ | **Polyline, polygon, ink** — 2026-08-14, three gesture families in one substrate. **Ink** is drag-shaped and reuses the existing path; **polyline and polygon are click-shaped** and get a live click and no drag from `press_kind`, in an early return beside the measure tools' — same shape, same reason. Ending follows `measure/circular.rs` exactly: **a double-click and a registered `markup.finish`, through one commit path**, because consistency with the tool that already solved this beats a marginally better third answer. `Geometry::{Band, Vertices, Strokes}` is **one enum on one** `Action::CommitMarkup` rather than three actions, so the CLI-equivalence claim keeps resting on one `spec()`. **Ink simplifies by Ramer–Douglas–Peucker at 0.5 pt, and the tolerance is derived rather than chosen**: RDP bounds deviation by ε, the pen is 2 pt wide, so ε = half the half-width keeps the simplified centreline strictly inside the stroke the raw trail would have drawn — a test asserts the pen and the tolerance stay tied. Measured 240 → 31 points at that tolerance, and `markup-commit … raw= kept=` carries both numbers because a build whose simplification did nothing emits an otherwise identical line |
| ✅ | **…and the ending disagreement is the second worked example of the reference-app rule** — Inkscape and SolidWorks both close a shape by clicking the first vertex; **Acrobat does not, and Acrobat won on applicability rather than head-count**: `/Polygon` closes back to `/Vertices[0]` by §12.5.6.13, so pdfce is in Acrobat's position and a click-the-first-vertex rule would author a duplicate vertex and a zero-length closing segment. SolidWorks' Escape-to-end was refused outright — in this shell Escape means *abandon*, and committing on the key an operator presses to say "no" is the least recoverable reading available |
| ✅ | **Underline, strikeout, squiggly** — 2026-08-14, the direct payoff of the text-selection gesture that unblocked them. **Select text, then press the control**: the selection is the operand, so there is no `CanvasTool` variant, no new gesture and no second text-range resolver — which is the same divergence `textsel` already refused Acrobat's `Alt`+rectangle over. Model taken from Acrobat *Reader*, which leads with select-then-mark; Inkscape and SolidWorks have no vote, neither having PDF text markup. The **wash is the preview** — literally the same boxes — so no second affordance was added, and `TextSelection` grew `page_quads` in the *same* walk that builds the canvas quads rather than inverting the projection at the authoring site, which on a `/Rotate 90` page would mirror the mark about the page centre. Gated on a new `selection.text` condition, published only for a live selection, so the control cannot be enabled while the press would decline. **Reachable in Review only** — see the row below |
| ✅ | **…and Edit reaches them too, since the Text tool landed** — the P3 tension is closed rather than recorded. With `view.tool_text` armed, Edit sweeps text and `selection.text` becomes true, so the three controls enable. **Observed, not argued**: `ui-verify`'s `text_tool_selects_and_marks_in_edit` drives Edit through dead control → arm tool → sweep → the *same* control now invoking → `add-text-markup epoch=1`, and then **retires the tool and proves the sweep stops working**. That last phase is the falsifier — every phase above it would also pass against a build that had simply deleted `takes_the_press`'s `!caps.edit_content` clause, which would have silently replaced the only content-selection gesture the product has |
| ✅ | **★ Revision clouds — 2026-08-19, and the blocker had been gone for days.** This row said `/BE` appeared nowhere in `pdfce-core` and that all three routes were closed. `MarkupSpec::Cloud` had shipped — vertices, border, interior, width, intensity, with a matching error variant beside it — and **nothing in this shell noticed**, while the operator asked for the tool three times. The polygon shipped first rather than the rectangle, and the reason is the gesture: `MarkupKind::Cloud` joins `is_vertex` and **nowhere else** in that impl, `markup::action` takes Polygon and Cloud through **one arm** with one three-vertex floor, and the whole difference lives in `spec` as `/BE << /S /C /I 1.0 >>` — Acrobat's own default cloud, under the standing *make it behave the way the program you are replacing does* tie-breaker. Its glyph is nine outward arcs on a closed loop: nine because an even count is symmetric about both axes and reads as an ornament, and because below nine the scallop flattens at 16 px into `shape-ellipse`. The preview draws the closing segment **straight**, not scalloped — a gesture overlay is the cursor, and approximating a border effect the engine bakes an appearance for would be a second rendering path, which drift |
| ⛔ | **Note text** — markup cannot carry `/Contents`. **Filed and accepted 2026-08-14**, landing as `/Contents` + `/T` + `/M` **together**, on the argument that a note with no author reads as a broken panel rather than an absent field. `/M` is engine-stamped (a caller-supplied date is a caller-supplied lie); `/T` is optional and `None` omits the key — **no placeholder author is invented** |
| ⬜ | Style: **width and fill** *(colour works)* — both engine-ready |
| ✅ | Style: **opacity — built 2026-08-19** in the Properties panel's restyle section, alongside colour and line width; the rest of this row is kept because of what it got wrong. ★ **The blocker was FALSE and this row said so for weeks.** `EditSession::set_markup_style` takes a `MarkupStyle { opacity: Some(StyleEdit::Set(a)) }` and writes `/CA` (`edit.rs:13093`), with `StyleEdit::Clear` removing it and tests for both directions. What is genuinely absent is narrower and is the second finding: `MarkupSpec` carries **no alpha**, so authoring a translucent mark is `add_markup` **then** `set_markup_style` — two verbs, two undo entries, and an operator who presses Ctrl+Z once gets an **opaque** mark rather than no mark. Filed as a boundary observation, not as a blocker. The restyle half is a **shell** gap: this build has no Format-tab surface calling that verb at all. The original question did uncover a live viewer defect — see **`DEFECTS.md` D9** |

### Phase 7 — measure completeness

**★ The substrate landed 2026-08-14.** `measure_tool.rs` came across whole —
2,044 lines split into `canvas/measure/{pick,scale,state}.rs`, all 36 tests
carried, and **no engine API had moved**. The 12.M1 snap primitives came with
it as `canvas/snap.rs`. `CanvasTool` grew a `Measure(MeasureKind)` variant on
the same one-variant-carrying-a-kind argument markup settled.

| | |
|---|---|
| ✅ | **Linear ce dimension** — click A, click B, click where it sits. The third click **is** the commit: the old shell's Accept/Reject property bar is deliberately not carried across, because decision 024 retired *"a separate accept / reject box somewhere on the screen"* at the operator's instruction and `MODES_AND_PANELS.md` makes application-initiated floating default to **Never**. Three clicks is also what SolidWorks does, which is what *"make it work the way other programs do"* asks for; the corrective for a mis-placed dimension is undo, as it is for a markup. A live preview draws the constrained A→pointer segment while measuring and, once placing, the dimension itself through the **same** `dimension_preview_segments` a committed one is drawn from — so what you see is what commits |
| ✅ | **Two-line ce dimension** — pick two lines already on the drawing; `pick_line_in_page` resolves each click against the page's own geometry, so the shell and `pdfce-cli dimension-add` resolve the same click to the same line. Registered as `measure.two_line`, moving out of `PLANNED`. **Corrected 2026-08-14:** this row used to read *"the canvas gesture has no caller"*, false in five documents at once — the caller that was missing was **ours**, and it now exists |
| ✅ | **Escape is two rungs, not one** — a measure pick is a sequence of *clicks*, so there is no drag for claimant 1 to cancel, yet a dimension with point A taken and B not is unmistakably in flight. One Escape corrects a mis-aimed pick; a second puts the tool down. Without the split, one press would do both — decision 025's L1 violated in a new place |
| ✅ | **Radius / diameter** — armed 2026-08-14, and it is the one measure gesture with **two endings** rather than none. A click toggles an object into the fit set (`CircularPick`, the tool's own pick set — never `SelectionState`, which `CircularPick`'s docs forbid because a half-assembled fit is not a selection and would arm Delete over it); the Taubin best-fit re-runs live; and the operator says when the set is complete by **double-clicking on the canvas** or pressing **Measure ▸ Finish** (`measure.finish`). Both go through **one** commit path (`canvas::measure::circular::commit`) — two arms that each assembled a `DimensionKind` would agree the day they were written and diverge after, invisibly, because a circle fitted from the same points looks the same whichever code drew it. Neither ending is a floating accept box, so decision 024 stands. Finish is enabled on `measure.finishable` — the circular tool armed **and** a non-degenerate fit — because a Finish that is always live is a control that does nothing on almost every press. The picked objects are outlined and the fitted circle is previewed through `dimension_preview_segments`, the same function a committed dimension is drawn from; without both the tool has no feedback at all, since its output is an inference and its picks land on geometry already on the page |
| ✅ | **The measure preview draws in the right place** — found while arming the circle preview and fixed with it. `measure::preview` converted PDF user space through `viewer::pdf_space_to_canvas` and handed the result straight to `ui.painter()`. That lands in **canvas** space (page top-left origin, no zoom) and the painter speaks **screen**, so the snap indicator and the linear preview were drawn offset by wherever the page sat in the window and at 100 % whatever the magnification — invisible at zoom 1 with the page at the window origin, wrong everywhere else. `measure::page_to_screen` now does both hops, the second through `PageMapping::to_screen`, which is the same outward crossing `canvas::overlay` has always used. `HANDOFF.md` defect 4's shape exactly, and no unit test could see it: the picking was always right, only the drawing was wrong |
| ✅ | **Set scale** — the dialog shipped, and with it the round trip this row was waiting for: `measure.set_scale` opens it, *Measure it on the drawing* closes the window and arms `MeasureKind::Scale`, the pick completes, and the window comes back with the measured length in it. Two edges read once per frame, in that order, because neither is a command — one is a button inside a dialog and the other is the canvas noticing its own state machine finished. `ScalePick` reuses `LinearPick` **verbatim**, since a reference line is a linear pick that happens not to be authored |
| ✅ | **Snapping** — the query is wired, so a pick lands on the geometry rather than near it. On a CAD sheet that is the difference between a dimension that measures a line and one that measures *close to* a line, which is worse than none because it is wrong by an amount nobody can see. `snap_candidates` is the **engine's** query, not a second one here; `PageMapping::snap_tolerance` sits beside its selection sibling so there is still exactly one place in `canvas/` that divides by zoom. **The indicator is drawn before the first pick**, which is the point of it — gating it on a gesture already in progress would show it everywhere except where it is needed. **One `Resolved` per frame** feeds both the marker and the click, so the point aimed at and the point committed are the same value rather than two derivations that agree until they don't. Alt refuses a snap for one pick, which is what makes the generous 10 px catch affordable. **Tab cycles candidates**, guarded on the armed tool rather than a mode, so focus navigation is untouched everywhere else |
| ✅ | **A derived candidate is confirmed, never assumed** — an inferred centerline takes two clicks: the first promotes, the second commits. The call site passed a constant `is_derived: false` until the query existed, which silently turned an inference into a commitment — rule 4's fuzzy-never-sneaky line, crossed by a placeholder argument |
| ✅ | **★ The snap indicator has its colour — 2026-08-19, and the absence had no symptom.** This row read *"`Overlays` is never installed by `pdfce-gui` … `snap_indicator_tint` returns `None` on every frame"*, and it was true for a whole phase: the marker drew, in the selection colour, and the cue distinguishing *pdfce proposing a point* from *a committed thing being selected* simply was not there. A missing role returns `None`, the caller falls back, and **nothing looks broken**. `canvas::overlays` now publishes the set beside `Theme::apply` — `preview` → `palette.notice` (*"something is worth knowing and nothing is broken"*, which is what a snap marker is), `dimension_selected` → `palette.accent` (what every other selection in this application is drawn in). The roles live in `pdfce-gui`, not `egui-shell`: **R7**, because `"dimension_selected"` is a pdfce concept wearing a colour. Both owed tests land, over `Preset::ALL` rather than the default — a preset is exactly where two roles collapse, and the two that are not the default are the two nobody looks at |
| ✅ | **★ Dimension groups are manageable — 2026-08-18, and the bigger half was not the window.** `measure.manage_groups` was registered, drawn on Measure ▸ Scale, listed in `reach`'s `SCAFFOLDED` set and **inert for the whole life of this build**; the operator hit it by name — *"I still can't get to edit dimension groups when I click on it."* Its recorded blocker, *"two of four verbs do not exist"*, had shrunk to two of **six** and was never re-measured. What was missing underneath is sharper than the window: `MeasureState::group` — *"the active authoring group the next dimension joins (ui-spec §2.6 group picker)"* — has existed since the Phase 7 salvage and **nothing in this crate ever wrote to it**, so a second group could be created, carry its own scale, and be joinable by nothing. The window's *Draw into* column is that picker. It also sets the drafting standard, shows or hides the group's layer, and sets the five appearance defaults of the style cascade's middle tier — each with **a count of how many members will visibly MOVE**, computed before the edit from `StyleProvenance::follows_group()` and deliberately **not** `set_group_style`'s return value, which counts every member regenerated including the ones that override the property being changed. `follows_group()` is true for `Factory` as well as `Group`, which is the easy thing to get wrong, and the predicate is the engine's throughout |
| ✅ | **★ A placed ce dimension is editable — the panel `FEATURES.md` has wanted since the style cascade shipped.** Select one and the Properties panel shows its group, what it measured, a radius/diameter switch for a circular one, and **all eleven** properties of the cascade's bottom tier — unit, precision, decimal marker, drafting standard, text height, line width, arrow length, arrow form, colour, **tolerance** and tolerance decimals — each with the override checkbox the operator asked for and a sentence naming which tier supplied the value in force. Nothing here **builds a label**: a limit tolerance *suppresses* the nominal rather than printing beside it and `Basic` prints no text at all, so the editor renders fields and states what each form will do; the measurement comes from `DimensionModel::display`, which is the producer the `/AP` is baked from — including the branch that never applies the group scale to an angular kind. Nothing is clamped or swapped: an inverted limit pair is refused by the engine with its own operator-facing sentence, shown verbatim, and **no action is raised until it validates**. Reachable from the contextual Format tab and the canvas right-click, both through `format.properties`, which raises `Action::Command("file.properties")` — a second route, never a second implementation |
| ✅ | **★ Renamed, deleted, and re-grouped — requested 2026-08-18, shipped by the engine 2026-08-19, wired the same morning.** The window carried a sentence where three controls should have been; it now carries the controls. **Delete asks the orphan question** rather than refusing or guessing: `pdfce-core` refuses a populated group with the **member count in the refusal**, because *"this group is not empty"* and *"this group holds forty dimensions"* prompt different decisions and only a surface can put that question in front of an operator — so pressing Delete on a populated group offers a destination and a button that will succeed. Two facts travel with it, both stated before the press: the members are **re-measured** against where they land, and pdfce **will not delete the dimensions with the group** (doing it inside the verb would be a second implementation of `delete_dimension`'s `/Annots` removal, and looping the existing one would make undo take a press per member and be able to stop halfway). **The group is a picker in the properties panel now**, and ★ `set_dimension_group` is **not a field assignment**: a ce dimension's label is derived from its group, so the verb re-measures and the printed number changes — `70.6 mm` becomes `2.00 m` for the same geometry, measured. That disclosure is the one the engine went out of its way to make sure a shell would not miss, and it is under the picker |
| ✅ | **A group's unit is settable, and it was never missing** — the request implied a gap that was not there. `set_group_scale` takes a whole `NumberFormat` and `NumberFormat.unit` is public, so the unit was reachable and merely **undiscoverable**; the engine declined to add sugar for it on speculation and was right. The window's unit combo goes through that path, carrying the group's own scale through **unchanged** — passing anything else would silently recalibrate a drawing while the operator was changing a unit |
| ⬜ | **Area** and **Angular** — the two a takeoff needs. `DimensionKind::Angular` **exists in the engine**, and the two-line tool already authors one when the picked lines are angled, so what is missing is a *direct* Angular tool (pick an apex and two directions) rather than the capability. The GUI's `MeasureKind` has no variant for it: shell-only work |
| ⬜ | Count tool and a takeoff schedule |

| ✅ | **Two-row ribbon bands** — 2026-08-14, operator feedback: *"your ribbons are only one row. the mockup previews I was shown have two rows and seem less cluttered."* `mockups/ribbon.html` specifies it precisely — `.gcmds {flex-wrap:wrap; max-width:440px}` inside a `.band {min-height:86px}` — and the shipped `render_group` emitted a single `ui.horizontal`, so controls never wrapped. Now `GROUP_ROWS = 2` with a balancing wrap that minimises the *widest* row rather than filling greedily as the mockup does. **Measured at 1,100 px, one variable changed**: Markup overflow 3 groups → **2**, File 3 → **1**, the Shapes group **45 % narrower** (548.7 → 300.5 pt). The row count is a **constant, not a parameter** — per-group or per-tab would make the band content-driven again, which is R128 exactly; a constant makes *"same height on every tab"* true by construction, and that is asserted including for the case where every group is in the overflow menu. Three rows refused: another `control_height + gutter` off the canvas permanently, and a caption three rows below its top row stops reading as that row's label. **Icons untouched** — the mockup had none and was still preferred, so the staging was the request |
| ✅ | **…and the group padding the mockup specifies is now drawn** — controls sat *flush* against their group box and against the separators, measured at an inset of **0.0**. Now 6 pt each side, symmetric, plus 4 pt of band bottom padding so captions no longer sit on the seam with the dock below. **It cost zero width budget**: `plan::GROUP_PADDING` had been budgeting 6 pt/side that the renderer never drew, and its own doc comment said so — the planner was reserving space the renderer spent as an accidental margin. Overflow unchanged on both tabs, verified rather than assumed. ★ **The mockup's 13 px and the build's 6 pt are the same gap**: the mockup separates groups with a zero-width `border-right` (13+0+13 = 26) and the build with a real 14 pt `ui.separator()` (6+14+6 = 26, measured at exactly 26.0). Raising the constant to 13 would have made it 40 pt and cost 70 pt across File's five band groups — enough to push a group out, which is the regression the change existed to avoid |
| ⬜ | **…and the remaining overflow is not a layout problem** — Style and Comments still overflow at 1,100 px and **no row count can fix them**: they are one-item groups (a custom colour swatch; one command), so they have nothing to wrap. That is the Markup-tab taxonomy question — does Finish belong in Shapes, does Ink, is Shapes now two bands — and it is the operator's, not a defect |

### OCR — specified and unblocked by the operator, 2026-08-14

✅ **The licensing question is answered.** Operator, 2026-08-14: *"yes ship
that model in the mit repo with proper credit."* The **mechanism to carry that
credit now exists** — see the attribution row below — and what remains when
OCR lands is one line in `PAYLOAD_ASSET_DIRS`, one section copied from the
engine's `about.hbs`, and one `Attribution` entry. **The gate refuses the
first without the other two**, which is the property that makes "with proper
credit" enforced rather than remembered.
The engine can already recognise text end to end (`ocr::layer` writes the
invisible sandwich at render mode 3, the `ocrs` weights ship at 12,240,008 B)
and **no shell has a surface**. The engine also records that recognition
quality is **unproven**: its only test documents are vector PDFs that already
contain text. Settle both before building.

**The behaviour is now specified, so it does not have to be re-derived when
the blocker lifts:**

| | |
|---|---|
| ✅ | **Find offers OCR when there is no text to find** — the trigger is `readout == Empty` **AND** `!page_has_extractable_text()`, the second behind a short-circuit so it is never evaluated unless a search already came back empty. The falsifying test is the one that matters: `an_ordinary_empty_result_on_a_text_page_offers_nothing`. A build that offered on *any* zero-hit search is one character simpler, passes the positive test perfectly, and would put *"this page has no text on it"* under every mistyped part number |
| ✅ | **OCR in Read mode, saving to a new file** — `file.ocr` prompts for a destination and suggests `<stem>-recognised.pdf`, **never the source**. No mode is consulted and no `Capabilities` flag was added: the rule holds in every mode *by construction*, because OCR's bytes have nowhere to go but a new file |
| ✅ | **…and it lives on File ▸ Recognise, not Tools ▸ Recognise** — `RIBBON_IA.md` §5.7 specified Tools, and Tools is not in Read's tab list, so building to the specification would have broken the instruction. **Third instance of one pattern**, now a rule in §5.7: a command refused where the operator needs it means the *tab* is wrong. Giving Read the Tools tab was the alternative and was refused — it would hand a reading stance batch merge, split and font embedding |
| ⚠ | **★ The conventional DPI was the worst of five measured** — `OCR_DPI = 300` yielded **3.3 %** accuracy against `SW41177.pdf`'s own vector text as ground truth. Measured: 72 → 34.8 %, 100 → 20.0 %, **150 → 44.7 %**, 200 → 27.5 %, 300 → 3.3 %. Cause: `ocrs` resizes every image to its model's fixed input, so **pixel count governs, not resolution**. Now `TARGET_PIXELS = 8_400_000` clamped to 50–300 DPI, with the table in the doc comment and a test pinning that the benchmark sheet lands near 150 |
| ⚠ | **Recognition quality on real scans is still unproven, and the fixture says so** — there is **no scanned PDF in the tree**: 30 documents in `D:\Dev	emp\pdfce\` and one fixture, every one carrying text-showing operators. `fixtures/synthetic-image-only.pdf` is a raster with one `Do` and no text operator; it establishes **the plumbing** — detection of no-text, the Find offer, the recogniser running, the invisible layer at render mode 3, the text becoming findable — and **nothing about quality**, having no noise, skew, ringing or uneven lighting. That caveat is in the fixture's header, in the check's report lines, and here |
| ✅ | **What the operator is told about confidence** — `ocrs` scores nothing, so the dialog recognises, **discloses, and only then offers to save**; the disclosure has force because it can still be declined. One shell-owned sentence sits above the engine's own list: *"This recogniser reports no confidence score for any word, so nothing here has been checked — that is not the same as everything being right."* Tests forbid accuracy claims anywhere on the surface |

★ **Two observations on that second row, both of which change what has to be
built rather than merely how it is described.**

**First, it is currently vacuous — and that is a fact about today, not a
reason to ignore it.** This shell has exactly one save command,
`file.save_copy` ("Save a copy…"), and `text::commands`' own note explains
that the label is load-bearing: pdfce *never* overwrites the original unless
the operator picks it, because a button labelled `Save` would promise
in-place saving, "which cannot ship before autosave and crash recovery
exist." So every mode already behaves the way the instruction asks Read to
behave. **The rule becomes load-bearing the day in-place `Save` lands**, and
that is the day it must be honoured — which is why it is written down now,
against a command that does not yet exist.

**Second, the instruction generalises, and the general form is the better
rule.** Stated as *"OCR prompts Save as in Read"* it is one feature's special
case. Stated as **"Read may produce a new document; it may not modify this
one"** it covers OCR, and it also settles in advance every future capability
of the same shape — flatten, redact-apply, PDF/A convert, page export. It
sits alongside the two exceptions Read already carries (**form filling**, and
now **OCR**), and it explains why both are exceptions rather than
inconsistencies: neither changes the document the operator was given.

| ✅ | **An attribution surface for shipped assets** — 2026-08-14, built as OCR's prerequisite. `PROVENANCE.md` per asset directory, `about.toml`/`about.hbs` generating a **319,430-byte `THIRD_PARTY_LICENSES.md`** into the package, a **File ▸ About** dialog (`file.about`), and a `check-shipped-assets` gate ported from the engine with a **seven-check** self-test. Two surfaces because a GUI needs both: the file carries the licence *texts*, the dialog carries the *attribution*. CC-BY-SA-4.0's **BY** clause requires the notice reach the end user, not merely a reader of the source tree — that distinction is the engine checker's own reasoning and it is why a `PROVENANCE.md` alone was never enough |
| ⚠ | **★ …and building it found three third-party works already being redistributed with no notice at all** — a **pre-existing** gap, not one OCR introduced: the **14 Foxit CFF faces** (BSD-3-Clause, `include_bytes!` in `pdfce-render`), the **Adobe Core-14 AFM metrics** (APAFML) and the **Adobe Glyph List** (BSD-3-Clause), all compiled into the binary. They now appear in the notice and the About dialog. Worth stating plainly: the shell has been shipping them unattributed for its whole life, and nothing detected it until an unrelated feature required the mechanism that could |

| ✅ | **★ Save a copy — wired 2026-08-14, and it had been inert for the whole project.** `file.save_copy` was registered, on the **quick access toolbar**, bound to **Ctrl+S**, and printed the chord in its own tooltip — with **no dispatch arm**. Nothing this shell built could be written to disk: not a dimension, not a markup, not a form fill, not a newly created document. It writes an **incremental** update via `to_incremental_bytes`, which is what the shipped tooltip already promised (*"the edits are appended as an update so the previous version stays intact inside the file"*), so the copy begins with the original **byte for byte** and a test asserts exactly that. Neither `edit_epoch` nor `save_pending()` moves — saving a copy changes nothing about the open document, and zeroing the epoch would silently switch off the OCR preflight's named refusal. **Proven by a second process** opening the saved file and counting one more annotation than the fixture had |
| ✅ | **New PDF** — `file.new`, `Ctrl+N`. The engine has **no document-creation verb** and `document.rs` carries a named permanent invariant forbidding one (*"no separate builder/generation model may ever be introduced"*), so filing a request would have asked the engine to break the architecture that decides its design. Instead a **443-byte blank A4 template** ships as an asset and New opens it through `from_bytes` — an *open*, which is the thing the shell already does well. **A4 not Letter**: Inkscape ships A4, Acrobat produces Letter only on its US branch, and this operator's corpus is A3/A1, which is A-series evidence |
| ✅ | **v0.1.0 released** — 2026-08-14, `github.com/KenM76/pdfceGUI`, public and MIT, with a 19 MB portable Windows x64 zip that needs no install. Engine dependencies moved from relative paths to a **pinned git revision** (`718d1e9d4`), verified byte-identical to the source every binary in the session was built from, so the exe and the published source are the same engine |
| ⚠ | **★ An audit after the release found the save defect was not isolated.** Of 98 registered commands, the ones with no dispatch arm and no guard arm include **`edit.undo` / `edit.redo`** — on the QAT, bound to Ctrl+Z/Y/Shift+Z — and **every page operation**, six of which `shell::menus` offers in the Pages thumbnail context menu while `panels/pages/select.rs` maintains a full multi-select model to feed them. All inert in v0.1.0. Both are engine-supported (`can_undo`/`undo`/`redo`; `delete_pages`/`reorder_pages`/`rotate_pages`) and are being wired. **The lesson is the audit itself**: registration, a ribbon control, a chord, a context-menu row and a tooltip are five promises, and none of them is a dispatch arm |

| ✅ | **★ Undo and redo — wired 2026-08-14, after being inert for the whole project.** On the **quick access toolbar**, bound to `Ctrl+Z`, `Ctrl+Y` and `Ctrl+Shift+Z`, with no dispatch arm — the same shape as `file.save_copy`, and worse once saving worked, because every authoring feature became reachable by an operator with no way to take any of it back. Both go through `vector_edit`, so the epoch bump, the worker cancel and the texture drop come from the one funnel rather than a second mutation path. `undo.available`/`redo.available` are published at last — `app::conditions`' comment saying they were *"deliberately absent: there is no undo stack to report on yet"* is **quoted above the new code**, not deleted. A chord with an empty stack **words a decline** rather than doing nothing silently, because the greyed control is not where an operator pressing `Ctrl+Z` is looking |
| ✅ | **…and the check does not rest on counts, because a plant proved counts are not enough** — a build that mutated the session but never bumped the epoch had **every annotation count already correct** and left the canvas showing the undone state. It was caught by two oracles from *other* subsystems: `objects n=` is emitted once per `(page, epoch)` so a new line means the epoch moved, and `render-spawn` means the texture was dropped. Four plants were run and all four fired |
| ⬜ | **Undo tooltips do not name the operation** — `undo_kind()` returns the `CommandKind` and the catalog could phrase it, but `Command::tooltip` is a `String` fixed at registration and `CommandRegistry` exposes no `get_mut`; it lives in `egui-shell`. The alternative — rebuilding all 101 commands every frame for one string — also changes an icon-only control's accessible name under the pointer. Shipped with the plain label and the one-line fix recorded for whoever next opens the registry |
| ✅ | **Page operations — rotate, delete, move up/down, extract** — 2026-08-14. **Every verb the Pages context menu offers now works**; it has no inert row left. The interesting part is `resync()`, a single choke point hung off `vector_edit`'s success path rather than off the four arms — so **undo gets the page refresh for free**, because an undone delete runs the same engine command backwards through the same funnel. It compares `(page id, rotate)` per page and acts on the difference. **Rotate preserves both selections** (nothing renumbers); **reorder remaps the panel picks** through the permutation and **clears the canvas selection** (its entries carry a page *index*, and adding a remapper to `SelectionState` would put page renumbering inside the module that owns object identity); **delete clears both**. Delete discharges rule 4 with real disclosures from `DeleteOutcome` — dangling bookmarks, stale `/PageLabels`, repaired separations — observed firing on a real fixture |
| ⚠ | **★ ~~Three~~ TWO Pages commands are still live and inert on the ribbon** — `pages.split` and `pages.merge_into`. ★ **`pages.insert_from_file` shipped 2026-08-18** and is struck from this row rather than kept with a note, because the row's contract is that everything in it is inert. Neither of the two is in the context menu (the panel's own test records that exclusion as deliberate), but all three are drawn on the Pages tab and fall through to `command-unimplemented`, which is the **P3 breach** the audit was about. Each is blocked on a *decision*, not on the engine: **split** has `pageops::split`/`plan_split` but no boundary chooser, output directory or name template — and there is no honest default, since splitting a 36-sheet set into 36 files because nobody was asked is not a lesser version of the feature. **merge/insert** shared `pageops::insert`, which **returns the bytes of a new document rather than mutating the session** — so wiring it would have replaced `OpenDoc::session` wholesale and **discarded the undo log that landed the same day**. ★★ **`pages.insert_from_file` shipped 2026-08-18**, and **`merge_document` shipped in `pdfce-core` on 2026-08-19** — one undo entry, session intact, incremental save, fields arriving fillable. So `pages.merge_into` is now unblocked and is the next Pages command to wire. ★ The way that verb came about is worth more than the verb: nobody on the engine side knew the button was parked, and it surfaced only when the shared `gui` column was re-based onto this build and **the Merge row went DOWN**. A capability marked present against a crate nobody used had been hiding a real API-shape gap. Their words: *an engine verb that exists but is the wrong shape for an editor is ours, and worth more to us than a feature request* |

| ✅ | **★ Command reachability is now checked** — `shell::commands::reach`. Every registered command must reach a dispatch arm or carry an **argued** entry in `SCAFFOLDED`; a reason under 40 characters, or one that merely restates the id, is refused, as is an entry whose command has since been wired. **38 of 101 commands have no arm**, and writing their reasons is the deliverable — it forced the question *why is this drawn at all* for each one. **Eleven are marked ★ P3**: the honest answer is that they should not be on the ribbon yet |
| ✅ | **…and the four dispatch arms for commands that do not exist are gone** — `view.zoom_in`, `view.zoom_out`, `view.next_page`, `view.prev_page`, each deleted only after **both** of its live routes were verified individually: the keyboard layer and a status-bar control. `UNREACHED_ARMS` is now **empty and kept**, its length pinned at 0, so a fifth cannot be added quietly. Three stale references fell out with them, including a `decline` test that dispatched `view.zoom_in` as *"an ordinary, unrelated verb"* — a test about any other **command** must name one that exists |

| ✅ | **Read mode and Full screen** — `Ctrl+H` and `F11` had a control, a glyph, a group and a keymap entry each, and no behaviour. ★ **Read mode is not a duplicate of the Read *mode*** — that was the question, and the answer is that they are orthogonal and compose: `mode.read` is a capability set (which tabs exist, whether the canvas authors), `view.read_mode` is a *chrome* stance (whether the ribbon and docks are drawn at all). All three reference applications separate the two, and `Ctrl+H` is Acrobat's chord for the chrome half. The **status bar stays** — it is the only remaining route to page navigation and zoom. Full screen reads its state **back off the viewport** rather than shadowing it, because a window manager can grant or revoke it unasked and a shadow would draw the control pressed over a windowed application |
| ✅ | **Render diagnostics is a report** — the status bar keeps its one-line disclosure and the dialog gets the room; both read the **same** derivation, so the editorial rules (which counters, in what order, which two excluded) are stated once |
| ⬜ | **`view.show_points` was investigated and deliberately left unwired** — the better outcome. Its tooltip promises *the editable points of every part of the object you are working inside*, and **this build draws no anchor mark at any rung**: `canvas/overlay.rs` emits outlines, grips, a ghost, find hits, a text wash and a marquee, and `CanvasTargetProvider` offers `nearest_node` as a *query* with no way to enumerate a part's anchors. Joining the `chrome_for_command` family was checked and refused — every other member is a flag the canvas reads *in order to draw something*, and this one would be the member nothing reads. Blocked on the unsalvaged `vector_edit_tool.rs`; the reason in `SCAFFOLDED` went from *"NO RECORDED REASON ANYWHERE"* to that citation |
| ⚠ | **★ View's seven groups overflow at the shipped 1,100 px default** — found by driving. Only `page_display`, `render` and `navigate` fit; `zoom`, `display`, `panels` and **`window`** fold into the band's overflow menu, so the Read-mode and Full-screen controls just wired are reachable at that size only through the menu or their chords. The two-row change reduced this and cannot fix it — it is an **IA question**: does the View tab want splitting, or Window promoting? |

### Phase 6/S6 — rendering

| | |
|---|---|
| ⛔ | **Deep zoom via `render_page_region`** — the API shipped, but per-viewport regions cost ~700 ms each on a dense sheet. **Blocked on the reusable parsed handle**, filed in `open/request_reusable_parsed_handle.md`. Without it this trades smooth pan for zoom range |
| ⬜ | **`MAX_ZOOM` from measured performance** — not from `f32` (sub-pixel accuracy holds to ~5,000×) and not from the pixmap guard |
| ❌ | ~~Tiled rendering~~ — **cancelled.** A 1×1 *point* region costs 691 ms; a 3×3 ring is a 9× regression. See `BENCHMARK.md` |

### Standing backlog — shell-only work

Exists in `pdfce-core` or `pdfce-cli`; needs a surface, not an engine.

| | |
|---|---|
| ⬜ | Attachments panel · page image export |
| ✅ | **A text tool for Edit** — 2026-08-14. `CanvasTool::Text`, armed by `view.tool_text` beside the hand tool in View ▸ Navigate. **The reference applications disagreed and the disagreement is the interesting part**: Acrobat and SolidWorks resolve text-versus-object *contextually inside one tool*; Inkscape uses a separate Text tool. Inkscape won, and **not on a head-count** — an object marquee over vector content is a surface Acrobat does not have at all, so its contextual answer is not an answer to this conflict. The deciding argument is concrete: a contextual press would make a marquee over a region containing text unpredictable, and that is the commonest gesture in Edit. The variant carries **nothing** where `Markup` and `Measure` each carry a kind — the only thing text needs to persist is that it is armed. It requires **no capability**: retiring it on entering Read would take away a tool that mode plainly permits, and retiring it on entering Edit would delete the feature on the frame the operator enters the mode it exists for |
| 🔨 | **★★ Annotations are SELECTABLE — 2026-08-18, and it was the answer to four operator reports at once.** *"How do I edit a stamp I've applied?"* · *"I still can't get to edit dimension groups when I click on it"* · *"I click and can't figure out how to enable some of the basic stuff"* · *"it feels like nothing is moving forward on these things."* One cause, recorded in the row below on 2026-08-17 and ending *"The second is ours"*: `Selection` named a paint-order index into page **content**, so a stamp, note, shape or ce dimension was not selectable at all — and could not be **deleted** except by `Ctrl+Z` immediately after placing. Now: click one in Review or Edit, it outlines; click away, it clears; `Delete` or Format ▸ Delete removes it through the same `vector_edit` funnel as every other edit, so it undoes and invalidates caches the one way that happens everywhere. ★ **Two predicates were too coarse and neither was a broken mechanism** — the click in Review was consumed by `textsel::takes_the_press` (true for the Select tool whenever `edit_content` is false, i.e. in the mode an operator is in *because* they are marking up), and the Delete key returned on `!edit_content` above any annotation branch. Now one predicate per capability: `author_markup` guards the annotation verb, `edit_content` the content verb, and entering Review from Edit drops a selected path while **keeping** a selected stamp. ★ **My first diagnosis was wrong and an existing test refused it** — I widened `press_kind`'s click predicate believing Review produced no click event; it produces one, and `review_mode_places_markup_but_refuses_content` failed against the change. The routing had no arm, not the predicate. `AnnotKind` carries Markup vs CeDimension **in the type** so the Format tab's routing to `set_dimension_style` is a match the compiler checks — restyling a ce dimension as ordinary markup regenerates it as a bare line with its label and witness lines gone. **No grips on the outline**: they would promise a resize `set_markup_style` deliberately excludes. Still missing and stated: **move, resize and restyle** — that is the Format tab slice, next |
| ⛔ | **★ The Format contextual tab — measured 2026-08-17 and BLOCKED, not merely unbuilt.** `RIBBON_IA.md` §5.8 calls it *"the single largest usability change proposed here … what makes selection **mean** something"* and specifies twenty-four property editors across six selection types. It can carry **one** of them, `Delete`, which it already has. Two independent blockers, neither schedulable away. **(1) `EditSession` has no verb that modifies an annotation** — `add_markup`, `add_text_annotation`, `delete_annotation` and two deletion predicates is the whole surface, so a markup's colour, width, fill, opacity, arrowheads and note text cannot be changed after placement. Delete-and-re-add is not a workaround and is deliberately not built: it loses the annotation's object identity and with it its `/NM`, its z-order in `/Annots`, and any reply thread hung off it as an `/IRT` target. The one exception is the **ce dimension** row — `set_group_style`, `set_dimension_style`, `set_group_scale`, `set_group_standard` all exist, so dimensions have a style model and nothing else does. **(2) The canvas selection cannot address an annotation** — `Selection` is `page + object + subpath + node`, four integers naming a paint-order index into page *content*, which is what makes it immune to zoom and also means a markup or dimension is not selectable at all. The second is ours; the first is filed. `manifest/format.rs`'s header carries the measurement. ★ **BOTH halves are now discharged** — blocker (1) cleared 2026-08-18 with `set_markup_style`, blocker (2) the same day with the row above. What remains is building the tab, which is work rather than a block |
| ✅ | **★ New at a chosen page size — 2026-08-18, and it was an ENGINE gap wearing a GUI gap's clothes.** `NO_SURFACE.md` listed *"new blank page size — A4, baked-in template, none"* as an unbuilt shell surface. It was not: nothing in `pdfce-core` wrote a `/MediaBox`, so the only implementation available to a shell was **one checked-in template asset per size** — ten with landscape, more with ANSI, **and a custom size impossible at any count**. That half-capability was refused rather than built, on the argument that a surface answering twelve of thirteen cases forecloses the fix for the thirteenth, and the gap was filed instead. `EditSession::set_media_box` and `pdfce_core::paper` shipped the next day. `file.new_from_template` now offers **A0–A6, Letter, Legal, Tabloid, Executive, ANSI A–E**, both orientations, and a custom size in millimetres bounded by ISO 32000-1 Annex C.2's advised 14,400 units — advice, and labelled as such, since 32000-2 drops the number entirely. The A series is listed **largest-first**, which is the engine's own ordering and is deliberate: this operator's sheets are A1 and A3. ★ The document is serialized and **re-parsed** rather than handed over as an edited session, so a brand-new A1 sheet is not already modified with `Ctrl+Z` waiting to take it back to A4. `file.new` is unchanged — Ctrl+N still makes A4 and asks nothing, which is Inkscape's split and what two of the three reference applications do. Driven by `new_document_sizes_the_page`, which picks A3, clicks Landscape, and reads the page back **after the re-parse** — the four places a size can be lost each look correct from the one next to it, and the unit tests can only see the first two |
| ✅ | **★ A crosshair pdfce draws itself — 2026-08-18, operator report.** *"The crosshairs when over the canvas are white making it hard to see them."* Nothing in this crate drew them: `CursorIcon::Crosshair` resolves to Windows' `IDC_CROSS`, a **monochrome** cursor whose colour belongs to the operator's pointer scheme and to the accessibility pointer-colour setting — neither readable nor influenceable by an application. So pdfce stops asking for it and hands the OS its own bitmap through `set_cursor_image`, which is a **real OS cursor**: no lag, not clipped by our window, unlike a `Painter`-drawn one. ★ **Two tones, not the inversion the report assumed** — XOR cursors were an AND/XOR mask pair and compositing killed them, since no blend mode means *"invert what is beneath me"*. A black core in a white halo is what Photoshop, Illustrator, GIMP, Inkscape and AutoCAD all use, and it is strictly **better** on mid grey, where an inverted cursor becomes another mid grey. The arms stop short of the centre so the pixel being aimed at stays visible. Black and white are deliberately **not theme colours**: a theme colour contrasts with the *application*, and this must contrast with the *operator's document* — content pdfce does not control and no palette can be right about. ★ **The one feature in this shell a screenshot cannot verify**: Windows composites the pointer separately, so `ui-verify`'s window capture contains no cursor at any price — hence `cursor-crosshair on px=…` / `off`, emitted on change, which is the only machine-readable evidence that the sticky `cursor_image` is both set *and* cleared |
| ✅ | **★ An icon in the executable — 2026-08-18, operator request.** *"make and add a pdf icon to the exe so it shows as the icon when I associate it with pdfs."* Explorer, the Start menu, the pinned taskbar entry and the *Open with* dialog all read that icon **without running the program**, so it has to live in the PE image's `.rsrc` section — `build.rs` puts it there via `embed-resource`, a **build** dependency that ships in nothing. Two halves for two questions: the resource is what the shell reads, and `window_icon()` is what winit reads for the title bar and taskbar, since winit registers its class without one. Both come from **one render pass** so they cannot drift. ★ **The art is a script** — `tools/make-icon.py` draws a page, a folded corner and a red PDF badge from named geometry, so a change is a diff rather than a paint program; P, D and F are rectangles and half-capsules, because there is no font in this repository. Generic, not Adobe's: no Acrobat *A*, no logotype, no brand colour, no typeface of theirs, and *PDF* is an ISO standard's name rather than a mark. `VERSIONINFO` too, because a blank Properties tab reads as an unidentified program — which matters most for something being registered as a file handler. ★ **`PROVENANCE.md` opens by saying the author is NOT the operator**, which no other asset record here has to: the ribbon glyphs are his art and `catalog.rs` forbids a build session adding one. This broke that rule on instruction, and the file says so, names the request, and states both routes to replacing it. It exists because `check-shipped-assets` **failed the build** on the commit that added the icon. Verified by extracting the icon back **out of the built release exe** with Windows' own `ExtractAssociatedIcon` |
| ⬜ | Imposition in the print dialog · insert blank page · push-button field creation |
| ⬜ | Script-driven-field census · unencrypted-wrapper warning |

### Not salvaged yet

Present in the old shell, not yet carried across. All are Class A or B in
`SALVAGE.md` and none is a rewrite.

| | |
|---|---|
| ✅ | **Print dialog** with live preview — split 2,022 → five files at the three questions it answers. Fixed on the way: stale device capabilities after a printer change, `/Rotate` ignored so a rotated page was planned portrait and rendered landscape, and a texture-name collision |
| ✅ | **★ Printing actually reaches a printer — 2026-08-17, and it never had before.** This row was ✅ above for the whole of v0.1.0 while the dialog opened, said *"This build cannot reach a print device"*, and drew **no printer selector, no preview, no page controls and no commit button** — on a machine with **twelve** printers installed. The row was not lying about the dialog; the dialog was lying about the build. `dialogs::print::spooler` is the adapter, it was written as four *holes* returning `NotLinked` while `pdfce-print` was genuinely unlinked, and its header set out in full the two edits that would fill them. **The manifest line landed; the holes did not.** `pdfce-print` was compiled and linked into every shipped binary and no source file named `pdfce_print` outside a doc comment. **The suite was green because a test held the defect in place**: `every_hole_refuses_rather_than_guessing` asserted all four calls refused — correct while unlinked, a lock the moment it was not, since filling the holes would have turned it red. *A test that pins a refusal must name the condition the refusal is conditional on, or it outlives its own premise and starts defending the absence of the feature.* Replaced by its own inverse, plus `print_dialog_reaches_the_spooler` in `ui-verify`, which **fails on the previously shipped build and passes on this one** — and which never presses the commit button, because a harness that can start a print job will eventually start one by accident |
| ✅ | **★ Paper size, tray and the driver's own Properties… — 2026-08-18, the surface the operator asked for three times.** *"acrobat reader has those options, and pretty much every program I have ever seen lets you press a properties button beside the selected printer in the drop-down menu to open the printer options."* All three asks were **one engine defect**: `build_devmode` synthesised a zeroed `DEVMODE`, and tray, paper and orientation all resolve through it. `pdfce-print` fixed it and shipped `printer_forms`, `PaperSelection`, `printer_caps_for`, `edit_printer_configuration` and `spool_with_config`; this is the shell half. **Paper** is a combo beside orientation in Pages & Layout, populated from the driver's own list, and the job is **re-planned** through `printer_caps_for` — without which a job asking for A3 on a Letter-default device would be laid out for Letter and printed on A3, with no clip reported and nothing to explain it. **Properties…** sits beside the printer combo and opens the *driver's* dialog, whose result is carried into the job — up to **97 % of a real `DEVMODE` is driver-private tail** holding media type, quality and finishing that pdfce cannot name and must not drop. **Tray-by-sheet-size is back**, deleted the day before because the engine read the field nowhere, and it is drawn in **all three** capability states: `DC_BINS` on Microsoft Print to PDF returns nothing while that device already picks by form, so a bool would have hidden a control from a device that was doing the thing by default. Two disclosures the operator could not otherwise learn: a chosen paper is a **request** two measured drivers were found silently ignoring, and a job whose driver would not report its settings prints with the rest **fallen back**. Driven by `print_paper_changes_the_plan`, which reads `paper=` and `sheet=` off one trace line and requires both to move — it never presses Properties… (a vendor's modal, unpublishable and un-dismissable by a harness) and never presses commit |
| 🔨 | **Measure tools** — **salvaged 2026-08-14**, whole: `canvas/measure/{pick,scale,state}.rs` + `canvas/snap.rs`, 45 tests carried, no engine API had moved. Linear, two-line **and radius/diameter place dimensions**, and the snap query is wired. `canvas/measure/circular.rs` joined them when the radius/diameter tool was armed — the one measure gesture that does not end itself, so the machinery for *saying when it is over* is a subject the other two have nothing corresponding to. ★ **The scale model is reachable as of the Set-scale window**, and the whole group model with it: `panels::dimension_groups` (2026-08-19) creates, renames, deletes, recalibrates and restyles a group, and carries the ***Draw into*** picker — which is the control the feature had been missing entirely. `MeasureState::group` had existed since the Phase 7 salvage, documented as the group picker, and **nothing in this build ever wrote to it**: a second group could be created from the CLI, carry its own scale, and be joinable by nothing |
| ✅ | **Redaction — mark, review, apply, with the true-removal proof salvaged** — 2026-08-15. Mandatory rather than convenient: core still has **no verdict type**, `apply_redactions` returns a *report*, and `pdfce-cli` exits SUCCESS on a file it never verified — so the proof came across from the old shell with the code. **Made unskippable four ways**: the prepared bytes are private with no accessor and a `Debug` that reports only a length; the single exit re-proves over the exact buffer between it and the syscall, so the guarantee belongs to the *write* rather than the constructor; the residual acknowledgement is a required argument rather than a convention, so a forgotten checkbox yields a refusal and not a partly-redacted file; and `redact::sealed` parses every `.rs` with `syn` to assert `apply_redactions` has exactly **one** call site — failing closed on zero as well as on two. **Always a save-as**: overwriting the source would destroy the only remaining copy of the content being removed, and the harness asserts byte-for-byte that it did not. Proven by a **second process** extracting nothing from the redacted page, with the falsifier running on every invocation — the secret must be *found* in the fixture first, so a scan that cannot see it is a failure rather than a pass |
| ✅ | **★ Markup ▸ Style — the group that was a caption over an empty band.** `RIBBON_IA.md` §5.5 specifies *Colour · Line width · Fill · Opacity* and the manifest has declared `Item::custom("colour_swatch")` since S2 — **and no renderer ever matched that kind**, so the shell reserved the item's space, the application declined to draw it, and the group rendered a caption over nothing for the whole of v0.1.0. The pen was two hard-coded constants, so every mark this shell has ever authored is red at 2 pt with no way to change either; §5.5 predicted the operator's report in advance — *"which is why a placed markup feels final"*. Now: **two colour swatches** (ink and highlighter, separate because setting a pen colour must not change your highlighter) and a **width** in points over the pen's own 0.25–12 range. The pen rides **in the action**, not read at apply time, so a mark is authored with the pen the operator had when the gesture completed and the action stays replayable. **Fill and Opacity are still ⬜ and for different reasons**: fill is a design decision on record (*"a filled comment shape hides the drawing it is a comment about"*), opacity is `/CA` and blocked on the engine — absent rather than greyed, because no setting in this build makes it reachable. **A third shape of invisible wiring**, and the quietest of the three: the manifest test asserted the item was *declared* and passed correctly, and the reachability check cannot see a `Custom` item at all because it carries no command id. Caught by `ui-verify --check markup_style_group_is_drawn`, which fails on the previously shipped build |
| ⬜ | **Canvas drag-to-mark for redaction** — panel marking (whole page, literal text, pattern) covers all three routes the tooltip promises, none of which is a drag. The gesture would need a `CanvasTool` variant, a capability entry, an Escape rung, an overlay preview and an `Action` carrying page-space quads — the tool-substrate warning applied to the one irreversible verb in the program |
| 🔨 | **Forms** — **fill** ✅ (all three modes since 2026-08-14), and ★ **Reset** and **Flatten** are reachable too, *through the panel rather than through the ribbon*: both are buttons in the Forms panel's own collapsed sections, calling `reset_form` and `flatten_fields`, while `edit.form_flatten` on Edit ▸ Forms is still `SCAFFOLDED` and inert. **That split is a reporting hazard worth naming**: a register that records the *command id* reports Flatten as unreachable and Reset as absent, when an operator can do both. Record the **surface**, not the id. Create field, rename, delete and FDF/XFDF/CSV are still ⬜. Filling found a genuine `pdfce-core` defect: `fill_refusal()` omits two of the three guards `fill_guards()` applies |
| ✅ | **Fill a field on the canvas** — click the widget on the page and type; Enter or clicking away commits **one** `FormEdit::FillText`, Escape abandons the draft without also ascending a selection rung. Check boxes toggle, radio widgets select *that widget's* on-state. **No `CanvasTool` variant**: a widget's `/Rect` is a region the file itself marks interactive, so a press inside it is unambiguous — markup needed a variant precisely because a drag on blank paper is not. An *unfocused* widget registers **no interactive rect**, so pans, marquees, hover and Ctrl+wheel over a field are untouched; only the focused field is a real `TextEdit`, and it therefore owns its own drags. The hit test takes **no tolerance** — a tolerance exists so a hairline can be hit, and form fields sit a point apart, so a 6 px catch radius would silently focus the neighbour of the field aimed at |
| ✅ | **What a fill inferred is in the status bar** — `applied_autosize` and `unencodable_chars` are the only two facts of a fill **not re-derivable from the saved document**: afterwards they look exactly like the author's decision. The Forms panel showed them, which sufficed while the panel was the only way to fill; canvas filling can happen in **Read mode with the panel closed**, so the bar — present in every mode — now carries them too. Keyed on the edit epoch, so any later edit or an undo retires the sentence with nothing having to remember to clear it, and asserted not to change the bar's height (R128) |
| ⬜ | **Highlight fillable fields** — a View ▸ Display toggle tinting widget rects, Acrobat-style. Today the *cursor* is the whole discovery mechanism, which is honest but weaker; it is also the only thing that would make an **undrawn** field discoverable |
| ⬜ | **Field list in tab order** — a **read-only**, per-page, widget-level view beside the fill list, which stays in `/AcroForm /Fields` order because that matches the printed form. It must report a page with no `/Tabs` as **absent** and must not translate that into a mode name: pdfce's roadmap records *"what Acrobat's Unspecified tab-order state mechanically denotes"* as **unsourced after two attempts**, so naming it would assert something nobody has been able to source |
| ⛔ | **Reordering tab navigation** — the operator asked for it; no verb can do it. None of `pdfce-core`'s 108 public `EditSession` verbs reorders `/Annots`, writes `/Tabs`, or writes a field ordering. Filed 2026-08-14; **blocked on their F4**, which is itself blocked on a spec dispatch — and they declined to build around the block, correctly, because the semantics are unsettled and a permutation verb would bake a wrong model into an undo entry. Three things are already agreed for the day it lands: refuse a non-permutation as `reorder_pages` does, one undo entry, and **disclose that paint order moved with it** — `/Annots` order is both. Note the request's own reasoning was an *extension* of their rule R104, whose stated scope is field **insertion**; the paint-order consequence is identical but the inference is ours |
| ✅ | **★ Settings dialog — ported 2026-08-17, and nine of its thirteen settings had never been read by anything.** `file.settings` was registered, drawn on File ▸ pdfce and inert for the whole project, which is what the operator hit: *"no surface for changing or editing the settings."* All thirteen spec-ambiguity choices are reachable, in **seven** groups rather than six — `parallel_epsilon_degrees` sat under *Copying and extracting text*, where nobody with the symptom *"my dimension came out as an angle"* would look, and now has a **Measuring and dimensioning** group. Colour is the expanded one, resolving a contradiction where the source's prose said Colour and its code opened Appearance. **The bigger half was not the window**: measured against the old shell, four of its thirteen settings reached the code that acts on them and **nine did not** — persisted, shown, edited, then discarded by every call site that built its own `ExtractOptions::default()` / `RenderOptions::default()` / `SaveOptions::identity()`. `app::settings` is the one funnel and a `syn` check fails the build if any file bypasses it. Five disclosures the old window owed and did not make were added: four guessed defaults that read as recommendations, the fact that *Leave it out* makes an all-unmappable run **disappear** rather than become markers, and the `/ActualText` bound that applies whichever option is chosen |
| ✅ | **★★ The page cache was a frame buffer with extra steps — fixed 2026-08-19 on the operator's report** *("increase cache to maximum for page view so they don't constantly redraw with larger files")*. `StripRasters::retain` took the **visible** page set and dropped everything outside it, every settle, and the 48 MB budget it also enforced almost never bit because the set it was handed was already tiny. So scrolling one page and back re-rastered — on `SW41177.pdf` that is a full strip decode for a page whose pixels had been thrown away a second earlier, and it is exactly the *"constantly redraw"* the report names. **A cache keyed on what is on screen right now is not a cache**; it is a frame buffer, and the LRU underneath it had nothing to do. `retain` now takes only the budget, so the eviction order is genuinely least-recently-used and a page that leaves the viewport stays until something needs its texels. ★ **A test was pinning the defect** — `pages_that_left_the_viewport_are_dropped` asserted the behaviour being removed, which is what a test does when it is written from the implementation rather than from the requirement; it was reversed in place to `…_are_kept_until_the_budget_bites` rather than deleted, so the file records that the property flipped deliberately. The ceiling went 48 MB → **256 MB**, and it is now a **preference** — Settings ▸ Drawing the page, four steps (Small · Medium · Large · **Maximum**), with the megabyte figure derived from the texel count rather than stored beside it, because two numbers that must agree eventually will not. Driven by `pages_stay_drawn_when_you_scroll_back`, whose oracle is a **duplicate page number in `strip-raster-requested`**: scroll forty notches down and forty back, and a page asked for twice is a cache that dropped it |
| ✅ | **★ The seven `view.*` settings, resolved 2026-08-17 — two built, five deleted.** All seven were registered, drawn on the View tab, and inert. Checked against the engine: **render strategy** has no tiled-progressive path in this shell, **thin lines** and **antialiasing** have no `RenderOptions` field at all (`interpret.rs` sets `anti_alias: true` as a literal), and **floating panels** names a dock mode `egui-shell` does not have. **App initiative** is the instructive one: its specified default is *Never*, nothing in this build floats a surface unasked, so the control existed to switch off a behaviour pdfce does not have. Four capabilities that do not exist plus one already true by construction. The two that were real — **render quality** (a raster-scale multiplier; `viewer::raster_scale` had none, so this is new capability rather than an exposed constant) and **zoom settle** (`ZOOM_SETTLE` was a compiled-in 150 ms) — became Settings ▸ **Drawing the page**, because a value set once and forgotten is not an activity, which is what P2 says a ribbon tab picks. All seven **unregistered**, not hidden, on R8. The now-empty View ▸ Render group was deleted rather than shipped as a caption over nothing — the same call made for Edit ▸ Clipboard. 32 groups → 31; `DIRECTED` 8 rows → 1; `SCAFFOLDED` 29 → 22 |
| ✅ | **★ Text box, sticky note and stamp — the revisioning set finished, 2026-08-18.** All three were registered, drawn on the Markup tab and had **no dispatch arm** for the whole life of the project. The recorded reason was accurate rather than an excuse — `canvas::markup`'s own table: *"Text-bearing, not geometric. A different gesture (place, then type) and a different spec type."* Building them took a fourth `CanvasTool` family, a `DragKind` **whose release does not author**, a `GestureOutcome` variant, two actions, a dialog and a click path. ★ **The release-not-authoring is the property that needed driving**: a build where it authored directly passes every unit test in `canvas::textannot` — the spec builder is pure and correct either way — and puts an *empty box* on the drawing every time the operator lets go of the mouse. Proved: *the release authored nothing — still 0 annotation(s) — and opened the dialog instead*, then Accept took it 0 → 1. ★ **A mistake kept in the docs**: the first draft invented seven stamp label strings, and `TextAnnotSpec::Stamp` takes a `StampName` — ISO Table 181's standard set — so every stamp would have carried `/Name /Draft` regardless of what it read, and any reader but pdfce would show *Draft* beneath a stamp saying APPROVED. An annotation disagreeing with its own appearance, invisible from inside the GUI. Three decisions argued at their sites: the sticky is **click**-placed because `/Text` is fixed-size and `NoZoom` so its rect is discarded; a text box is **bordered** unlike Acrobat's default, because on a drawing sheet a borderless caption is indistinguishable from the drawing's own annotation; the popup starts **closed** because one that opened itself covers the drawing it is about |
| ✅ | **★★ Revision clouds — and this row is kept, struck, because of what it got wrong.** It read *"`grep Cloud` and `grep BorderEffect` over `pdfce-core` return nothing … nothing on this side is missing"*, which was true when it was written and had stopped being true without anything failing. **A blocker naming a repository this project does not build cannot fail a test** — nothing compiles differently, nothing lints, CI stays green *precisely because the feature is still absent* — so it went on being read as current for three weeks while the operator asked. One `grep` disproved it. Every external blocker in this file is now a **dated citation** rather than a verdict: *"`MarkupSpec` has no `Cloud` variant — `annot_author.rs:280`, checked 2026-08-14"* is falsifiable in ten seconds and visibly breaks when the symbol appears; *"blocked on the engine"* is neither. `NO_SURFACE.md` §1c and `D:\dev\rag\rust\` carry the whole argument |
| ✅ | **★ Set the scale by measuring something on the drawing — 2026-08-17, and the model had been there since the Phase 7 salvage.** The operator's report was *"still missing the feature where we set the scale by selecting two lines or points and defining what that distance represents"*, and `dialogs::scale`'s own header had already named the gap: the real-length path *"is offered when a reference line has been drawn, and drawing one is a canvas gesture (`ScalePick`) that is not yet armed by any command."* **Not one line of scale arithmetic was added.** `ScalePick`, `ScaleEntryFields::sync_real_length`, the back-calculation through the engine's own `preview_group_scale` and even `MeasureState.scale` were all present, pure and unit-tested — the last still carrying a doc comment referring to a `CanvasTool::MeasureScale` from the **old** shell. What was missing was arming, click routing and a way back into the dialog. Now: *Measure ▸ Set scale* opens the dialog, **Measure it on the drawing…** closes it and arms the pick, two clicks draw a reference line with the same snapping and H/V constraint as a linear dimension, and the dialog re-opens on the **real-length** path with the measured length shown. A button rather than a second ribbon control, because the dialog is where an operator already is when they discover they need this — and because it needs no new registered command. Driven: 396.08 pt measured for a 400 pt span, the 3.92 pt being the snap correctly pulling picks onto nearby geometry |
| ✅ | **★ UI scale — the accessibility gap, and a capability that was switched off rather than never built.** `NO_SURFACE.md` recorded *"no UI-scale or base-font-size control anywhere"*, and it was not a hard-coded constant like its neighbours: `egui` has offered `Context::set_zoom_factor` throughout and **no line in this crate ever called it**. Every control, label and icon has been drawn at one size on every machine for the life of the project. **Why nobody noticed is the reusable part** — `egui` ships a built-in `Ctrl` `+`/`-`/`0` handler for exactly this, so on a stock `eframe` app the capability appears to exist without anyone building it, and this shell switches that handler off *for a good reason* (those chords mean **page** zoom, as in every browser and in Acrobat). Closing the one path that would have surfaced it removed a feature nobody had decided to have. Now `Settings ▸ Appearance`, 0.8–2.0 in twentieths, shown as a percentage: a **multiplier on the OS display setting**, never a replacement, so an operator who fixes their scaling once does not have to fix pdfce separately. Live-previewed from the draft **like the theme and for the identical reason** — you cannot judge a scale from a number, you choose it by seeing whether you can read the ribbon — which makes Appearance the group of the two settings that take effect before Save. A settings control and **no chord**: Inkscape and SolidWorks both put it in preferences and neither binds a key, Acrobat has no such control, and `Ctrl+` digits here are the mode selector |
| ✅ | **★ The freehand tolerance follows the pen again — a defect the Style group introduced and this module had predicted.** `ink::SIMPLIFY_TOLERANCE_PTS` was a `const` = `PEN_WIDTH_PTS / 4`, and the derivation is not decorative: Ramer–Douglas–Peucker bounds how far the drawn centreline can move, so setting ε to half the stroke's half-width is what guarantees *no pixel of the mark moves outside the mark*. §3.2 wrote down the rule for the day the input changed — *"if the pen ever becomes an operator control, the tolerance follows it"* — the pen became one on 2026-08-17, and the `const` went on deriving itself from the **default** 2 pt. At the 0.25 pt pen that exists to match a CAD sheet's own linework, a fixed 0.5 pt ε is **four times** the half-width, so the simplification could author a curve the operator did not draw, silently, on the thinnest strokes. Now `Pen::simplify_tolerance_pts`, beside the width it derives from, and asserted at **both ends** of the operator's range — the old test compared two constants and so kept passing through the whole regression. **The lesson is not about ink:** a constant derived from another value is safe exactly as long as that value is also a constant, and when the input becomes settable nothing about the expression looks wrong |
| ✅ | **★ The opening view — two more preferences, 2026-08-17, and the first two from the `NO_SURFACE.md` sweep rather than from the commissioned list.** `ViewState::default` held **fit-page** and **all three View ▸ Display overlays off** as compiled-in constants, and the sweep's note against them was *"toggles exist; the default is not settable."* That is worse than it sounds, because there is no per-document memory of them either — `viewer::remembered` persists the page-display arrangement and nothing else, deliberately — so an operator who works with rulers on flicked the same switch on every document, forever, and the program never learned. Now **Settings ▸ Drawing the page** carries *How a page is sized when a document opens* (whole page · full width · actual size) and *What is switched on when a document opens* (rulers · grid · guides). The trio is **one setting with three switches**, not three settings, because they interlock: a guide is dragged out of a ruler gutter, so guides-without-rulers is a switch that appears to do nothing, and saying that once beats printing it three times. Read **once per open**, in `PdfceApp::adopt` — not in `adopt_settings`, which also runs on every settings Save and would have snapped the operator's current page back to fit and switched their rulers off. Guides **OR** rather than assign, so a document that has remembered guides still opens showing them: work the operator did outranks a default about documents in general |
| ✅ | **★ …and the copy contract had stopped covering the newest group.** `every_setting_states_its_silence_and_its_radius` listed **thirteen** triples — exactly the `pdfce_core::settings` entries — and neither of the 2026-08-17 preferences had ever reached it, so a test reading as though it covered the window was checking a subset of it. The `header` helper's required arguments still forced three answers; nothing forced them to be non-empty, which is the whole failure the test exists for. Fixed both ways: the list is now **17**, and a new `syn` check parses the eight group modules and counts the `widgets::header` calls the dialog **actually makes**, asserting it equals what the catalog describes. A catalog cannot notice a setting that is not in it, so the guard had to count from the other end. `include_str!` makes a moved module a compile error, so "scanned nothing" cannot pass as "found nothing" |
| ✅ | **`userdata/preferences.txt`** — the shell's own store, beside `settings.txt` under the same roof so the update instructions cover it unchanged. Separate from `pdfce_core::settings` because that file's every entry cites a clause the standard leaves silent and *how sharply a page is drawn* cites nothing; filing it there would make the settings window's own opening paragraph dishonest. Same flat `key = value` grammar and the same **per-key recovery**: an unknown key is kept and reported, a bad value falls back for that key alone, one bad line never discards the rest |
| ✅ | **Icons** — **72 glyphs**, rasterized at physical pixel size, tinted from the theme. All **65 keys** named by commands resolve, asserted against the live registry. An unknown key draws a **visible slashed mark**, never a blank: the label fallback is decided upstream of the painter, so drawing nothing silently reproduces the blank boxes it exists to prevent |
| ✅ | **Every ribbon control is a glyph or a recorded refusal** — 2026-08-14. It was 47 named and 41 bare with **no rule behind which was which**, so a band drew pictures and words side by side and the ribbon read as half-finished because it was. Now **77 of 88 named, 11 refused**, each refusal argued at its own registration: `view.zoom_actual` because the icon spec argues against it by name, the five `view.render_*` because their labels *are* the control's content, `view.app_initiative` because every honest drawing of it pictures what its default forbids, and the three mode segments because the renderer draws text there and a key would be art nothing can draw. Three glyphs were redrawn after being rendered at real size — `reset-layout` read as an umbrella, `form-flatten` was indistinguishable from `download`, and `zoom-region`'s lens filled to a disc |
| ⬜ | **Text editing** — the whole tool |

---

## Not planned, and why

| | |
|---|---|
| ❌ | **A Home tab** — would mirror commands across tabs and re-create the Pass 47.1 defect |
| ❌ | **Automatic reflow on edit** — R75: reflow invents line breaks the file never stated. Make it reachable, not silent |
| ❌ | **Tiled rendering** — measured as a 9× regression |
| ❌ | **JavaScript execution** — standing refusal |
| ❌ | **Provisional styling on the canvas** — Rule 4. Disclosure lives off-canvas; a second rendering path for the same content drifts |
